# 2026-W33 项目历史

## 2026-08-10 09:53:14

### ChimeraX `runscript` 入口真实集成修复

- 用户已在 ChimeraX 1.12 中打开项目目录内的原始 `Nb252-optimization.cxs`，并以截图确认 `NK2R-252.pdb` 的合作者橙色界面注释可见。ChimeraX 命令历史进一步确认当前打开路径为仓库内 CXS，而不是桌面根目录的同哈希副本。
- 连续运行既定 `runscript` 命令时，ChimeraX 只回显并折叠重复命令，`data/structures/cxs_exports/` 及其父目录均未生成；因此没有把该尝试记录为成功导出，结构、映射、界面和候选设计阶段门继续保持 `blocked`。
- 根因是 `export_cxs_session_chimerax.py` 原先只在 `__name__ == "__main__"` 时调用 `main()`，而 ChimeraX 1.12 的 `runscript` 将 Python 脚本置于具有唯一模块名、`__spec__ is None` 且注入 `session` 的 sandbox 中执行。旧入口因而只定义函数后静默结束，没有进入任何输入、模型或写出检查。
- 导出器现通过 sandbox 特征分派入口：ChimeraX `runscript` 环境直接调用 `main(session, sys.argv[1:])`，普通 `importlib` 导入保持无副作用，普通 Python 直接执行仍明确提示必须使用 ChimeraX。没有使用 `SystemExit(0)` 结束 ChimeraX sandbox，也没有放宽源 CXS 路径、哈希、模型身份或输出目录安全检查。
- 外部 CLI 合同、三个必需参数和输出目录合同均未改变。预计和本次修复/测试实测均远低于 1 小时，不需要 Slurm、checkpoint 或服务器环境。
- 新增真实执行语义回归测试：模拟非 `__main__` 的 ChimeraX sandbox 并断言注入的 `session` 传入 `main()`；同时验证普通模块导入不调用 `main()`。完整本地验收为 `93 passed, 1 skipped, 4 subtests passed`，唯一跳过仍是 Windows 真实 symlink 权限测试；`pip check`、Python `compileall` 和 `git diff --check` 均通过。
- 修复后尚待用户在当前正确打开的 ChimeraX 1.12 会话中重新运行原命令。只有生成并验证 `cxs_export_manifest.json`、5 个 mmCIF 和颜色清单后，才能首跑 Gemmi 结构基线并进入链角色/橙色人工审核。

### Git sync status

- 本次入口修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:01:46

### CXS真实模型名合同修正

- 修复 sandbox 入口后，ChimeraX 已真实进入导出器的模型身份检查，并报告三个原子模型实际名称为 `NK2R-252.pdb`、`NK2R-NKA` 和 `fold_2r_252_nomg_model_0.cif`。这证明先前记录的 `NK2R-NKA.pdb` 与原会话实际名称不一致；本次失败发生在任何 staging/output 写出之前，`data/structures/cxs_exports/` 仍不存在。
- 按真实会话证据将导出器和 Gemmi 结构基线的唯一模型名合同同步修正为 `NK2R-NKA`，不要求用户重命名会话模型，也不增加别名、模糊匹配或自动 fallback。对应更新项目级科学上下文、handoff及测试，使导出和下游结构入口继续使用同一精确三模型集合。
- 外部命令、源CXS哈希和输出路径均不变。完整本地验收仍为 `93 passed, 1 skipped, 4 subtests passed`；`pip check`、Python `compileall` 和 `git diff --check` 均通过。当前仍待用户在正确打开的原会话中再次运行相同命令，真实导出成功前所有结构相关阶段门继续为 `blocked`。

### Git sync status

- 本次入口与模型名合同修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:06:30

### CXS真实导出与Gemmi首轮结构清单

- 用户在正确打开仓库原始CXS的ChimeraX 1.12会话中重跑修复后导出器。`data/structures/cxs_exports/cxs_export_manifest.json` 为 `status=pass`：源CXS SHA-256仍为 `1bc636c28f66ae60edc658d2e1c4aad0b07f4141ca5411c78662aa19da793c4d`且运行前后身份未变；会话含3个目标`AtomicStructure`和2个名为`missing structure`的非原子pseudobond child。
- 导出生成3个native-frame和2个实验参考坐标系mmCIF、`cxs_residue_colors.csv`、manifest及run summary，共8个文件；5个mmCIF均由Gemmi 0.7.5成功读回。ChimeraX提示`Not saving entity_poly_seq for non-authoritative sequences`是`bestGuess false`下不补写非权威序列的保守行为，不是导出失败。
- 三个原子模型计数分别为：实验`NK2R-252.pdb` 2链/396残基/3090 atom sites，`NK2R-NKA` 2链/307残基/2424 atom sites，AF3 VHH 1链/126残基/988 atom sites；全部occupancy为1且无非空altloc。颜色清单共829行，其中唯一精确内置橙色类为`[255,165,0,255]`，在实验模型chain C的24个残基上同时出现在ribbon和atom通道；无自定义颜色或无法回映的surface颜色。
- 在全新目录`docs/result_artifacts/input_baseline/structure_review_20260810/`完成Gemmi结构基线首跑。export和inventory为`pass`；跨工具模型/链/残基/atom object/site计数一致，两个reference-frame文件的全原子刚体残差均低于0.001 Å。按既定合同，首跑因5个链角色、橙色绑定和权威构建体仍待review而返回code 2/`status=blocked`，并生成inventory、residue inventory和`baseline_review_template.json`，未自动填写科学身份。
- 当前需要用户集中确认链角色和唯一橙色类；权威构建体边界可继续保持pending。真实导出与首轮清单是科学阶段进展，但尚未生成Nb252结构映射、FR-only对齐、严格`<4.0 Å`界面或临时保护集合。

### Git sync status

- 真实导出、首轮结构清单、入口/模型名修复、测试和文档尚未提交或推送；等待review确认及后续结构基线重跑。

## 2026-08-10 10:27:21

### 链角色、橙色注释、结构映射与临时界面基线完成

- 用户在 ChimeraX 1.12 中逐项确认：实验模型 `#1/C` 为 Nb252、`#1/R` 为 NK2R；NKA 模型 `#2/L` 为 NKA、`#2/R` 为 NK2R；预测模型 `#3/A` 为 AF3 Nb252；24 个绿色选中残基与合作者橙色区域一致。确认已写入 `structure_review_20260810/baseline_review.json`，绑定精确 RGBA `[255,165,0,255]` 与 `atom+ribbon` 渠道；权威构建体 review 仍保持 `pending_collaborator_confirmation`。
- 首次 confirmed structure run 忠实阻断于实验结构缺失片段造成的多最佳序列比对。检查发现实验 VHH 的 115 个有坐标残基全部具有 1–128 范围内唯一、严格递增的整数 source auth 编号，并逐位匹配 reported Nb252。`polymer_mapping` 1.1.0 因此新增严格直接路线：仅在无插入码、编号唯一递增、范围有效且每一 WT 均一致时使用 source auth 编号；否则仍进入原 exact-first/BLOSUM62 安全门。
- 第二次 run 暴露 AF3 导出保留 536-aa source polymer 元数据，而当前坐标仅含其起始 126-aa VHH；536-aa 序列的前 128 aa 与 reported Nb252 完全一致。映射器现仅在较长 polymer 中存在唯一、完整、逐字一致的 128-aa 连续片段，且全部有坐标残基落在该片段内时接受该片段；额外序列不被称为 VHH，内部冲突、多匹配或坐标越界仍阻断。失败 run 及其 run summary 予以保留作为负结果。
- 最终结构制品位于 `structure_reviewed_20260810/`：实验 VHH 以 source auth exact-WT 映射，AF3 VHH 以 source label ID 加唯一 authoritative segment 映射；residue mapping 为 `pass`。82 个共同 framework Cα、无 outlier rejection 的 Kabsch 拟合 RMSD 为 0.631994 Å；拟合后 CDR3 Cα RMSD 为 6.490853 Å，明显高于 FR aggregate 0.631994 Å。这些是结构计算结果，不是实验功能效应。
- 严格非 H/D、正 occupancy、polymer atom-center `<4.0 Å` 界面得到 246 个原子接触对和 24 个 VHH 残基；这 24 个残基与确认橙色集合完全相同，保护并集仍为 24 个。该集合仅是保守突变保护标志，不是能量热点集合。
- finalizer 已使用真实 mapping/interface 制品重建 128 位点绘图数据、PNG/SVG 和阶段门。期间修复 summary 对缺失坐标位点合法 `not_evaluable` 状态的读取，不再误当非法布尔值；人工查看 PNG 后又将旧的“结构轨道仍为灰色”说明替换为当前适用的灰色单元含义。`local_baseline_build=pass`；`candidate_design_release` 仅因 authoritative Nb252 construct 阻断；`pooled_expression_model_release` 仍因跨 assay pooling 证据阻断。
- 本轮计算实测远低于 1 小时，不需要 Slurm。最终验证：`96 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 已提交并推送至 `origin/main`。
- 结果提交：`1e6bd655ef316a9f5e10ea7331f3eceff302bc45`（`Complete reviewed stage-1 structure baseline`）。

## 2026-08-10 12:08:42

### 合作者确认解除构建体与跨来源可比性阻断

- 项目用户转达合作者明确确认：LTT、WCC、LLJ 不同来源的 reported yield 可直接相互比较；Nb252 末端 `SSGS` 不是 linker，必须保留原始 128-aa 序列。该确认分别写入 machine-readable expression confirmation 和既有统一 `baseline_review.json`，证据来源标为 `collaborator_via_project_user`。
- Nb252 authoritative construct 现固定为 reported sequence 1–128、`construct_scope=mature_vhh`、terminal `GS=included_in_authoritative_construct`。ANARCII 仍只编号前 126 aa；未编号不再解释为构建体外序列。新目录 `structure_released_20260810/` 的 export、inventory、chain role、mapping 和 authoritative sequence 均为 `pass`；重新绑定 review 哈希的 `interface_released_20260810/` 仍复现原 246 个原子接触对和 24 个 VHH 界面残基。
- 表达审核升级为 1.1.0并新增显式 `--comparability-confirmation` 输入。47 条样本的跨来源 pooling 状态现为 `pass`，允许保留原始观测语义的联合探索：31 条 LTT/WCC individual approximate 仍按数值处理，9 条 LLJ group lower bound 与 7 条 group approximate 仍为删失/分组信息，不得插值成个体点估计。协议字段未知不再阻断跨来源比较，但 `nb252_transfer_status` 对 47 条仍为 `blocked`，因为可比性不等于对未见突变的预测准确性验证。
- 用户同时明确优化必须维持实验观察到的表位和结合构象；合作者没有额外人工突变禁区，也没有既往突变效应数据。24 个严格 `<4.0 Å` 界面位点继续作为“突变时谨慎”的可复现注释，而不是绝对禁止或能量热点。
- finalizer 已用 released structure/interface 与更新的 expression audit 重建 canonical summary。当前 `local_baseline_build=pass`、`candidate_design_release=pass`、`pooled_expression_model_release=pass`；独立的 `nb252_expression_transfer` 子门仍为 `blocked`，需后续模型验证而非更多协议询问。
- 本次确认同步、轻量重跑和测试实测远低于 1 小时，不需要 Slurm。最终验证为 `97 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 本次合作者确认、审核逻辑、released制品、阶段门、测试和文档尚未提交或推送。

## 2026-08-10 15:03:19

### 末端 `SSGS` 设计约束澄清

- 用户明确“保留末端 `SSGS`”的操作含义是候选设计时不得突变或删除该四肽，而不只是把它纳入权威参考序列。该约束现固定为 reported-sequence positions 125–128 的硬冻结集合；由于末端 `GS` 不在 provisional ANARCII/IMGT 编号跨度内，后续必须用 reported-sequence index 表达，不能为其虚构 IMGT 位点。
- 新增机器可读设计约束 `docs/result_artifacts/input_baseline/reviews/nb252_design_constraints.json`，记录母本哈希、不可变位点和证据来源；同步修正 `AGENTS.md` 与 handoff。该澄清不改变既有结构映射、界面结果或三个已通过阶段门，也不释放仍需模型验证的 `nb252_expression_transfer`。
- 复核现有证据后，没有必须继续向合作者确认、否则会阻断第二阶段计算基线的重要信息。最终实验面板容量和是否进入组合突变属于看到单点候选景观后的项目决策，可后置，不需要现在补问无关实验细节。
- 本次仅为设计合同与文档更新，预计和实测均远低于 1 小时，不需要 Slurm。复核确认权威母本长度为128 aa且reported positions 125–128精确为`SSGS`；全套验证为`97 passed, 1 skipped, 4 subtests passed`，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`871a2db8c3f4a4fa3d1bdc7b2df961179c389d43`（`Record collaborator confirmations and release stage gates`）。

## 2026-08-10 15:41:44

### 关键残基事实的机器可读约束与运行前核验规则

- 用户要求实验缺失坐标、界面残基和其他会改变设计的关键事实不得仅依赖对话上下文或代理记忆。既有`AGENTS.md`虽要求记录缺失坐标、界面定义、映射和数值来源，但没有明确要求集中机器可读绑定、每次设计前重读核验以及哈希不一致时拒绝运行，因此本次补充了强制preflight规则。
- 新增`docs/result_artifacts/input_baseline/reviews/nb252_critical_residue_sets.json`，绑定authoritative parent、实验VHH链、reported indices `9-15,24-29`的实验缺失坐标集合、24个严格`<4.0 Å`实验可评估界面位点、不可突变的`SSGS` positions `125-128`，并记录mapping、interface、design constraints和confirmed review的路径与SHA-256。缺失坐标在实验界面语义中明确为`not_evaluable`，不能被预测或补全模型静默改写为实验接触/非接触。
- 规则现要求所有候选生成、突变建模、亲和力/稳定性评分和排序入口重新打开关键制品，核对schema/status、哈希、模型/链、母本序列、编号、coordinate-evaluable和WT残基；缺失、过期、blocked或不一致时必须停止。所有下游manifest须绑定实际使用的关键制品哈希。
- 本次为规则和轻量机器可读事实汇总，预计和实测均远低于1小时，不需要Slurm；未运行结构补全、候选生成或评分。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`8b768aaec6de837f205523d4d55db4cdb2571189`（`Require critical residue preflight validation`）。

## 2026-08-10 20:24:50

### 阶段2阶段0：候选设计合同与本地preflight完成

- 用户要求把后续完整优化方案写入replace-in-place handoff，并在方案、工具、门或时间估计变化时同步更新；`AGENTS.md`的文档维护规则现显式要求持续维护`Current Optimization Plan`并删除被替代路线。handoff已记录从远程WT结构门、WT序列/可开发性基线、单点景观、PyRosetta筛选、表达/多目标排序到组合与AF3终选复核的完整路线及约1–2周计算时间估计。
- 新增`antibody_optimization.design_contract`与`design_contract_plot`，以及单一阶段0入口`scripts/candidate_design/build_stage2_design_contract.py`。入口重新打开`nb252_critical_residue_sets.json`，核验其绑定的sequence mapping、interface manifest、design constraints、confirmed baseline review的精确SHA-256，并进一步核验`stage1_gate.json`和实验`NK2R-252__native.cif`；缺失、过期、越出仓库、角色/WT/编号/坐标状态不一致均拒绝运行。
- 阶段0从真实实验chain C的SG几何识别到唯一一对1.8–2.3 Å范围内二硫键，映射为reported Cys22/Cys95；与不可替换/删除的reported positions 125–128 `SSGS`合并为6个硬冻结位点。实验缺失坐标仍为13位并保持`not_evaluable`；24个严格`<4.0 Å`实验界面位点为“谨慎但可突变”，也是首轮亲和力放行位点。首轮不要求批量补全缺失结构，缺失位点不进入首轮亲和力扫描。
- 正式Git-tracked输出位于`docs/result_artifacts/candidate_design/stage0_contract_20260810/`：`stage2_design_contract.json`、严格128行且UTF-8 BOM/LF的`mutable_position_inventory.csv`、`stage2_preflight.json`、精确绘图源对应的600 dpi PNG/SVG及`stage2_stage0_manifest.json`；独立run summary位于`docs/run_summaries/candidate_design/stage0_contract_20260810.json`。正式运行耗时1.209181秒，SVG已规范为LF、无行尾空白和单一终止换行，图已人工检查且轨道、轴、图例无遮挡。
- 阶段门结果为`stage0_local_contract=pass`、`candidate_manifest_release=pass`；`pyrosetta_affinity_scoring_release=blocked_pending_remote_gap_safe_import`，直到服务器验证缺失密度jump/cutpoint、PDBInfo可逆映射、有限能量及约束WT pose稳定性。阶段0没有生成突变、没有补全结构，也没有运行PyRosetta、AF3或语言模型。
- 24个界面位点的全氨基酸替换理论上限为`24 × 19 = 456`，因此将旧的500–800个首轮单点估计修正为最多456个原始替换、预计经提议/硬约束和风险过滤后保留约200–400个；实际数目以后续真实候选制品为准。
- 预计与实测均远低于1小时，不需要Slurm或checkpoint。专项测试`3 passed`，覆盖真实合同、陈旧哈希阻断、拒绝覆盖、CSV BOM/LF和固定时间戳双跑六个正式制品逐字节一致；完整验收为`100 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 主提交：`5851dc67f53cb08c5a1007e01168e218fd74bccc`（`Implement stage-2 local design contract`）。
- 推送前SVG规范化提交：`aeb9e2ef62b19750c6ad2509a4c0be39c13e4c71`（`Normalize stage-2 SVG artifact`）。

## 2026-08-10 21:35:07

### 信息记录与防御性检查规则精简

- 用户要求同一信息只在最合适的权威制品中记录一次，其他文件只引用并保留自身决策所需内容；不保留重复叙述或不影响解释、复现和阶段门的低价值元数据。
- `AGENTS.md`现要求按风险在阶段边界执行一次防御性验证，并在输入未变化的同一阶段复用结果；只有上游变化、新阶段风险不同或先前检查失败时才重验。哈希仅在冻结新输入或绑定新输出边界时计算，不在每个候选或子步骤重复计算。
- 下一阶段相应压缩为一次合并的PyRosetta WT导入与基线验证：复用阶段0已确认事实，只新增PyRosetta特有的缺口连接、映射、能量和约束构象稳定性检查。

## 2026-08-10 21:51:33

### PyRosetta WT安全导入门实现

- 按精简方案新增唯一远程入口`scripts/structure_preparation/validate_pyrosetta_wt_import.py`及纯逻辑模块`antibody_optimization.pyrosetta_import_gate`。入口复用阶段0和released structure baseline的一次性验证结果，直接以`-missing_density_to_jump true`导入实验mmCIF；不补全、relax、最小化、生成突变或解释亲和力。
- released结构清单给出实验Pose预期396个polymer残基（Nb252 chain C 115个、NK2R chain R 281个）及四个有意义断点：Nb252 auth 8/16、23/30，C/R链边界，以及NK2R auth 228/240之间缺失229–239。入口核验Pose/PDBInfo逐位身份、四个cutpoint/jump、跨断点C–N非成键、Cys22–Cys95二硫键和`ref2015`非零权重raw score有限性。
- 真实远程运行只输出断点表、raw score term表、一个权威gate JSON、一张SVG和一个run summary；`pass`只将亲和力流程推进到`ready_for_scoring_protocol_calibration`。本地没有PyRosetta，因此未生成或伪造远程gate结果。
- 阶段0合同已按当前决策重建，其下一门不再要求全局或重复relax，只要求gap-safe导入、PDBInfo、二硫键和有限raw score。预计远程实跑低于半小时，不需要Slurm或checkpoint。
- 新增5个本地测试，覆盖真实released断点合同、安全/错误FoldTree和成键状态、映射、无PyRosetta时CLI帮助及Slurm资源合同。完整验收为`105 passed, 1 skipped, 4 subtests passed`；`pip check`、`compileall`、Slurm LF检查和`git diff --check`通过。

## 2026-08-10 21:55:59

### PyRosetta WT导入改为Slurm唯一执行路线

- 用户要求该短任务也通过Slurm提交。新增`submit_pyrosetta_wt_import.slurm`并替换直接运行说明：固定`batch`、1 GPU、12 CPU、1节点/1任务、1小时上限，不显式申请内存；激活既有`/data/software/env/luly25/multi_ligand`后调用唯一Python入口。GPU仅因集群不能单独申请CPU作业而请求，不代表PyRosetta导入使用GPU加速。
- 科学逻辑、输出合同和阶段门不变，不增加checkpoint、重复preflight或备用执行路线。真实远程运行仍未发生。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`c4a7985f2911a1dd2312bebb41dfa37c7fa11532`（`Implement PyRosetta WT import gate`）。

## 2026-08-10 22:09:08

### PyRosetta WT安全导入门远程结果同步

- 已拉取远程结果提交`5efbc99`。权威gate为`pass`且无blocker：Pose共396个polymer残基（Nb252 chain C 115、NK2R chain R 281），两个Nb252缺失密度断点、C/R链边界和NK2R缺失229–239断点均同时具有FoldTree cutpoint与jump，且断点两侧C–N未成键；PDBInfo逐位映射、Cys22–Cys95二硫键和所有记录的raw score均通过。
- Slurm任务实测14.123538秒。`ref2015` raw total score为1292.7485711160023；主要正向加和项包括`fa_dun` 1436.9269014761067、`fa_sol` 1363.7636623957333和`fa_rep` 911.2761509327626，主要负向项包括`fa_atr` -2325.0185592374214和`fa_elec` -440.57010917714024。各非total加权项之和与记录total在浮点误差内一致。
- 这些能量是未经relax的原始导入诊断，证明结构可安全进入PyRosetta且可产生有限能量，不是亲和力或稳定性预测。较大的排斥与构象相关项说明候选评分前必须先校准一个对WT和所有候选一致的最小结构准备与界面评分协议；当前门只推进到`ready_for_scoring_protocol_calibration`，未释放候选评分。
- 同步后仅更新当前handoff与本周历史；未改写远程生成制品、未启动下一阶段计算。PyRosetta导入门针对性测试`5 passed`，`compileall`、`pip check`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`31f232750f941ed3eda08bf163d0d03f80490e35`（`Record PyRosetta WT import result`）。

## 2026-08-10 22:38:13

### PyRosetta WT评分协议校准实现

- 新增纯逻辑模块`antibody_optimization.pyrosetta_scoring_calibration`、远程入口`calibrate_pyrosetta_scoring.py`和唯一Slurm包装。流程复用已通过的阶段0及WT导入门，不重复上游哈希；以实验complex为同一母本，比较8个固定主链界面repack和8个界面repack加`N/CA/C/O`坐标约束局部最小化重复。
- 局部可移动集合由实验C/R链跨链重原子`<8 Å`定义；接触保持使用已发布24个VHH严格`<4 Å`位点及raw Pose的NK2R表位。每个重复重新核验PDBInfo映射、4个cutpoint/jump、跨缺口非成键和Cys22–Cys95二硫键，并记录配对`dG_separated`、跨界面能、界面`fa_atr/fa_rep`、接触/表位保持、界面Cα RMSD和最短距离。
- 选择合同优先固定主链repack；只有其未通过而坐标约束局部最小化通过时才采用后者。显式内部校准限值为VHH接触保持至少0.80、NK2R表位保持至少0.90、界面Cα RMSD至多0.50 Å、`dG_separated` MAD至多3.0 REU且界面`fa_rep`不得高于raw。通过只释放同一准备协议下相对WT的候选界面排序，不把完整complex total score解释为亲和力/稳定性，也不并行运行RosettaMP。
- 输出合同为重复指标CSV、raw与选中WT逐残基能量CSV、协议选择JSON、代表WT PDB、权威gate、SVG和run summary；默认拒绝覆盖。Slurm固定`batch`、1 GPU、12 CPU、4小时上限、无显式内存，逐重复打印进度；预计1–3小时，不需要checkpoint。GPU仅为集群资源约束。
- PyRosetta入口841行但保持为单一、工具环境专用工作流：Pose准备、测量和事务性输出只在该远程入口使用；可本地复用和独立测试的输入验证、汇总、阈值、选择与gate逻辑已提取到419行纯模块，因此未为满足行数机械拆分PyRosetta运行态代码。
- 本地没有PyRosetta，未生成或伪造校准结果。新增5个测试覆盖真实released输入、两条选择分支、双失败阻断、代表重复、SVG、CLI与Slurm合同；全套验收`110 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`1b1112648c2f26f31945f3166f530769f16307e4`（`Implement PyRosetta scoring calibration`）。

## 2026-08-11 09:17:37

### 评分校准首次远程运行API修复与source缺失侧链审计

- 首次Slurm运行在raw跨界面能量读取阶段停止，未进入16个校准重复且未生成正式结果。根因是PyRosetta 2026.03的`EnergyEdge.fill_energy_map()`只接受零参数并直接返回`EMapVector`，入口错误地传入了预建map；现改为零参数调用，并以只接受该签名的fake edge增加回归测试。
- 用户报告的`missing heavyatom`警告来自实验source本身的不完整侧链，不是上述异常的原因。released residue inventory按20种标准氨基酸重原子计数审计得到20个不完整残基、合计缺90个重原子：Nb252 chain C为auth 30 PHE、44 GLU、102 TYR，NK2R chain R为17个；其中C102属于已确认`<4 Å`界面。PyRosetta导入会补建这些原子，因此warning不单独阻断，但这一source事实现写入未来gate/selection JSON，界面邻域残基统一repack，禁止把补建构象称为实验rotamer。
- 不隐藏PyRosetta warning，也不修改原始mmCIF或补全缺失序列区。正式重跑仍使用原Slurm入口；本次异常发生在事务性安装前，若目标目录仅为空目录而没有正式输出文件，可直接重跑。
- 专项测试现为`6 passed`；全套验收`111 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。本地没有PyRosetta，真实API修复仍需服务器重跑确认。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`7765843868c77b2958b4587163f53e3b69eefea4`（`Fix PyRosetta scoring calibration API`）。

## 2026-08-11 09:50:28

### PyRosetta评分校准远程结果同步与科学release复核

- 已拉取远程结果提交`6d8bac1`。任务实测404.321078秒，两条协议各8个重复，全部映射、断点、二硫键、有限指标和原阈值检查通过；输出包含重复表、raw/选中WT逐残基能量、选择JSON、396残基代表PDB、机器gate、SVG和run summary。source审计复现20个不完整侧链/90个缺失重原子，其中8个残基在PyRosetta补原子后的`<8 Å`局部邻域，Nb252界面C102 TYR在内。
- `interface_repack`重复稳定且主链不动：中位`dG_separated=+58.425061 REU`（MAD 0.608699）、跨界面能`+12.675360 REU`、界面`fa_rep=112.064095 REU`，VHH接触保持0.958333、NK2R表位保持0.918919。相对raw界面`fa_rep=183.483686`仅下降38.924%，选中代表中C102 TYR仍有`fa_rep=41.655401 REU`，说明固定主链repack仍受实验缺侧链补建及局部几何碰撞主导。
- `interface_repack_constrained_min`中位`dG_separated=-58.147156 REU`（MAD 2.357017）、跨界面能`-82.876253 REU`、界面`fa_rep=22.466594 REU`，相对raw下降87.756%；界面Cα RMSD中位0.071540 Å、最大0.071804 Å，VHH接触保持最低0.875、NK2R表位保持最低0.918919。小幅受约束主链调整消除了大部分残余排斥，同时保持既定构象限值。
- 当前选择器只要求界面`fa_rep`不高于raw，因而两协议均形式通过并按“最简单优先”错误选择repack-only。对于实验已结合复合物，repack-only仍为正`dG_separated`和正跨界面能，与受约束最小化定性冲突；这些数值不能被解释为实验亲和力，但足以证明当前选择标准未完成校准。
- 不改写远程生成gate或代表PDB。其机器字段虽为`pass`，项目科学release暂记为`held_pending_protocol_selection_revision`，候选评分不得启动。下一步应增加“非碰撞主导且结合方向一致”的选择条件，以全新输出目录重跑，生成受约束局部最小化代表WT及更新gate；RosettaMP仍无须启用。
- 同步后专项测试`6 passed`，`compileall`、`pip check`和`git diff --check`均通过；未启动下一阶段计算。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`8d3312c2a4cae0dcac775391f46f8a7a366fce60`（`Review PyRosetta scoring calibration result`）。

## 2026-08-11 10:09:53

### 评分校准v2科学选择门与接触变化制品实现

- v1正式制品保持不变。v2在原映射、断点、二硫键、有限指标、重复MAD、接触/表位保持、界面Cα RMSD和相对raw界面`fa_rep`门之外，新增不可关闭的逐重复方向门：所有`dG_separated < 0`且所有跨界面能`< 0`。协议摘要同时记录两项的最大值和跨界面能中位数；schema提升为2。
- 新增真实v1重复表集成测试，确定`interface_repack`同时触发`one_or_more_dg_separated_not_negative`和`one_or_more_cross_interface_energy_not_negative`，`interface_repack_constrained_min`成为唯一通过协议。该符号门只用于排除仍由碰撞主导的准备协议，不能把Rosetta REU解释为实验亲和力。
- v2为选中代表WT新增`selected_contact_changes.csv`，按Nb252 chain C和NK2R chain R的source auth位置逐行记录`retained/lost/gained`、残基身份、raw/prepared接触布尔值及两状态最短跨链重原子距离；代表结构重建后的保持率必须与选中重复记录逐字义一致，否则停止安装结果。gate只保留分侧的精确状态计数，详细事实只记录在该CSV一次。
- Slurm唯一活动路线已替换为全新`docs/result_artifacts/structure_preparation/pyrosetta_scoring_calibration_v2_20260811/`及对应run summary，不覆盖v1。资源合同不变；按v1实测预计7–15分钟，无checkpoint或RosettaMP。
- v2入口现为1007行，已再次按职责检查：科学选择、输入审计、汇总、接触状态和gate均在605行纯模块中独立测试；入口剩余内容是同一PyRosetta环境中的Pose准备、测量、代表结构重建及事务性编排。当前没有第二个运行入口复用这些PyRosetta态操作，继续拆分只会创建无稳定复用方的工具层，因此保留单一入口；候选评分开始复用这些操作时再提取共享runtime。
- 本地没有PyRosetta，未生成或伪造v2结果。专项测试`8 passed`，包含真实v1选择回归与精确接触状态/距离测试；全套验收`113 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 实现提交：`e3de4e6229e680d8199e398ad2ecb9168fecc75c`（`Implement PyRosetta scoring calibration v2`）。

## 2026-08-11 10:33:43

### 评分校准v2远程结果同步与科学release

- 已同步远程结果提交`288bd45c7a38d4d07091d7bf863e3232d7091e17`。任务实测465.154733秒，两协议各8个重复；结果目录包含schema 2 gate、重复表、选择JSON、代表prepared WT、逐残基能量、67行精确接触变化和QC SVG，v1制品未被覆盖。
- v2正确阻断`interface_repack`：8/8重复的`dG_separated`及跨界面能均为正；中位分别为+58.425061 REU和+12.675360 REU。`interface_repack_constrained_min`为唯一通过协议，8/8重复两项均为负；中位`dG_separated=-58.147156 REU`、MAD 2.357017，中位跨界面能-82.876253 REU。
- 选中协议的界面`fa_rep`中位22.466594 REU，相对raw 183.483686下降87.755536%；最大界面Cα RMSD 0.071804 Å，最低VHH接触保持0.875、最低NK2R表位保持0.918919，映射、断点、二硫键和有限指标全部通过。Nb252 C102 TYR的`fa_rep`从raw 61.562396降至代表结构4.922464 REU，原缺侧链碰撞不再主导局部界面。
- 代表结构seed为8112027，VHH原24接触中保留21、丢失3并新增2；NK2R原37表位残基中保留34、丢失3并新增4。VHH source-auth E46、D101、I103成为`lost`，S50、T53成为`gained`；I103 prepared距离4.000093 Å属于严格4 Å边界切换，D101从2.975572 Å变为4.568493 Å更具实际结构意义。低Cα RMSD说明结合主链构象得到保持，但局部侧链接触并非逐项冻结。
- 接受v2 gate并将科学状态更新为`released_for_paired_relative_candidate_scoring`。后续候选必须相对同一prepared WT、使用同一受约束局部协议和结构安全门；Rosetta值只用于相对排序。实验24位界面仍是候选定义和谨慎保护依据，prepared接触集合只作评分/QC，不得覆盖实验界面；E46/D101/I103变体不得只依据单一prepared构象淘汰。
- 一次性一致性验证确认gate pass、16行重复、67行接触状态、396个polymer残基及关键符号条件相互一致；评分校准专项测试`8 passed`，`git diff --check`通过。未启动候选生成或评分。

### Git sync status

- 远程结果提交`288bd45c7a38d4d07091d7bf863e3232d7091e17`已在`origin/main`并同步至本地。
- 科学复核已提交并推送至`origin/main`。
- 复核提交：`3b06297ceacbb6a912ee1a456ea6c85bd31762f5`（`Review PyRosetta scoring calibration v2 result`）。

## 2026-08-11 10:58:44

### 多目标优化路线确认为并行三轨道发现

- 用户确认继续采用“亲和力、稳定性/表达、风险修复并行发现，汇合后少量组合，实验验证后再串行迭代”的方案。三条轨道均以同一authoritative 128-aa WT为母本，不能将某条纯计算轨道的优选序列提前作为另一条轨道母本；资源调度可以分批，但不改变科学上的并行可比设计。
- 亲和力轨道完整枚举实验24界面位点的456个单点；稳定性/表达轨道从非界面、非冻结位点中按结构或序列证据提出候选；风险修复轨道只针对实际识别的化学、聚集或异常表面风险提出保守候选。后两轨不为凑数进行全位点穷举。
- 三轨分别筛选后以硬约束和Pareto方法汇合，第一轮只组合少量机制互补的单点，所有组合重新评估全部目标。实验面板保留WT、各轨代表单点和少量双突变；只有实验结果产生后才进入以验证优质序列为母本的串行第二轮。
- 下一实施阶段限定为本地WT与候选空间基线：生成透明理化/风险和保留观测语义的yield基线、三轨位点资格及独立manifest、候选空间gate和可复现图；不安装服务器模型、不运行PyRosetta/AF3或组合突变。预计0.5–1天，不超过1小时的单次本地入口，无需Slurm、checkpoint或重复输入检查。

### Git sync status

- 本次路线与交接更新尚未提交或推送。

## 2026-08-11 11:15:26

### 当前执行顺序收敛为先完成亲和力轨道

- 用户决定先完成证据较确定的亲和力轨道，表达/稳定性候选及模型方案随后另行讨论。总体仍保留“两条主轨从同一WT独立发现、风险横向监测、实验后串行迭代”的架构，但当前不实施47条yield小模型、nanoBERT PLL相关性、AntiFold/AbMPNN或风险修复候选。
- 下一阶段先在本地从实验24界面位点生成全部456个非WT单点，不按prepared接触变化或主观化学偏好预删；随后实现严格复用v2受约束局部协议的成对PyRosetta评分。先以约12个分层代表候选做3重复Slurm pilot，通过后再分片运行456候选3重复初筛，入围者补足8重复并进行更严格亲和力复核。
- 当前阶段不组合突变，不把Rosetta REU解释为实验亲和力；实验24位界面和NK2R原表位继续作为结构保持基准，E46/D101/I103不得只依据单一prepared构象淘汰。单次本地manifest入口预计小于1小时；pilot小于1小时但因PyRosetta远程环境仍走Slurm，完整初筛采用每任务低于5小时的分片路线。

### Git sync status

- 本次顺序调整与交接更新尚未提交或推送。

## 2026-08-11 12:00:00

### 亲和力单突变空间与成对PyRosetta pilot实现

- 本地正式生成`affinity_single_mutants_20260811/`：实验严格`<4 Å`的24个Nb252界面位点各枚举19种非WT替换，共456条单突变。每条保留authoritative 128-aa序列、reported/IMGT/source-auth三套可逆标识、实验界面语义及prepared-WT接触敏感标志；E46/D101/I103未被删除。12个分层pilot覆盖FR/CDR、保守/跨类别替换和三个prepared敏感位点。候选表、FASTA、位置摘要、pilot ID、gate、manifest、600 dpi PNG/SVG及run summary均已生成，实测约2秒；PNG已人工检查无遮挡。
- 新增共享`pyrosetta_runtime`，校准入口和候选入口均调用同一`ref2015`、局部repack、主链坐标约束最小化、断点/二硫键/映射检查、严格接触及配对分离能实现；校准入口由1007行缩减到503行，旧重复runtime实现已删除。候选评分以v2 selected prepared WT为共同起点；每个replicate/seed只计算一次共享WT，随后全部突变体以同一seed分别计算并引用该WT。12候选×3重复因此是3次WT和36次突变体，不是每个候选重复计算WT。WT控制独立保存3行，候选表只保存`wt_control_id`、突变体指标和`mutant-WT`差值，避免重复记录。候选能量、接触保持或RMSD不合格只淘汰候选；仅映射/断点/二硫键/有限数值等运行安全失败或WT对照失效阻断pilot。
- 新增远程评分入口和唯一pilot Slurm包装：12候选×3重复，`batch`、1 GPU、12 CPU、2小时上限、不显式申请内存，预计实际5–15分钟且不需要checkpoint。当前只实现pilot，不提前实现456候选分片或后续8重复；真实PyRosetta运行尚未发生。
- 本地全套验收为`126 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`、`git diff --check`和Slurm LF检查通过。表达/稳定性、nanoBERT、AntiFold/AbMPNN、风险修复和组合突变均未启动。

### Git sync status

- 已提交并推送至`origin/main`。
- 实现提交：`052d08e4318a1d0681649b5b872344a637634a6b`（`Implement affinity single-mutant pilot`）。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
