# 2026-W33 项目历史

## 2026-08-10 09:53:14

### ChimeraX `runscript` 入口真实集成修复

- 用户已在 ChimeraX 1.12 中打开项目目录内的原始 `Nb252-optimization.cxs`，并以截图确认 `NK2R-252.pdb` 的合作者橙色界面注释可见。ChimeraX 命令历史进一步确认当前打开路径为仓库内 CXS，而不是桌面根目录的同哈希副本。
- 连续运行既定 `runscript` 命令时，ChimeraX 只回显并折叠重复命令，`data/structures/cxs_exports/` 及其父目录均未生成；因此没有把该尝试记录为成功导出，结构、映射、界面和候选设计阶段门继续保持 `blocked`。
- 根因是 `export_cxs_session_chimerax.py` 原先只在 `__name__ == "__main__"` 时调用 `main()`，而 ChimeraX 1.12 的 `runscript` 将 Python 脚本置于具有唯一模块名、`__spec__ is None` 且注入 `session` 的 sandbox 中执行。旧入口因而只定义函数后静默结束，没有进入任何输入、模型或写出检查。
- 导出器现通过 sandbox 特征分派入口：ChimeraX `runscript` 环境直接调用 `main(session, sys.argv[1:])`，普通 `importlib` 导入保持无副作用，普通 Python 直接执行仍明确提示必须使用 ChimeraX。没有使用 `SystemExit(0)` 结束 ChimeraX sandbox，也没有放宽源 CXS 路径、哈希、模型身份或输出目录安全检查。
- 外部 CLI 合同、三个必需参数和输出目录合同均未改变。预计和本次修复/测试实测均远低于 1 小时，不需要 Slurm、checkpoint 或服务器环境。
- 新增真实执行语义回归测试：模拟非 `__main__` 的 ChimeraX sandbox 并断言注入的 `session` 传入 `main()`；同时验证普通模块导入不调用 `main()`。完整本地验收为 `93 passed, 1 skipped, 4 subtests passed`，唯一跳过仍是 Windows 真实 symlink 权限测试；`pip check`、Python `compileall` 和 `git diff --check` 均通过。
- 修复后尚待用户在当前正确打开的 ChimeraX 1.12 会话中重新运行原命令。只有生成并验证 `cxs_export_manifest.json`、5 个 mmCIF 和颜色清单后，才能首跑 Gemmi 结构基线并进入链角色/橙色人工审核。

### Git sync status

- 本次入口修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:01:46

### CXS真实模型名合同修正

- 修复 sandbox 入口后，ChimeraX 已真实进入导出器的模型身份检查，并报告三个原子模型实际名称为 `NK2R-252.pdb`、`NK2R-NKA` 和 `fold_2r_252_nomg_model_0.cif`。这证明先前记录的 `NK2R-NKA.pdb` 与原会话实际名称不一致；本次失败发生在任何 staging/output 写出之前，`data/structures/cxs_exports/` 仍不存在。
- 按真实会话证据将导出器和 Gemmi 结构基线的唯一模型名合同同步修正为 `NK2R-NKA`，不要求用户重命名会话模型，也不增加别名、模糊匹配或自动 fallback。对应更新项目级科学上下文、handoff及测试，使导出和下游结构入口继续使用同一精确三模型集合。
- 外部命令、源CXS哈希和输出路径均不变。完整本地验收仍为 `93 passed, 1 skipped, 4 subtests passed`；`pip check`、Python `compileall` 和 `git diff --check` 均通过。当前仍待用户在正确打开的原会话中再次运行相同命令，真实导出成功前所有结构相关阶段门继续为 `blocked`。

### Git sync status

- 本次入口与模型名合同修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:06:30

### CXS真实导出与Gemmi首轮结构清单

- 用户在正确打开仓库原始CXS的ChimeraX 1.12会话中重跑修复后导出器。`data/structures/cxs_exports/cxs_export_manifest.json` 为 `status=pass`：源CXS SHA-256仍为 `1bc636c28f66ae60edc658d2e1c4aad0b07f4141ca5411c78662aa19da793c4d`且运行前后身份未变；会话含3个目标`AtomicStructure`和2个名为`missing structure`的非原子pseudobond child。
- 导出生成3个native-frame和2个实验参考坐标系mmCIF、`cxs_residue_colors.csv`、manifest及run summary，共8个文件；5个mmCIF均由Gemmi 0.7.5成功读回。ChimeraX提示`Not saving entity_poly_seq for non-authoritative sequences`是`bestGuess false`下不补写非权威序列的保守行为，不是导出失败。
- 三个原子模型计数分别为：实验`NK2R-252.pdb` 2链/396残基/3090 atom sites，`NK2R-NKA` 2链/307残基/2424 atom sites，AF3 VHH 1链/126残基/988 atom sites；全部occupancy为1且无非空altloc。颜色清单共829行，其中唯一精确内置橙色类为`[255,165,0,255]`，在实验模型chain C的24个残基上同时出现在ribbon和atom通道；无自定义颜色或无法回映的surface颜色。
- 在全新目录`docs/result_artifacts/input_baseline/structure_review_20260810/`完成Gemmi结构基线首跑。export和inventory为`pass`；跨工具模型/链/残基/atom object/site计数一致，两个reference-frame文件的全原子刚体残差均低于0.001 Å。按既定合同，首跑因5个链角色、橙色绑定和权威构建体仍待review而返回code 2/`status=blocked`，并生成inventory、residue inventory和`baseline_review_template.json`，未自动填写科学身份。
- 当前需要用户集中确认链角色和唯一橙色类；权威构建体边界可继续保持pending。真实导出与首轮清单是科学阶段进展，但尚未生成Nb252结构映射、FR-only对齐、严格`<4.0 Å`界面或临时保护集合。

### Git sync status

- 真实导出、首轮结构清单、入口/模型名修复、测试和文档尚未提交或推送；等待review确认及后续结构基线重跑。

## 2026-08-10 10:27:21

### 链角色、橙色注释、结构映射与临时界面基线完成

- 用户在 ChimeraX 1.12 中逐项确认：实验模型 `#1/C` 为 Nb252、`#1/R` 为 NK2R；NKA 模型 `#2/L` 为 NKA、`#2/R` 为 NK2R；预测模型 `#3/A` 为 AF3 Nb252；24 个绿色选中残基与合作者橙色区域一致。确认已写入 `structure_review_20260810/baseline_review.json`，绑定精确 RGBA `[255,165,0,255]` 与 `atom+ribbon` 渠道；权威构建体 review 仍保持 `pending_collaborator_confirmation`。
- 首次 confirmed structure run 忠实阻断于实验结构缺失片段造成的多最佳序列比对。检查发现实验 VHH 的 115 个有坐标残基全部具有 1–128 范围内唯一、严格递增的整数 source auth 编号，并逐位匹配 reported Nb252。`polymer_mapping` 1.1.0 因此新增严格直接路线：仅在无插入码、编号唯一递增、范围有效且每一 WT 均一致时使用 source auth 编号；否则仍进入原 exact-first/BLOSUM62 安全门。
- 第二次 run 暴露 AF3 导出保留 536-aa source polymer 元数据，而当前坐标仅含其起始 126-aa VHH；536-aa 序列的前 128 aa 与 reported Nb252 完全一致。映射器现仅在较长 polymer 中存在唯一、完整、逐字一致的 128-aa 连续片段，且全部有坐标残基落在该片段内时接受该片段；额外序列不被称为 VHH，内部冲突、多匹配或坐标越界仍阻断。失败 run 及其 run summary 予以保留作为负结果。
- 最终结构制品位于 `structure_reviewed_20260810/`：实验 VHH 以 source auth exact-WT 映射，AF3 VHH 以 source label ID 加唯一 authoritative segment 映射；residue mapping 为 `pass`。82 个共同 framework Cα、无 outlier rejection 的 Kabsch 拟合 RMSD 为 0.631994 Å；拟合后 CDR3 Cα RMSD 为 6.490853 Å，明显高于 FR aggregate 0.631994 Å。这些是结构计算结果，不是实验功能效应。
- 严格非 H/D、正 occupancy、polymer atom-center `<4.0 Å` 界面得到 246 个原子接触对和 24 个 VHH 残基；这 24 个残基与确认橙色集合完全相同，保护并集仍为 24 个。该集合仅是保守突变保护标志，不是能量热点集合。
- finalizer 已使用真实 mapping/interface 制品重建 128 位点绘图数据、PNG/SVG 和阶段门。期间修复 summary 对缺失坐标位点合法 `not_evaluable` 状态的读取，不再误当非法布尔值；人工查看 PNG 后又将旧的“结构轨道仍为灰色”说明替换为当前适用的灰色单元含义。`local_baseline_build=pass`；`candidate_design_release` 仅因 authoritative Nb252 construct 阻断；`pooled_expression_model_release` 仍因跨 assay pooling 证据阻断。
- 本轮计算实测远低于 1 小时，不需要 Slurm。最终验证：`96 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 已提交并推送至 `origin/main`。
- 结果提交：`1e6bd655ef316a9f5e10ea7331f3eceff302bc45`（`Complete reviewed stage-1 structure baseline`）。

## 2026-08-10 12:08:42

### 合作者确认解除构建体与跨来源可比性阻断

- 项目用户转达合作者明确确认：LTT、WCC、LLJ 不同来源的 reported yield 可直接相互比较；Nb252 末端 `SSGS` 不是 linker，必须保留原始 128-aa 序列。该确认分别写入 machine-readable expression confirmation 和既有统一 `baseline_review.json`，证据来源标为 `collaborator_via_project_user`。
- Nb252 authoritative construct 现固定为 reported sequence 1–128、`construct_scope=mature_vhh`、terminal `GS=included_in_authoritative_construct`。ANARCII 仍只编号前 126 aa；未编号不再解释为构建体外序列。新目录 `structure_released_20260810/` 的 export、inventory、chain role、mapping 和 authoritative sequence 均为 `pass`；重新绑定 review 哈希的 `interface_released_20260810/` 仍复现原 246 个原子接触对和 24 个 VHH 界面残基。
- 表达审核升级为 1.1.0并新增显式 `--comparability-confirmation` 输入。47 条样本的跨来源 pooling 状态现为 `pass`，允许保留原始观测语义的联合探索：31 条 LTT/WCC individual approximate 仍按数值处理，9 条 LLJ group lower bound 与 7 条 group approximate 仍为删失/分组信息，不得插值成个体点估计。协议字段未知不再阻断跨来源比较，但 `nb252_transfer_status` 对 47 条仍为 `blocked`，因为可比性不等于对未见突变的预测准确性验证。
- 用户同时明确优化必须维持实验观察到的表位和结合构象；合作者没有额外人工突变禁区，也没有既往突变效应数据。24 个严格 `<4.0 Å` 界面位点继续作为“突变时谨慎”的可复现注释，而不是绝对禁止或能量热点。
- finalizer 已用 released structure/interface 与更新的 expression audit 重建 canonical summary。当前 `local_baseline_build=pass`、`candidate_design_release=pass`、`pooled_expression_model_release=pass`；独立的 `nb252_expression_transfer` 子门仍为 `blocked`，需后续模型验证而非更多协议询问。
- 本次确认同步、轻量重跑和测试实测远低于 1 小时，不需要 Slurm。最终验证为 `97 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 本次合作者确认、审核逻辑、released制品、阶段门、测试和文档尚未提交或推送。

## 2026-08-10 15:03:19

### 末端 `SSGS` 设计约束澄清

- 用户明确“保留末端 `SSGS`”的操作含义是候选设计时不得突变或删除该四肽，而不只是把它纳入权威参考序列。该约束现固定为 reported-sequence positions 125–128 的硬冻结集合；由于末端 `GS` 不在 provisional ANARCII/IMGT 编号跨度内，后续必须用 reported-sequence index 表达，不能为其虚构 IMGT 位点。
- 新增机器可读设计约束 `docs/result_artifacts/input_baseline/reviews/nb252_design_constraints.json`，记录母本哈希、不可变位点和证据来源；同步修正 `AGENTS.md` 与 handoff。该澄清不改变既有结构映射、界面结果或三个已通过阶段门，也不释放仍需模型验证的 `nb252_expression_transfer`。
- 复核现有证据后，没有必须继续向合作者确认、否则会阻断第二阶段计算基线的重要信息。最终实验面板容量和是否进入组合突变属于看到单点候选景观后的项目决策，可后置，不需要现在补问无关实验细节。
- 本次仅为设计合同与文档更新，预计和实测均远低于 1 小时，不需要 Slurm。复核确认权威母本长度为128 aa且reported positions 125–128精确为`SSGS`；全套验证为`97 passed, 1 skipped, 4 subtests passed`，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`871a2db8c3f4a4fa3d1bdc7b2df961179c389d43`（`Record collaborator confirmations and release stage gates`）。

## 2026-08-10 15:41:44

### 关键残基事实的机器可读约束与运行前核验规则

- 用户要求实验缺失坐标、界面残基和其他会改变设计的关键事实不得仅依赖对话上下文或代理记忆。既有`AGENTS.md`虽要求记录缺失坐标、界面定义、映射和数值来源，但没有明确要求集中机器可读绑定、每次设计前重读核验以及哈希不一致时拒绝运行，因此本次补充了强制preflight规则。
- 新增`docs/result_artifacts/input_baseline/reviews/nb252_critical_residue_sets.json`，绑定authoritative parent、实验VHH链、reported indices `9-15,24-29`的实验缺失坐标集合、24个严格`<4.0 Å`实验可评估界面位点、不可突变的`SSGS` positions `125-128`，并记录mapping、interface、design constraints和confirmed review的路径与SHA-256。缺失坐标在实验界面语义中明确为`not_evaluable`，不能被预测或补全模型静默改写为实验接触/非接触。
- 规则现要求所有候选生成、突变建模、亲和力/稳定性评分和排序入口重新打开关键制品，核对schema/status、哈希、模型/链、母本序列、编号、coordinate-evaluable和WT残基；缺失、过期、blocked或不一致时必须停止。所有下游manifest须绑定实际使用的关键制品哈希。
- 本次为规则和轻量机器可读事实汇总，预计和实测均远低于1小时，不需要Slurm；未运行结构补全、候选生成或评分。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`8b768aaec6de837f205523d4d55db4cdb2571189`（`Require critical residue preflight validation`）。

## 2026-08-10 20:24:50

### 阶段2阶段0：候选设计合同与本地preflight完成

- 用户要求把后续完整优化方案写入replace-in-place handoff，并在方案、工具、门或时间估计变化时同步更新；`AGENTS.md`的文档维护规则现显式要求持续维护`Current Optimization Plan`并删除被替代路线。handoff已记录从远程WT结构门、WT序列/可开发性基线、单点景观、PyRosetta筛选、表达/多目标排序到组合与AF3终选复核的完整路线及约1–2周计算时间估计。
- 新增`antibody_optimization.design_contract`与`design_contract_plot`，以及单一阶段0入口`scripts/candidate_design/build_stage2_design_contract.py`。入口重新打开`nb252_critical_residue_sets.json`，核验其绑定的sequence mapping、interface manifest、design constraints、confirmed baseline review的精确SHA-256，并进一步核验`stage1_gate.json`和实验`NK2R-252__native.cif`；缺失、过期、越出仓库、角色/WT/编号/坐标状态不一致均拒绝运行。
- 阶段0从真实实验chain C的SG几何识别到唯一一对1.8–2.3 Å范围内二硫键，映射为reported Cys22/Cys95；与不可替换/删除的reported positions 125–128 `SSGS`合并为6个硬冻结位点。实验缺失坐标仍为13位并保持`not_evaluable`；24个严格`<4.0 Å`实验界面位点为“谨慎但可突变”，也是首轮亲和力放行位点。首轮不要求批量补全缺失结构，缺失位点不进入首轮亲和力扫描。
- 正式Git-tracked输出位于`docs/result_artifacts/candidate_design/stage0_contract_20260810/`：`stage2_design_contract.json`、严格128行且UTF-8 BOM/LF的`mutable_position_inventory.csv`、`stage2_preflight.json`、精确绘图源对应的600 dpi PNG/SVG及`stage2_stage0_manifest.json`；独立run summary位于`docs/run_summaries/candidate_design/stage0_contract_20260810.json`。正式运行耗时1.209181秒，SVG已规范为LF、无行尾空白和单一终止换行，图已人工检查且轨道、轴、图例无遮挡。
- 阶段门结果为`stage0_local_contract=pass`、`candidate_manifest_release=pass`；`pyrosetta_affinity_scoring_release=blocked_pending_remote_gap_safe_import`，直到服务器验证缺失密度jump/cutpoint、PDBInfo可逆映射、有限能量及约束WT pose稳定性。阶段0没有生成突变、没有补全结构，也没有运行PyRosetta、AF3或语言模型。
- 24个界面位点的全氨基酸替换理论上限为`24 × 19 = 456`，因此将旧的500–800个首轮单点估计修正为最多456个原始替换、预计经提议/硬约束和风险过滤后保留约200–400个；实际数目以后续真实候选制品为准。
- 预计与实测均远低于1小时，不需要Slurm或checkpoint。专项测试`3 passed`，覆盖真实合同、陈旧哈希阻断、拒绝覆盖、CSV BOM/LF和固定时间戳双跑六个正式制品逐字节一致；完整验收为`100 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 主提交：`5851dc67f53cb08c5a1007e01168e218fd74bccc`（`Implement stage-2 local design contract`）。
- 推送前SVG规范化提交：`aeb9e2ef62b19750c6ad2509a4c0be39c13e4c71`（`Normalize stage-2 SVG artifact`）。

## 2026-08-10 21:35:07

### 信息记录与防御性检查规则精简

- 用户要求同一信息只在最合适的权威制品中记录一次，其他文件只引用并保留自身决策所需内容；不保留重复叙述或不影响解释、复现和阶段门的低价值元数据。
- `AGENTS.md`现要求按风险在阶段边界执行一次防御性验证，并在输入未变化的同一阶段复用结果；只有上游变化、新阶段风险不同或先前检查失败时才重验。哈希仅在冻结新输入或绑定新输出边界时计算，不在每个候选或子步骤重复计算。
- 下一阶段相应压缩为一次合并的PyRosetta WT导入与基线验证：复用阶段0已确认事实，只新增PyRosetta特有的缺口连接、映射、能量和约束构象稳定性检查。

## 2026-08-10 21:51:33

### PyRosetta WT安全导入门实现

- 按精简方案新增唯一远程入口`scripts/structure_preparation/validate_pyrosetta_wt_import.py`及纯逻辑模块`antibody_optimization.pyrosetta_import_gate`。入口复用阶段0和released structure baseline的一次性验证结果，直接以`-missing_density_to_jump true`导入实验mmCIF；不补全、relax、最小化、生成突变或解释亲和力。
- released结构清单给出实验Pose预期396个polymer残基（Nb252 chain C 115个、NK2R chain R 281个）及四个有意义断点：Nb252 auth 8/16、23/30，C/R链边界，以及NK2R auth 228/240之间缺失229–239。入口核验Pose/PDBInfo逐位身份、四个cutpoint/jump、跨断点C–N非成键、Cys22–Cys95二硫键和`ref2015`非零权重raw score有限性。
- 真实远程运行只输出断点表、raw score term表、一个权威gate JSON、一张SVG和一个run summary；`pass`只将亲和力流程推进到`ready_for_scoring_protocol_calibration`。本地没有PyRosetta，因此未生成或伪造远程gate结果。
- 阶段0合同已按当前决策重建，其下一门不再要求全局或重复relax，只要求gap-safe导入、PDBInfo、二硫键和有限raw score。预计远程实跑低于半小时，不需要Slurm或checkpoint。
- 新增5个本地测试，覆盖真实released断点合同、安全/错误FoldTree和成键状态、映射、无PyRosetta时CLI帮助及Slurm资源合同。完整验收为`105 passed, 1 skipped, 4 subtests passed`；`pip check`、`compileall`、Slurm LF检查和`git diff --check`通过。

## 2026-08-10 21:55:59

### PyRosetta WT导入改为Slurm唯一执行路线

- 用户要求该短任务也通过Slurm提交。新增`submit_pyrosetta_wt_import.slurm`并替换直接运行说明：固定`batch`、1 GPU、12 CPU、1节点/1任务、1小时上限，不显式申请内存；激活既有`/data/software/env/luly25/multi_ligand`后调用唯一Python入口。GPU仅因集群不能单独申请CPU作业而请求，不代表PyRosetta导入使用GPU加速。
- 科学逻辑、输出合同和阶段门不变，不增加checkpoint、重复preflight或备用执行路线。真实远程运行仍未发生。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`c4a7985f2911a1dd2312bebb41dfa37c7fa11532`（`Implement PyRosetta WT import gate`）。

## 2026-08-10 22:09:08

### PyRosetta WT安全导入门远程结果同步

- 已拉取远程结果提交`5efbc99`。权威gate为`pass`且无blocker：Pose共396个polymer残基（Nb252 chain C 115、NK2R chain R 281），两个Nb252缺失密度断点、C/R链边界和NK2R缺失229–239断点均同时具有FoldTree cutpoint与jump，且断点两侧C–N未成键；PDBInfo逐位映射、Cys22–Cys95二硫键和所有记录的raw score均通过。
- Slurm任务实测14.123538秒。`ref2015` raw total score为1292.7485711160023；主要正向加和项包括`fa_dun` 1436.9269014761067、`fa_sol` 1363.7636623957333和`fa_rep` 911.2761509327626，主要负向项包括`fa_atr` -2325.0185592374214和`fa_elec` -440.57010917714024。各非total加权项之和与记录total在浮点误差内一致。
- 这些能量是未经relax的原始导入诊断，证明结构可安全进入PyRosetta且可产生有限能量，不是亲和力或稳定性预测。较大的排斥与构象相关项说明候选评分前必须先校准一个对WT和所有候选一致的最小结构准备与界面评分协议；当前门只推进到`ready_for_scoring_protocol_calibration`，未释放候选评分。
- 同步后仅更新当前handoff与本周历史；未改写远程生成制品、未启动下一阶段计算。PyRosetta导入门针对性测试`5 passed`，`compileall`、`pip check`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`31f232750f941ed3eda08bf163d0d03f80490e35`（`Record PyRosetta WT import result`）。

## 2026-08-10 22:38:13

### PyRosetta WT评分协议校准实现

- 新增纯逻辑模块`antibody_optimization.pyrosetta_scoring_calibration`、远程入口`calibrate_pyrosetta_scoring.py`和唯一Slurm包装。流程复用已通过的阶段0及WT导入门，不重复上游哈希；以实验complex为同一母本，比较8个固定主链界面repack和8个界面repack加`N/CA/C/O`坐标约束局部最小化重复。
- 局部可移动集合由实验C/R链跨链重原子`<8 Å`定义；接触保持使用已发布24个VHH严格`<4 Å`位点及raw Pose的NK2R表位。每个重复重新核验PDBInfo映射、4个cutpoint/jump、跨缺口非成键和Cys22–Cys95二硫键，并记录配对`dG_separated`、跨界面能、界面`fa_atr/fa_rep`、接触/表位保持、界面Cα RMSD和最短距离。
- 选择合同优先固定主链repack；只有其未通过而坐标约束局部最小化通过时才采用后者。显式内部校准限值为VHH接触保持至少0.80、NK2R表位保持至少0.90、界面Cα RMSD至多0.50 Å、`dG_separated` MAD至多3.0 REU且界面`fa_rep`不得高于raw。通过只释放同一准备协议下相对WT的候选界面排序，不把完整complex total score解释为亲和力/稳定性，也不并行运行RosettaMP。
- 输出合同为重复指标CSV、raw与选中WT逐残基能量CSV、协议选择JSON、代表WT PDB、权威gate、SVG和run summary；默认拒绝覆盖。Slurm固定`batch`、1 GPU、12 CPU、4小时上限、无显式内存，逐重复打印进度；预计1–3小时，不需要checkpoint。GPU仅为集群资源约束。
- PyRosetta入口841行但保持为单一、工具环境专用工作流：Pose准备、测量和事务性输出只在该远程入口使用；可本地复用和独立测试的输入验证、汇总、阈值、选择与gate逻辑已提取到419行纯模块，因此未为满足行数机械拆分PyRosetta运行态代码。
- 本地没有PyRosetta，未生成或伪造校准结果。新增5个测试覆盖真实released输入、两条选择分支、双失败阻断、代表重复、SVG、CLI与Slurm合同；全套验收`110 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`1b1112648c2f26f31945f3166f530769f16307e4`（`Implement PyRosetta scoring calibration`）。

## 2026-08-11 09:17:37

### 评分校准首次远程运行API修复与source缺失侧链审计

- 首次Slurm运行在raw跨界面能量读取阶段停止，未进入16个校准重复且未生成正式结果。根因是PyRosetta 2026.03的`EnergyEdge.fill_energy_map()`只接受零参数并直接返回`EMapVector`，入口错误地传入了预建map；现改为零参数调用，并以只接受该签名的fake edge增加回归测试。
- 用户报告的`missing heavyatom`警告来自实验source本身的不完整侧链，不是上述异常的原因。released residue inventory按20种标准氨基酸重原子计数审计得到20个不完整残基、合计缺90个重原子：Nb252 chain C为auth 30 PHE、44 GLU、102 TYR，NK2R chain R为17个；其中C102属于已确认`<4 Å`界面。PyRosetta导入会补建这些原子，因此warning不单独阻断，但这一source事实现写入未来gate/selection JSON，界面邻域残基统一repack，禁止把补建构象称为实验rotamer。
- 不隐藏PyRosetta warning，也不修改原始mmCIF或补全缺失序列区。正式重跑仍使用原Slurm入口；本次异常发生在事务性安装前，若目标目录仅为空目录而没有正式输出文件，可直接重跑。
- 专项测试现为`6 passed`；全套验收`111 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。本地没有PyRosetta，真实API修复仍需服务器重跑确认。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`7765843868c77b2958b4587163f53e3b69eefea4`（`Fix PyRosetta scoring calibration API`）。

## 2026-08-11 09:50:28

### PyRosetta评分校准远程结果同步与科学release复核

- 已拉取远程结果提交`6d8bac1`。任务实测404.321078秒，两条协议各8个重复，全部映射、断点、二硫键、有限指标和原阈值检查通过；输出包含重复表、raw/选中WT逐残基能量、选择JSON、396残基代表PDB、机器gate、SVG和run summary。source审计复现20个不完整侧链/90个缺失重原子，其中8个残基在PyRosetta补原子后的`<8 Å`局部邻域，Nb252界面C102 TYR在内。
- `interface_repack`重复稳定且主链不动：中位`dG_separated=+58.425061 REU`（MAD 0.608699）、跨界面能`+12.675360 REU`、界面`fa_rep=112.064095 REU`，VHH接触保持0.958333、NK2R表位保持0.918919。相对raw界面`fa_rep=183.483686`仅下降38.924%，选中代表中C102 TYR仍有`fa_rep=41.655401 REU`，说明固定主链repack仍受实验缺侧链补建及局部几何碰撞主导。
- `interface_repack_constrained_min`中位`dG_separated=-58.147156 REU`（MAD 2.357017）、跨界面能`-82.876253 REU`、界面`fa_rep=22.466594 REU`，相对raw下降87.756%；界面Cα RMSD中位0.071540 Å、最大0.071804 Å，VHH接触保持最低0.875、NK2R表位保持最低0.918919。小幅受约束主链调整消除了大部分残余排斥，同时保持既定构象限值。
- 当前选择器只要求界面`fa_rep`不高于raw，因而两协议均形式通过并按“最简单优先”错误选择repack-only。对于实验已结合复合物，repack-only仍为正`dG_separated`和正跨界面能，与受约束最小化定性冲突；这些数值不能被解释为实验亲和力，但足以证明当前选择标准未完成校准。
- 不改写远程生成gate或代表PDB。其机器字段虽为`pass`，项目科学release暂记为`held_pending_protocol_selection_revision`，候选评分不得启动。下一步应增加“非碰撞主导且结合方向一致”的选择条件，以全新输出目录重跑，生成受约束局部最小化代表WT及更新gate；RosettaMP仍无须启用。
- 同步后专项测试`6 passed`，`compileall`、`pip check`和`git diff --check`均通过；未启动下一阶段计算。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`8d3312c2a4cae0dcac775391f46f8a7a366fce60`（`Review PyRosetta scoring calibration result`）。

## 2026-08-11 10:09:53

### 评分校准v2科学选择门与接触变化制品实现

- v1正式制品保持不变。v2在原映射、断点、二硫键、有限指标、重复MAD、接触/表位保持、界面Cα RMSD和相对raw界面`fa_rep`门之外，新增不可关闭的逐重复方向门：所有`dG_separated < 0`且所有跨界面能`< 0`。协议摘要同时记录两项的最大值和跨界面能中位数；schema提升为2。
- 新增真实v1重复表集成测试，确定`interface_repack`同时触发`one_or_more_dg_separated_not_negative`和`one_or_more_cross_interface_energy_not_negative`，`interface_repack_constrained_min`成为唯一通过协议。该符号门只用于排除仍由碰撞主导的准备协议，不能把Rosetta REU解释为实验亲和力。
- v2为选中代表WT新增`selected_contact_changes.csv`，按Nb252 chain C和NK2R chain R的source auth位置逐行记录`retained/lost/gained`、残基身份、raw/prepared接触布尔值及两状态最短跨链重原子距离；代表结构重建后的保持率必须与选中重复记录逐字义一致，否则停止安装结果。gate只保留分侧的精确状态计数，详细事实只记录在该CSV一次。
- Slurm唯一活动路线已替换为全新`docs/result_artifacts/structure_preparation/pyrosetta_scoring_calibration_v2_20260811/`及对应run summary，不覆盖v1。资源合同不变；按v1实测预计7–15分钟，无checkpoint或RosettaMP。
- v2入口现为1007行，已再次按职责检查：科学选择、输入审计、汇总、接触状态和gate均在605行纯模块中独立测试；入口剩余内容是同一PyRosetta环境中的Pose准备、测量、代表结构重建及事务性编排。当前没有第二个运行入口复用这些PyRosetta态操作，继续拆分只会创建无稳定复用方的工具层，因此保留单一入口；候选评分开始复用这些操作时再提取共享runtime。
- 本地没有PyRosetta，未生成或伪造v2结果。专项测试`8 passed`，包含真实v1选择回归与精确接触状态/距离测试；全套验收`113 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Slurm LF和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 实现提交：`e3de4e6229e680d8199e398ad2ecb9168fecc75c`（`Implement PyRosetta scoring calibration v2`）。

## 2026-08-11 10:33:43

### 评分校准v2远程结果同步与科学release

- 已同步远程结果提交`288bd45c7a38d4d07091d7bf863e3232d7091e17`。任务实测465.154733秒，两协议各8个重复；结果目录包含schema 2 gate、重复表、选择JSON、代表prepared WT、逐残基能量、67行精确接触变化和QC SVG，v1制品未被覆盖。
- v2正确阻断`interface_repack`：8/8重复的`dG_separated`及跨界面能均为正；中位分别为+58.425061 REU和+12.675360 REU。`interface_repack_constrained_min`为唯一通过协议，8/8重复两项均为负；中位`dG_separated=-58.147156 REU`、MAD 2.357017，中位跨界面能-82.876253 REU。
- 选中协议的界面`fa_rep`中位22.466594 REU，相对raw 183.483686下降87.755536%；最大界面Cα RMSD 0.071804 Å，最低VHH接触保持0.875、最低NK2R表位保持0.918919，映射、断点、二硫键和有限指标全部通过。Nb252 C102 TYR的`fa_rep`从raw 61.562396降至代表结构4.922464 REU，原缺侧链碰撞不再主导局部界面。
- 代表结构seed为8112027，VHH原24接触中保留21、丢失3并新增2；NK2R原37表位残基中保留34、丢失3并新增4。VHH source-auth E46、D101、I103成为`lost`，S50、T53成为`gained`；I103 prepared距离4.000093 Å属于严格4 Å边界切换，D101从2.975572 Å变为4.568493 Å更具实际结构意义。低Cα RMSD说明结合主链构象得到保持，但局部侧链接触并非逐项冻结。
- 接受v2 gate并将科学状态更新为`released_for_paired_relative_candidate_scoring`。后续候选必须相对同一prepared WT、使用同一受约束局部协议和结构安全门；Rosetta值只用于相对排序。实验24位界面仍是候选定义和谨慎保护依据，prepared接触集合只作评分/QC，不得覆盖实验界面；E46/D101/I103变体不得只依据单一prepared构象淘汰。
- 一次性一致性验证确认gate pass、16行重复、67行接触状态、396个polymer残基及关键符号条件相互一致；评分校准专项测试`8 passed`，`git diff --check`通过。未启动候选生成或评分。

### Git sync status

- 远程结果提交`288bd45c7a38d4d07091d7bf863e3232d7091e17`已在`origin/main`并同步至本地。
- 科学复核已提交并推送至`origin/main`。
- 复核提交：`3b06297ceacbb6a912ee1a456ea6c85bd31762f5`（`Review PyRosetta scoring calibration v2 result`）。

## 2026-08-11 10:58:44

### 多目标优化路线确认为并行三轨道发现

- 用户确认继续采用“亲和力、稳定性/表达、风险修复并行发现，汇合后少量组合，实验验证后再串行迭代”的方案。三条轨道均以同一authoritative 128-aa WT为母本，不能将某条纯计算轨道的优选序列提前作为另一条轨道母本；资源调度可以分批，但不改变科学上的并行可比设计。
- 亲和力轨道完整枚举实验24界面位点的456个单点；稳定性/表达轨道从非界面、非冻结位点中按结构或序列证据提出候选；风险修复轨道只针对实际识别的化学、聚集或异常表面风险提出保守候选。后两轨不为凑数进行全位点穷举。
- 三轨分别筛选后以硬约束和Pareto方法汇合，第一轮只组合少量机制互补的单点，所有组合重新评估全部目标。实验面板保留WT、各轨代表单点和少量双突变；只有实验结果产生后才进入以验证优质序列为母本的串行第二轮。
- 下一实施阶段限定为本地WT与候选空间基线：生成透明理化/风险和保留观测语义的yield基线、三轨位点资格及独立manifest、候选空间gate和可复现图；不安装服务器模型、不运行PyRosetta/AF3或组合突变。预计0.5–1天，不超过1小时的单次本地入口，无需Slurm、checkpoint或重复输入检查。

### Git sync status

- 本次路线与交接更新尚未提交或推送。

## 2026-08-11 11:15:26

### 当前执行顺序收敛为先完成亲和力轨道

- 用户决定先完成证据较确定的亲和力轨道，表达/稳定性候选及模型方案随后另行讨论。总体仍保留“两条主轨从同一WT独立发现、风险横向监测、实验后串行迭代”的架构，但当前不实施47条yield小模型、nanoBERT PLL相关性、AntiFold/AbMPNN或风险修复候选。
- 下一阶段先在本地从实验24界面位点生成全部456个非WT单点，不按prepared接触变化或主观化学偏好预删；随后实现严格复用v2受约束局部协议的成对PyRosetta评分。先以约12个分层代表候选做3重复Slurm pilot，通过后再分片运行456候选3重复初筛，入围者补足8重复并进行更严格亲和力复核。
- 当前阶段不组合突变，不把Rosetta REU解释为实验亲和力；实验24位界面和NK2R原表位继续作为结构保持基准，E46/D101/I103不得只依据单一prepared构象淘汰。单次本地manifest入口预计小于1小时；pilot小于1小时但因PyRosetta远程环境仍走Slurm，完整初筛采用每任务低于5小时的分片路线。

### Git sync status

- 本次顺序调整与交接更新尚未提交或推送。

## 2026-08-11 12:00:00

### 亲和力单突变空间与成对PyRosetta pilot实现

- 本地正式生成`affinity_single_mutants_20260811/`：实验严格`<4 Å`的24个Nb252界面位点各枚举19种非WT替换，共456条单突变。每条保留authoritative 128-aa序列、reported/IMGT/source-auth三套可逆标识、实验界面语义及prepared-WT接触敏感标志；E46/D101/I103未被删除。12个分层pilot覆盖FR/CDR、保守/跨类别替换和三个prepared敏感位点。候选表、FASTA、位置摘要、pilot ID、gate、manifest、600 dpi PNG/SVG及run summary均已生成，实测约2秒；PNG已人工检查无遮挡。
- 新增共享`pyrosetta_runtime`，校准入口和候选入口均调用同一`ref2015`、局部repack、主链坐标约束最小化、断点/二硫键/映射检查、严格接触及配对分离能实现；校准入口由1007行缩减到503行，旧重复runtime实现已删除。候选评分以v2 selected prepared WT为共同起点；每个replicate/seed只计算一次共享WT，随后全部突变体以同一seed分别计算并引用该WT。12候选×3重复因此是3次WT和36次突变体，不是每个候选重复计算WT。WT控制独立保存3行，候选表只保存`wt_control_id`、突变体指标和`mutant-WT`差值，避免重复记录。候选能量、接触保持或RMSD不合格只淘汰候选；仅映射/断点/二硫键/有限数值等运行安全失败或WT对照失效阻断pilot。
- 新增远程评分入口和唯一pilot Slurm包装：12候选×3重复，`batch`、1 GPU、12 CPU、2小时上限、不显式申请内存，预计实际5–15分钟且不需要checkpoint。当前只实现pilot，不提前实现456候选分片或后续8重复；真实PyRosetta运行尚未发生。
- 本地全套验收为`126 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`、`git diff --check`和Slurm LF检查通过。表达/稳定性、nanoBERT、AntiFold/AbMPNN、风险修复和组合突变均未启动。

### Git sync status

- 已提交并推送至`origin/main`。
- 实现提交：`052d08e4318a1d0681649b5b872344a637634a6b`（`Implement affinity single-mutant pilot`）。

## 2026-08-11 15:15:12

### 亲和力PyRosetta pilot远程结果同步与科学hold

- 已同步远程结果提交`e28207d79e4b2cc83f3ecb85130f148c2040300f`。任务实测1157.355071秒（19.289251分钟），完整产生3行共享WT、36行候选重复、12行候选汇总、machine gate和SVG；12候选各3重复。所有突变体映射、断点、二硫键和有限数值均通过，3个WT的`dG_separated`和跨界面能均为负，说明突变入口、共享WT循环和运行安全路线可执行。
- 生成machine gate为pass并报告3个candidate pass、9个blocked，但该分类存在基线逻辑错误：再次局部准备后的配对WT自身仅保持33/37个实验NK2R表位残基，即0.8918918918918919，低于候选使用的绝对0.90阈值。9个blocked候选的最低表位保持与WT完全相同，却被判为结构不合格；因此“3 pass/9 blocked”和machine `full_affinity_scan_release=pass`均不接受为科学release。
- 能量只保留为门修订前的探索性诊断。D101K的中位`ΔdG_separated=-0.582901 REU`、中位跨界面能变化`-0.738628 REU`、中位`Δfa_rep=-0.109795 REU`，但dG MAD为0.997795 REU；Y102F相应为`-0.841451`、`-1.047791`、`-0.260875 REU`，却被上述WT基线问题错误标为blocked，且source C102原本侧链不完整，后续必须谨慎。Y37A虽然`ΔdG_separated=-3.966383 REU`，跨界面能变化却为`+0.473039 REU`，属于指标方向不一致；这些都不是实验推荐结论。
- 新增机器可读`affinity_pilot_scientific_review.json`，将状态置为`held_pending_candidate_structure_gate_revision`。下一步应把运行安全与候选选择继续分离，并将接触/表位及构象保持显式定义为相对同seed配对WT、同时保留原实验集合解释；随后在新目录重跑pilot，不能覆盖v1结果或直接启动456扫描。

### Git sync status

- 远程结果提交`e28207d79e4b2cc83f3ecb85130f148c2040300f`已在`origin/main`并同步至本地。
- 科学复核已提交并推送至`origin/main`。
- 复核提交：`1a15c89fbd0a13016b65157e2225dc529437fd70`（`Review affinity PyRosetta pilot result`）。

## 2026-08-11 15:40:32

### 亲和力PyRosetta pilot V2实现

- 将候选计算与科学筛选彻底分离：pilot V2对显式列出的全部候选完成评分，扫描阶段不按能量、接触保持率或界面RMSD淘汰候选。候选摘要统一标记`selection_status=not_applied_scan_stage`；运行门只检查候选映射、断点、二硫键、有限数值和配对WT对照有效性。
- 每个WT和突变体重复现保存实际VHH/NK2R source-auth接触位置集合；逐候选同时保留相对原实验集合的接触保持率，并新增相对同seed配对WT的VHH及NK2R接触保持率。这样可纠正v1用WT自身未达到的0.90绝对阈值筛掉同水平候选的问题，同时不丢失实验表位解释。
- V2 gate和run summary显式记录`candidate_filtering_applied=false`及`full_scan_contract=score_all_declared_candidates_then_filter_once`。Slurm入口改为全新`affinity_pyrosetta_pilot_v2_20260811/`目录，保留v1制品不覆盖；资源仍为`batch`、1 GPU、12 CPU、2小时，预计约20分钟且无需checkpoint。
- 全量456单点的后续合同固定为：先完成所有候选及分片合并完整性核验，再通过独立入口一次性统一筛选；本次只实现pilot V2，未实现或启动456全量扫描。精确接触集合由候选入口显式请求，校准入口维持原CSV schema，避免共享runtime新增字段破坏历史校准重跑。
- 本地专项测试`8 passed`；全套验收`126 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`和`git diff --check`通过。真实PyRosetta V2尚待服务器Slurm运行。

### Git sync status

- 已提交并推送至`origin/main`。
- 实现提交：`f438f3a6c95fefaccf1f6b13f71e6c905e6eadd0`（`Implement affinity scoring pilot V2`）。

## 2026-08-11 16:24:23

### 亲和力PyRosetta pilot V2远程结果同步与科学release

- 已同步远程结果提交`181cbc51561950f6d06b15433fb47a38764c0d05`。任务实测1146.969247秒（19.116154分钟），完整产生3行共享WT、36行突变体重复和12行候选摘要；36/36重复、12/12摘要及3个WT均通过运行安全，所有候选摘要明确为`not_applied_scan_stage`，没有在pilot中执行候选筛选。
- 从CSV接触集合独立重算后，接触计数和两类配对WT保持率逐行一致：12个候选的NK2R表位相对同seed WT保持率均为1.0，VHH接触相对同seed WT保持率为0.958333–1.0。相对原实验集合的VHH范围为0.875–0.958333、NK2R为0.891892–0.918919，仅作为非阻断来源/QC记录。
- V1和V2的共同数值字段在相同候选、重复和seed下逐字一致；V2没有改变评分轨迹，只修正了候选筛选时机和配对WT参照语义。新增`affinity_pilot_v2_scientific_review.json`，科学状态为`released_for_full_456_scan_implementation`。
- 下一阶段可实现456候选的确定性Slurm分片和独立合并入口；所有候选必须先完成3重复评分和1368行完整性核验，再统一筛选。pilot中的能量方向不构成候选推荐，表达/稳定性轨道仍暂停。
- 科学复核JSON与gate/CSV一致性检查通过，亲和力评分专项测试`8 passed`，`git diff --check`通过；未改写远程生成的gate、CSV、SVG或run summary。

### Git sync status

- 远程结果提交`181cbc51561950f6d06b15433fb47a38764c0d05`已在`origin/main`并同步至本地。
- 科学复核已提交并推送至`origin/main`。
- 复核提交：`94533692721c5995033d6c8a95a2d8be981f466f`（`Review affinity scoring pilot V2 result`）。

## 2026-08-11 16:52:30

### 456×3亲和力全量扫描实现

- 本地从完整456候选生成确定性12×38分片plan；每片恰含两个完整实验界面位置的19种非WT替换，共预期1368个突变体评估。plan显式记录`candidate_filtering_applied=false`和`score_all_declared_candidates_then_filter_once`，未根据pilot、能量或理化类别改变候选集合。
- 评分入口新增显式`full_scan_shard`模式并复用pilot V2的prepared WT、三组seed、PyRosetta协议、接触和运行安全语义；每片只产生3个WT、114行突变体重复、38行未筛选摘要、shard gate和ignored run summary，不生成分片候选图或科学筛选状态。
- Slurm array固定`0-11%4`，即最多4片并发；每片`batch`、1 GPU、12 CPU、2小时，不显式申请内存。按pilot实测预计每片55–65分钟、三轮约3–4小时且无需checkpoint。提交包装在提交前只检查一次merge环境的matplotlib/numpy导入，并以`afterok`自动提交合并任务。
- 合并入口只有在12片全pass后运行，验证456候选、1368个唯一`candidate×replicate×seed`键、分片归属、候选身份、未筛选状态和跨片三组WT；WT数值仅允许`1e-6`跨节点误差。通过后写3行去重WT、1368行重复、456行摘要、绘图表、merge gate、600 dpi PNG/SVG和run summary；统一筛选仍是后续独立阶段。
- 本地plan已正式生成于`affinity_pyrosetta_full_scan_plan_20260811/`，实测0.029907秒，run summary同步生成；未运行远程PyRosetta或伪造全量结果。
- 全套验收`132 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`、Shell/Slurm LF和`git diff --check`通过。测试覆盖真实456分片、完整1368键合并、缺片/提前筛选阻断、分片等待merge语义、最终PNG/SVG及`0-11%4`/`afterok`合同。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement unfiltered affinity full scan`。

## 2026-08-12 09:14:40

### 456×3亲和力全量扫描结果同步与完整性复核

- 已同步远程结果提交`6268ee8`。merge gate为pass：12片累计42645.718076秒（11.846033 job-hours），完整产生3个去重WT、1368行突变体重复和456行摘要；所有candidate×replicate×seed键唯一且与manifest完全相等，0个runtime failure，全部摘要为`not_applied_scan_stage`，扫描和合并均未筛选。
- 从合并CSV独立复算全部接触集合计数、相对同seed WT的VHH/NK2R保持率、`ΔdG`和跨界面能差，均与记录一致。配对WT VHH接触保持范围0.916667–1.0，NK2R表位保持0.891892–1.0；界面Cα RMSD最大值范围0.037837–0.072868 Å。
- 未筛选景观中113/456个候选`ΔdG_separated`中位数为负，143/456个跨界面能变化为负，87/456个两者均负；82个候选两项方向不一致。`ΔdG`中位数分布为-5.446951至30.681403 REU、总体中位1.576840；MAD总体中位0.111585 REU，355个候选MAD≤0.5、429个≤1.0。这些是完整扫描的描述统计，不是候选选择。
- 新增`affinity_full_scan_scientific_review.json`，科学状态为`ready_for_post_scan_filter_implementation`并明确`candidate_selection_performed=false`。下一步只能对完整456候选实施一次预声明统一筛选，不能回到分片或按当前叙述中的个别数值人工挑选。
- 人工检查远程600 dpi PNG时发现右面板重复Y轴标签压入热图；已修正共享Y轴和colorbar标题布局，并只从已跟踪`full_scan_plot_data.csv`重绘PNG/SVG，未改动评分CSV、gate或run summary。修正后图片无遮挡。
- 科学复核与绘图修正后全套验收`132 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`和`git diff --check`通过。SVG现由绘图函数统一规范为LF且移除行尾空白。

### Git sync status

- 远程结果提交`6268ee8`已在`origin/main`并同步至本地。
- 科学复核和图布局修正已提交并推送至`origin/main`。
- 提交：`e6d798c`（`Review affinity full scan result`）。

## 2026-08-12 14:00:00

### 亲和力全量扫描统一分层

- 新增本地唯一post-scan入口，只读取已通过merge和科学复核的456候选、1368重复、候选manifest、关键残基集及评分release；不重新运行PyRosetta、不删除候选、不生成组合突变。实测约1.5秒，不需要Slurm。
- 硬有效性与科学分层分离：456条全部通过映射、断点、二硫键、有限数值、运行状态和`≤0.5 Å`界面Cα RMSD门。Tier 1要求两项配对WT能量3/3重复均为负、VHH/NK2R配对WT接触保持均为1.0且`Δfa_rep`中位数不增加；Tier 2保留3/3双能量且NK2R表位完全保持但需局部复核者；其余依次按双负中位数、能量方向冲突和无双负中位数支持分层。
- 完整互斥结果为Tier 1/2/3/4/5=`18/30/39/82/287`；Tier 1/2共48条构成下一阶段严格复核池。风险标签记录prepared敏感、C102 source侧链不完整、接触变化、排斥增加、重复翻转和能量冲突；层内Pareto使用七项原始指标，不构造加权综合分。该结果明确`candidate_selection_performed=false`，不是最终实验面板。
- 正式制品位于`affinity_post_scan_filter_20260812/`：456行Tier表同时作为精确绘图数据、48行复核池、Tier摘要、gate及600 dpi PNG/SVG；run summary独立保存。四面板图已人工检查无遮挡，固定时间戳双跑六个正式制品逐字节一致。
- 全套验收为`141 passed, 1 skipped, 4 subtests passed`；唯一skip仍为Windows真实symlink权限测试。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement affinity post-scan tiers`。

## 2026-08-12 10:58:00

### Flex ddG生产参数计时pilot实现

- 在不决定是否纳入Tier 3的前提下，生成固定4候选×2独立样本的8任务pilot：Tier 1常规低风险`R45T`、Tier 1的C102 source侧链不完整/大芳香替换`Y102W`、Tier 2排斥增加`E105W`、Tier 3受体表位接触风险`S114I`。每任务固定35,000次backrub、1.2 kT、突变位点8 Å邻域、唯一seed；Slurm数组为`0-7%8`，每任务`batch`、1 GPU、12 CPU、6小时上限，汇总任务以`afterok`衔接。完整正式范围预计超过5小时，因此提交包装先在真实PyRosetta环境执行一次`--check_only`；pilot自身就是生产参数烟雾测试，不增加任务内checkpoint。
- 可复用的manifest/结果验证/median-P90耗时投影、PyRosetta backrub运行层和确定性绘图分别沉入`flex_ddg`、`flex_ddg_runtime`和`flex_ddg_plot`。运行层复用现有`pyrosetta_runtime`的source-auth突变、repack、界面能、接触、RMSD及映射/断点/二硫键安全检查；只新增WT约束最小化、局部backrub和同一最终接受backbone上的独立WT/突变分支。协议保持Flex ddG成对backbone顺序，但使用项目`ref2015`和released prepared WT，不声称逐字复现旧talaris/GAM权重。
- 正式plan已写入`docs/result_artifacts/candidate_design/flex_ddg_pilot_plan_20260812/`，run summary位于`docs/run_summaries/candidate_design/flex_ddg_pilot_plan_20260812.json`。plan明确`candidate_selection_performed=false`和`tier_3_scope_decision_performed=false`；两次样本只用于协议可行性、阶段耗时、峰值内存和输出规模，不支持候选排序。汇总完成后将按实测任务median/P90投影48候选和87候选各20样本、并发8的墙钟时间，再由用户决定正式范围。
- 本地Flex ddG专项`7 passed`，全套验收`148 passed, 1 skipped, 4 subtests passed`；真实PyRosetta backrub API与8个远程任务尚未运行，因此当前没有pilot耗时、分数或Tier 3范围结论。预计pilot墙钟受单任务速度控制，任务上限6小时；真实耗时必须由本次运行测得，不能用旧单突扫描线性替代。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement Flex ddG timing pilot`。

## 2026-08-12 18:30:00

### Flex ddG pilot C++段错误诊断与分段修复

- 首次远程8任务数组在Python入口内由PyRosetta C++层产生segmentation fault，没有Python traceback。原实现把空间邻域交给`BackrubMover.add_mainchain_segments()`自动分段；本项目prepared WT保留实验缺失区对应的FoldTree jump/cutpoint，而底层backrub自动分段不适合作为该gap-safe Pose的安全边界。现有日志不足以精确证明崩溃发生于建段还是第一次move，因此根因表述保持为该高风险路径，不能把段错误解释为内存不足或候选能量问题。
- 活动路线已删除自动分段，改为显式枚举3–12残基CA-pivot segment；每个segment必须由同链、连续pose索引组成且不跨任何FoldTree cutpoint。新增阶段开始日志和`faulthandler`，便于区分初始最小化、segment建立和backrub轨迹；不改变4个候选、2样本、35,000 trials、1.2 kT、8 Å邻域、`ref2015`或成对WT/突变分支的科学合同。
- 原`--check_only`只初始化PyRosetta并检查方法存在，无法触发C++运行错误。现在提交包装仍只检查一次，但会在真实prepared WT上对4个代表候选各构建其实际邻域并执行1次backrub move，同时复核映射、断点和二硫键；任何segment/API/C++失败都会发生在`sbatch`前。失败任务没有产生正式任务结果，Tier 3范围及候选科学结论均未改变。
- 本地Flex ddG专项`9 passed`，全套验收`150 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`和`git diff --check`通过。修复后的真实PyRosetta one-move precheck及数组尚未在服务器执行。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Fix Flex ddG backrub segmentation`。

## 2026-08-12 19:10:00

### Flex ddG显式segment的input pose绑定修复

- 修复后首次提交前one-move检查没有进入`sbatch`，而是在`BackrubMover.add_segment()`处由Rosetta给出明确断言`input_pose != nullptr`（`BackrubMover.cc:360`）。这说明上一轮已成功把错误前移并避免8个数组任务，但直接PyRosetta调用缺少JobDistributor通常负责的mover input pose初始化；该错误不是随机段错误、内存不足、候选评分失败或FoldTree cutpoint再次被跨越。
- 在创建`BackrubMover`后、添加任何显式AtomID segment前调用官方绑定`set_input_pose(sampled)`，并把`set_input_pose`加入API合同。安全segment定义、4候选×2样本、35,000 trials、1.2 kT、8 Å邻域及所有评分语义不变；真实4邻域one-move检查仍在`sbatch`前且只执行一次。
- 本地Flex ddG专项`9 passed`，全套验收`150 passed, 1 skipped, 4 subtests passed`；修复后的真实PyRosetta one-move检查尚待服务器执行。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Bind input pose for Flex ddG segments`。

## 2026-08-12 14:32:57

### Flex ddG生产参数pilot结果同步与独立复核

- 已同步远程结果提交`0bfcc885b7832c979c6a1a4b094fb5e1f2715676`。pilot gate为pass：4个代表候选×2样本共8任务全部完成，任务身份逐项与manifest一致，WT和突变体的映射、预期断点、二硫键检查全部通过；汇总仍明确`candidate_selection_performed=false`和`tier_3_scope_decision_performed=false`。
- 8任务累计6000.201128秒（1.666723 job-hours）；单任务耗时678.203680–875.336479秒，中位717.980720秒、P90 874.753734秒。backrub累计占总任务时间94.7212%，是明确的运行瓶颈；峰值内存975.652–1000.617 MiB，每任务输出约1.700–1.702 MB。计时图已人工检查，坐标、图例和“不可用于候选排名”的限定语无遮挡。
- 独立复算20样本、并发8、1.2开销因子的范围投影：Tier 1/2共48候选为median 28.7192小时、P90 34.9901小时；Tier 1/2/3共87候选为median 52.0536小时、P90 63.4196小时，纳入39个Tier 3的P90增量约28.4295小时。建议纳入Tier 3，因为额外约1.18天可保留较弱但仍有双负中位数证据的机制多样性；这是待用户确认的建议，不是已执行的范围决策。
- 两重复能量只作协议诊断：代表候选中`R45T`、`E105W`和`S114I`至少一项能量方向在样本间不稳定，证明pilot没有候选排名统计能力；所有REU仍是模型特异信号而非实测亲和力。新增机器可读`flex_ddg_pilot_scientific_review.json`记录完整性复核、资源统计、投影、科学限制和待确认建议。

### Git sync status

- 远程结果提交`0bfcc88`已在`origin/main`并同步至本地。
- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Review Flex ddG timing pilot result`。

## 2026-08-12 15:00:05

### Tier 1–3 Flex ddG正式生产与任务级续传实现

- 原始实现曾按用户当时决定纳入全部Tier 3；合作者随后明确最终仅需30条优化序列，用户据此要求将生产范围修订为Tier 1/2全部`18+30`条，加补充独有位置的Tier 3 `D33N/Y115F`两条，共50候选。每候选20个独立样本，形成1000个唯一`candidate×sample×seed`任务；其余37条Tier 3明确不进入本轮。生产plan仍要求全部计算后统一筛选，当前`candidate_selection_performed=false`。
- 复用pilot已验证的显式gap-safe backrub、成对WT/突变分支、PyRosetta测量和结构安全代码；运行入口增加显式`pilot/production`合同选择，生产precheck仍只对固定4个代表候选各运行一次真实backrub move，成功后按Git revision记录一次性marker，续跑不重复同版本检查。新增的manifest、恢复检查、任务聚合和绘图逻辑沉入`src/antibody_optimization/`，实验脚本只负责I/O和编排。
- 任务级断点续传只把六个输出文件齐全、manifest身份一致、任务status为pass且WT/突变映射、断点、二硫键检查均pass的任务视为完成；无输出任务重新提交，部分输出、身份冲突或安全失败明确阻断且不自动覆盖。默认把pending任务按900项分成顺序依赖批次，以避免单一超大数组；并发只是调度参数，不改变候选、样本或seed。
- 正式包装默认数组并发12，支持提交/续跑时`--concurrency N`，也可用独立脚本对同一次提交的全部数组执行Slurm`ArrayTaskThrottle`更新。按pilot P90、1.2开销因子线性外推，并发12约24.30小时，超过5小时，因此保留真实precheck和恢复边界。默认每任务2小时上限；完整结果后由依赖任务生成1000行任务表、50行未筛选候选摘要、gate及600 dpi PNG/SVG。
- 根据用户的新统一要求，AGENTS新增Slurm日志规则；生产数组和汇总日志均写入`logs/flex_ddg_production/`，提交包装在`sbatch`前创建该目录，`logs/`加入Git忽略。正式生产plan和1000行manifest已重新生成；真实远程precheck和生产数组尚未运行。
- 本地专项验收`15 passed`，全套验收`156 passed, 1 skipped, 4 subtests passed`；`compileall`、Bash语法检查、`pip check`和`git diff --check`通过。唯一skip仍为Windows真实symlink权限测试；历史的87候选manifest未保留为活动路线。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement resumable Flex ddG production`。

## 2026-08-13 10:30:00

### Flex ddG正式生产结果同步与独立复核

- 从`origin/main`快进同步远程结果提交`41f6983`。机器gate为pass：50候选×20样本共1000任务，manifest任务身份逐项一致，1000个task ID及`candidate×sample×seed`键均唯一；1000/1000状态pass，所有WT/突变映射、断点、二硫键检查均通过，数值均有限。汇总仍明确`candidate_selection_performed=false`和`ready_for_ensemble_filter_design`。
- 1000任务累计764515.9383秒（212.3655 job-hours）；单任务耗时554.7669–959.1019秒，中位760.8766秒、P90 871.7730秒。backrub占累计任务时间94.8628%；峰值内存964.8125–1009.0586 MiB，每任务输出约1.6998–1.7021 MB。Git跟踪制品没有调度起止时间，因此不报告未经记录的实际墙钟时间。
- 50条中`ΔdG_separated`中位数为负38条、跨界面能中位数为负47条，两项均负38条、方向冲突9条、两项均正3条。两项负向样本数均达到20/20、≥19/20、≥18/20、≥16/20、≥15/20的候选分别为1、5、8、13、17条；旧三重复与20样本相比有12个候选至少一项中位数跨过零点，因此原Tier只能保留为来源标签，不能直接作为最终ensemble排名。
- 候选相对各自同样本配对WT的最低接触保持率范围下限为VHH 0.913043、NK2R 0.944444；20条候选两侧最低保持率均为1.0。选入的Tier 3中`D33N`为10/20与15/20负向支持、两项中位数方向冲突；`Y115F`为10/20与18/20负向支持，只有弱双负中位数。因此两条都不能仅凭旧Tier补位直接进入最终面板。
- 新增`flex_ddg_production_scientific_review.json`固化完整性、资源、ensemble计数、Tier 3复核和解释限制。原结果图的纵轴标签相互重叠，已仅修改绘图布局和准确标题并从同一50行候选表重绘PNG/SVG；人工检查后无标签、标题或面板重叠，未修改任何评分表。
- 下一步不是按单项REU取前30，而是先设计显式ensemble筛选，再结合最终30条总名额，为亲和力单点、稳定性/表达候选、少量组合和对照分配空间。
- 本次同步复核后全套验收`157 passed, 1 skipped, 4 subtests passed`；`compileall`、`pip check`和`git diff --check`通过。唯一skip仍为Windows真实symlink权限测试。

### Git sync status

- 远程结果提交`41f6983`已在`origin/main`并同步至本地。
- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Review Flex ddG production result`。

## 2026-08-13 11:30:00

### 多目标设计路线确认

- 用户确认采用“并行发现、条件化组合”：亲和力与稳定性/表达模块先分别从同一WT发现，但最终不是机械拼接或分开交付；以多个亲和力核心为序列背景，重新设计和筛选稳定性/表达模块，再对完整组合重新评价。该路线避免把WT背景下的模块效果视为在任意亲和力背景中可加和，也避免过早锁定唯一亲和力母本。
- 单突只用于建立可解释证据，不作为最终序列硬限制。亲和力主要推进至双突；稳定性/表达模块允许1–3个协调framework突变；跨轨道完整组合以总计2–4个突变为主。最终30条以多目标组合为主体，同时保留少量WT和单模块对照用于实验归因。
- 工具路线收敛为AntiFold主导结构条件模块生成、nanoBERT先经47条yield相关性验证后决定其证据等级、PyRosetta复核完整亲和力/跨轨道组合、理化与聚集/化学风险横向过滤、AF3只复核少量终选组合。同类型AbMPNN暂不重复部署。
- 下一阶段先在本地并行完成亲和力ensemble核心模块筛选和稳定性/表达发现合同；随后验证nanoBERT信号并建立AntiFold最小路线。尚未生成稳定性/表达突变或跨轨道组合，也未确定最终30条名单。

### Git sync status

- 状态：尚未提交；目标为`origin/main`。
- 计划提交信息：`Update conditional multi-objective design route`。

## 2026-08-13 12:00:00

### 亲和力ensemble核心筛选与稳定性/表达设计合同

- 本地实现并运行亲和力ensemble核心筛选。入口同时核验50×20生产gate/scientific review与456候选post-scan gate，完整保留50条证据；唯一选择门为`ΔdG_separated`和cross-interface两项均至少18/20样本为负且两项中位数均为负，不使用加权综合分。结果选择`R45C/R45V/D101W/I103W/E105F/E105L/N107A/S114M`共8个核心单点、覆盖6个位置；R45和E105的替换在组合时互斥。`fa_rep`、两侧接触保持、prepared敏感性、旧风险标签和全局Pareto层均保留为独立证据，未生成双突或最终实验候选。
- 基于已发布的128位阶段0清单建立稳定性/表达WT发现合同：72个有实验坐标的非界面framework位点正常放行，9个实验缺失framework位点仅在完整VHH AF3证据下谨慎放行；24个实验界面、17个CDR/terminal flank以及Cys22/Cys95和末端SSGS共47位冻结。合同规定主要模块1–3突变、极少探索项最多4突变，但本阶段未调用AntiFold/nanoBERT，也未生成或评分突变。
- 可复用逻辑分别沉入`affinity_ensemble`、`stability_expression_contract`和统一绘图模块；新增两个本地CLI，均支持一次`--check_only`、默认拒绝覆盖并输出紧凑表、机器gate、600 dpi PNG/SVG和独立run summary。正式制品位于`affinity_ensemble_core_20260813/`与`stability_expression_design_contract_20260813/`，图已人工检查。
- 本阶段预计和实测均为本地分钟级，不超过1小时或5小时，无需Slurm、断点续传或远程环境。
- 新增专项测试5项并通过；全套验收为`162 passed, 1 skipped, 4 subtests passed`，唯一skip仍是Windows真实symlink权限测试。`compileall`、`pip check`和`git diff --check`通过。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement affinity core and stability design contract`。

## 2026-08-13 14:00:00

### nanoBERT—reported yield关联验证实现与运行计划

- 完成47条真实序列的固定验证计划：31条LTT/WCC `individual_approximate`保留原reported数值用于主要数值关联；LLJ的7条`group_approximate`与9条`group_lower_bound`只映射为`~2/~10/>20`三级有序/删失标签，不生成个体点估计。46条ANARCII pass序列保留provisional IMGT区域映射；`WCC__4-28`仍允许完整reported序列评分，但因编号失败不生成FR/CDR汇总。
- nanoBERT运行固定使用`NaturalAntibody/nanoBERT` revision `edc8182ad89a827f8737fa572c6b5fac6197e6b0`及本地离线快照，许可证记录为`CC BY-NC-SA 4.0`。评分逐残基单独mask，记录WT residue自然对数概率；主指标为完整reported序列长度归一化mean PLL，编号域/FR/CDR仅作敏感性分析。实现显式验证tokenizer必须保持一残基一token，避免把未mask输出或错误tokenization称为LogP。
- 统计合同预先固定：Spearman/Kendall、LTT/WCC来源内方向、来源分层rank相关、控制provider和sequence length的partial rank、5000次来源分层bootstrap和permutation、provider-intercept LOOCV增益；LLJ只做Kendall tau-b。同步计算长度、分子量、pI、pH 7.4电荷、GRAVY、芳香性、instability index、组成及少量序列motif基线。不训练高容量模型。
- 证据gate只允许`weak_ranking_evidence`、`compatibility_filter_only`或`no_supported_use`；无论结果如何，nanoBERT都不称为表达量或mg/L预测器，`nb252_expression_prediction_validated=false`。正式本地plan位于`nanobert_yield_validation_plan_20260813/`；当前没有远程模型分数、结果图或关联结论。
- 远程唯一入口为`submit_nanobert_yield_validation.sh`：Slurm `batch`、1 GPU、12 CPU、1小时，日志写入`logs/nanobert_yield_validation/`；模型评分使用`vhh-lm`离线环境，统计绘图切换项目`ab_optim`。预计低于1小时且两个阶段都不独立昂贵，因此默认拒绝已有输出、不实现断点续传。
- 本地真实输入precheck及新增专项`4 passed`；全套验收`166 passed, 1 skipped, 4 subtests passed`，`compileall`、`pip check`、Bash语法和`git diff --check`通过。正式远程任务尚未提交。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement nanoBERT yield validation`。

## 2026-08-13 16:30:00

### nanoBERT—reported yield真实结果同步与科学判定

- 已快进同步远程结果提交`64c5ac9`。首次Slurm任务已完成47/47条nanoBERT评分，随后在项目`ab_optim`统计阶段因缺少Biopython而停止；用户安装Biopython后直接复用完整模型分数运行分析，没有重复GPU评分。最终结果含47行样本证据、17行特征统计、机器gate、600 dpi PNG/SVG和run summary；统计阶段实测7.020229秒。
- nanoBERT主指标为完整reported序列mean PLL。31条LTT/WCC个体数据的全样本Spearman为0.188679，但来源分层Spearman转为-0.229454、长度调整partial Spearman为-0.201963；LTT内部-0.241345、WCC内部0.060245。来源分层5000次bootstrap 95% CI为[-0.598167, 0.221902]，5000次置换p=0.279944；provider-intercept LOOCV Spearman为-0.301685，相对provider-only基线增益-0.050701。16条LLJ有序/删失tau-b为-0.230783。合并弱正趋势在控制来源后反向，且不确定区间跨零、置换不显著、预测外检验无增益。
- Gate本身`status=pass`表示流程和数据完整，但科学证据等级为`no_supported_use`，release为`nanobert_not_supported_for_candidate_use`，`nb252_expression_prediction_validated=false`。因此nanoBERT不进入Nb252候选表达/稳定性排序、过滤或组合评价；该结果不否定模型作为通用VHH infilling工具的用途，只说明本项目47条reported yield没有支持其PLL用于目标任务。
- 基础理化特征结果只作探索性对照：例如hydrophobic fraction的来源分层ρ=0.3004、pI=0.3935、pH 7.4电荷=0.3953，但没有为这些多特征执行相同的预注册证据门或多重比较校正，且部分来源内部方向不一致。不得将其解释为已验证的表达优化规律或据此训练/筛选Nb252候选。
- 结果图已人工核对：A面板展示LTT/WCC来源分层与合并混杂，B面板展示预声明特征的来源内效应方向，C面板保留LLJ三档有序/删失语义；轴、图例和标签无遮挡。

### Git sync status

- 远程结果提交`64c5ac9`已在本地`main`；本次科学解释和handoff同步状态为`ready to commit`，目标为`origin/main`。
- 计划提交信息：`Review nanoBERT yield validation result`。

## 2026-08-13 19:18:37

### 多目标路线收敛为WT框架保持策略

- 重新核对47条真实序列的provisional IMGT映射：46条可编号序列总体差异主要集中在CDR，但并非单一framework库；Nb252的完整可比framework在`LTT__Nb232`、`LLJ__2C8`和`LLJ__2H8`中重复出现，多条LTT/LLJ仅相差1–2个framework残基。该证据支持将其称为现有数据中的高频近公共框架，但不把它升级为经合作者确认的正式通用框架。
- 当前方案取代此前“从81个非界面framework位点独立发现稳定性/表达模块”的路线：默认保持authoritative Nb252 WT framework，以现有亲和力单突及机制互补多突为候选来源，把BL21表达/溶解性、VHH热稳定性、TNP developability、AntiFold结构相容性和化学风险同步纳入完整序列Pareto筛选。81位合同作为历史证据保留，不再驱动候选生成。
- 现有核心中的FR2界面`R45C/R45V`单独降级：额外未配对Cys使`R45C`退出主组合；`R45V`改变VHH hallmark位置，不进入常规多突组合。二者既有20样本亲和力证据不删除，可作为高风险单突对照。只有CDR3主路线无法形成足够合格候选时，才另行审议少量证据驱动framework补救，而不是恢复大范围扫描。
- 合作者确认47条产量来自BL21体系。后续先验证NetSolP `U/S`与真实产量的关联，再逐个部署一个VHH专用Tm预测器、TNP和AntiFold评分路线；同类型工具不重复并行，预测分数不解释为实测产量、Tm或亲和力。本次仅更新当前方案文档，没有生成候选、修改既有结果或运行模型，预计及实测均低于1小时，无需Slurm。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Update framework-preserving design route`。

## 2026-08-14 09:27:38

### NetSolP远程部署合同确认

- 用户已在独立环境`/data/software/env/luly25/netsolp`完成官方5.63 GB发行包下载、解压、依赖安装、输出检查和环境版本记录。发行包位于`/homes/Tianlab/luly25/software/netsolp`，其中推理脚本按相对路径读取顶层`models/`，因此固定从该顶层目录启动，不从嵌套`Data_and_Code/PredictionServer/`启动。
- 固定调用为`MODEL_TYPE=Distilled`、`PREDICTION_TYPE=SU`；Usability是与BL21产量最接近的主要验证指标，Solubility为辅助指标。官方3序列测试同时完成S/U预测，实测57.812787秒；远程环境快照保存在软件目录的`environment.freeze.txt`。该成功只证明部署和推理入口可用，不代表NetSolP分数已与本项目47条产量建立关联。
- 下一实现阶段是一次性导出47条规范reported序列、远程评分并在项目环境进行预先固定的来源/序列簇感知关联验证。预计整体低于1小时，不需要数组或断点续传；按照服务器资源规则以单个Slurm任务运行。本次只更新当前调用合同和计划，未运行项目真实序列。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 合并纳入后续实现提交：`Implement NetSolP yield validation`。

## 2026-08-14 09:41:05

### NetSolP—BL21 yield关联验证实现

- 新增固定三段式路线：本地计划入口导出47条完整reported序列；远程评分入口从`/homes/Tianlab/luly25/software/netsolp`调用官方`predict.py`并规范化Distilled `U/S`；项目`ab_optim`分析入口执行来源分层、序列簇敏感性、简单基线增量和LLJ有序/删失分析。共享表型、Spearman/Kendall、bootstrap/permutation和基础理化逻辑直接复用既有`nanobert_yield`工具，没有复制统计实现。
- 正式计划位于`docs/result_artifacts/candidate_design/netsolp_yield_validation_plan_20260814/`，run summary位于`docs/run_summaries/candidate_design/netsolp_yield_validation_plan_20260814.json`。计划保留47/47条序列（包括ANARCII失败但可进行整序列评分的`WCC__4-28`）、31条LTT/WCC数值记录和16条LLJ有序/删失记录；90%归一化Levenshtein identity的single-linkage得到40个序列簇。
- 固定科学合同以NetSolP Usability为主指标、Solubility为辅助指标；除普通provider-intercept LOOCV外，增加leave-one-sequence-cluster-out增量，防止近邻序列泄漏被误解为外推能力。输出gate仍只允许弱排序证据、相容性过滤或不支持使用，预测分数不得称为实测产量或mg/L，也不训练47样本深度模型。
- 远程唯一入口为`submit_netsolp_yield_validation.sh`与对应Slurm脚本，日志写入`logs/netsolp_yield_validation/`；固定`batch`、1 GPU、12 CPU、30分钟。预计低于1小时和5小时，不实现断点续传；当前只完成计划和代码，尚未运行项目47条真实NetSolP评分，因此没有U/S关联结论。
- 新增专项测试4项并通过；覆盖真实47条计划/40簇、官方输出身份与概率校验、主辅指标及LLJ语义、single-linkage传递性。全套验收为`170 passed, 1 skipped, 4 subtests passed`；`compileall`、Bash语法、`pip check`和`git diff --check`通过。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement NetSolP yield validation`。

## 2026-08-14 10:00:13

### NetSolP—BL21 yield真实结果同步与科学判定

- 已同步远程结果提交`cddc166`。正式制品含47行逐样本U/S证据、15行指标、机器gate、600 dpi PNG/SVG及run summary；统计/绘图阶段实测7.856325秒。程序gate为`pass`，科学证据等级为`compatibility_filter_only`，release为`netsolp_compatibility_filter_only`，`nb252_expression_prediction_validated=false`。
- 主指标Usability在31条LTT/WCC数值记录上的合并Spearman为0.497831，来源分层Spearman为0.394300，控制来源和长度后的partial Spearman为0.285838；LTT内部为0.415430，WCC内部为-0.120491。5000次来源分层bootstrap 95% CI为[-0.066543, 0.747848]，5000次来源内置换p=0.050390，未通过预声明的不确定性、显著性和两来源同向门。
- U的普通LOOCV和leave-one-90%序列簇-out绝对Spearman分别为0.108970和0.107961；相对各自provider-only基线的增益为0.359954和0.266133。正增益部分来自provider-only基线本身为负，不能将增益值误读为强预测准确性。16条LLJ有序/删失tau-b为-0.164845，也不支持跨全部表型语义的一致排序。
- 辅助Solubility呈更一致但仍属探索性的信号：来源分层/长度调整partial Spearman为0.375772/0.453798，LTT/WCC内部为0.374877/0.397619，普通/簇外CV绝对Spearman为0.410453/0.455252；LLJ tau-b仍为-0.274742。S不是预声明主指标，没有执行同等级bootstrap/permutation证据门，不能事后升级为经验证的产量预测器。
- 结论：NetSolP U/S可保留为完整亲和力候选的辅助相容性证据，优先关注相对WT的明显回退并在Pareto表中保留原始值；当前没有校准硬阈值，不能用U/S单独排序、淘汰候选或换算产量。Nb252本身U=0.398494、S=0.531907，仅为模型输出，不代表其实测表达水平。
- 本地从正式紧凑样本表重新运行分析，47条样本、15项指标、主指标10个关键统计和证据等级与gate逐项一致；结果PNG已人工检查，四面板、坐标轴、图例和标签无遮挡。没有修改模型输出或重新运行远程推理。

### Git sync status

- 远程结果提交`cddc166`已在本地`main`；本次科学解释和handoff同步状态为`ready to commit`，目标为`origin/main`。
- 计划提交信息：`Review NetSolP yield validation result`。

## 2026-08-14 22:30:00

### TNP—BL21 yield关联验证实现（单进程路线）

- 用户明确不使用数组并发。活动路线固定为一个Slurm作业、一个Python进程顺序处理47条真实序列；每次调用固定TNP入口并使用`hscale=0`、`ncores=1`，不保留array或多进程替代路线。预计远程评分约1–3小时、低于5小时，使用`batch`、1 GPU、12 CPU、4小时上限并逐样本输出进度；不实现断点续传。
- 新增TNP结果规范化、统计和绘图工具：保留6项原始指标/flag、完整输入与NanoBodyBuilder2模型序列、可解释末端裁剪和逐样本失败；复用既有47条表型语义与90%序列簇。覆盖门要求至少46/47总样本、30/31数值样本且LTT/WCC均存在。
- PSH固定为唯一预声明主指标，预期PSH越高则reported yield越低；执行来源分层相关、5000次bootstrap/permutation、长度调整、LTT/WCC方向一致性及序列簇外CV。其余5项为探索性并做BH-FDR；另比较provider-only、NetSolP U、TNP PSH和U+PSH的普通及簇外CV增量。TNP无论是否与yield相关，仍只能作为developability风险证据，不能称为实测表达或mg/L预测。
- 本地计划制品已生成于`docs/result_artifacts/candidate_design/tnp_yield_validation_plan_20260814/`：47条序列、31条数值记录、16条LLJ有序/删失记录、40个90%序列簇。当前仅发布远程评分，尚无47条TNP关联结论。
- 新增3项专项测试并通过；全套验收为`173 passed, 1 skipped, 4 subtests passed`。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement single-process TNP yield validation`。

## 2026-08-14 23:40:00

### TNP V1失败诊断与V2适用域计划

- V1真实运行顺序尝试47条，实测1416.280578秒，得到38 pass/9 failed并由覆盖门正确停止分析。逐样本日志证明末端`NoneType`只是TNP汇总二次错误：5条实际在ImmuneBuilder strained-sidechain修复分支因`{'Threads', str(n_threads)}`被误写为set而失败；安装文件已备份并修为OpenMM要求的dict，`LTT__Nb294`由失败转为完整TNP smoke成功。
- 其余4条均为WCC且固定为TNP不适用：`WCC__4-1`为不完整H域且被TNP ANARCI拒绝，`WCC__4-28`无法识别抗体域，`WCC__4-11`在项目ANARCII中为L且被TNP拒绝，`WCC__4-42`由NanoBodyBuilder2明确判定缺失过多残基。不得裁剪、补写FR4或以预测序列替代真实输入来制造分数。
- 活动实现已升级为V2并移除V1活动执行路径：新计划的47行样本表保留全部身份、适用状态和原因，FASTA仅含43条适用序列；数值子集为LTT 23+WCC 4共27条，LLJ仍为16条，适用集合含37个90%序列簇。评分前一次核对ImmuneBuilder补丁，43/43必须pass，结果必须声明4条WCC非随机排除偏倚。
- V2远程路线仍为单Slurm作业、单Python进程、无array；使用全新计划/评分/结果/日志目录，不覆盖V1诊断。依据V1实测预计20–30分钟，固定`batch`、1 GPU、12 CPU、1小时上限，不实现断点续传。
- 本地全套验收为`174 passed, 1 skipped, 4 subtests passed`；`pip check`、Python编译检查、Bash语法检查和`git diff --check`均通过。当前只完成V2实现与计划制品，尚未运行远程43条真实评分或生成关联结果图。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement TNP V2 applicability workflow`。

## 2026-08-15 09:55:40

### TNP V2分析写出修复与评分复用

- 用户报告远程V2评分逐项完成，但分析入口在写`tnp_yield_associations.csv`时失败。根因是PSH主指标在重采样和CV后含5个专属字段，而其他5个指标行缺少这些键；`csv.DictWriter`使用首行字段时拒绝异构后续行。该错误发生在临时目录内，未发布部分结果，也不影响既有远程评分文件。
- 分析逻辑现为所有6个指标行固定同一schema：PSH保留实际bootstrap、permutation和CV增益，非主指标对应字段明确留空。专项测试实际执行同一`DictWriter`写出并读回全部指标行，防止该故障再次出现。
- 远程活动路线增加窄范围评分复用：提交入口仅在评分CSV和model-run JSON均存在时允许继续；Slurm作业跳过TNP推理，由分析入口重新验证schema、run状态、样本身份、4条不适用状态和适用覆盖后再生成制品。失败分析可能留下空结果父目录，因此提交入口允许该目录存在，但分析入口仍逐个拒绝覆盖任何正式结果文件；缺少任一评分文件仍停止，不实现一般性断点恢复。
- 修复和复用判断均为秒级，低于1小时和5小时；无需重跑已完成的TNP推理。真实评分结果尚未pull到本地，因此本条不记录关联数值或科学结论。
- TNP专项测试为`4 passed`，全套验收为`174 passed, 1 skipped, 4 subtests passed`；`pip check`、Python编译、Bash语法和`git diff --check`均通过。唯一skip仍为Windows真实symlink权限测试。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Fix TNP V2 analysis output and score reuse`。

## 2026-08-15 10:08:05

### TNP V2真实结果同步与科学判定

- 已同步远程结果提交`1a6e908`；此前V2实现`e0647d9`和分析修复`87b7566`均已在`origin/main`。结果制品包含47行样本证据、6行指标、4行CV比较、机器gate、600 dpi PNG/SVG和run summary；分析阶段实测8.019517秒。覆盖为43/43适用序列pass、4条WCC明确`not_applicable`，程序gate为`pass`。
- 唯一预声明主指标PSH没有获得yield排序证据：27条LTT/WCC数值记录的合并/来源分层Spearman为-0.0959/-0.0856，长度调整partial Spearman为0.0665；LTT/WCC内部为-0.0841/-0.4000，但WCC仅4条。5000次来源分层bootstrap 95% CI为[-0.5377, 0.3820]，来源内置换p=0.6901；16条LLJ有序/删失tau-b为-0.0769。证据等级因此为`compatibility_filter_only`，`nb252_expression_prediction_validated=false`。
- CV必须看绝对相关而非仅看相对负基线的“增量”：TNP PSH单独的普通/leave-cluster-out Spearman为-0.5925/-0.3867；NetSolP U单独为0.0788/0.1332；U+PSH为-0.0299/0.0134，低于U单独模型。因此TNP没有增加可用的样本外yield预测信息，不进入yield排序、回归或硬过滤。
- 其余指标是探索性：总CDR长度、CDR3长度、CDR3 compactness和PNC的合并数值关联经6项BH-FDR后为负，但长度/compactness在LLJ中方向相反，且这些不是预声明主指标，不能解释为因果表达规律或据此设计突变。它们与官方颜色flag只保留为候选developability背景证据。
- Nb252正式单次结果为总CDR长度35、CDR3长度20、compactness 1.281539、PSH 136.6287、PPC 0、PNC 0.3037；PSH为amber且在43条适用序列中第三高，其余5项flag均为green。这提示后续完整候选应避免PSH/flag相对WT进一步恶化，但不证明Nb252低表达，也不提供产量换算。
- 颜色flag不能作为高yield阳性证据：27条数值记录最低四分位7条中2条六项全green、5条含amber/red，只有1条为PSH amber；同时PSH amber也出现在15.0–17.8的较高yield记录，LLJ最低约2 mg记录六项全green。green仅表示该TNP指标未触发风险阈值，候选可优先避免相对WT新增red或明显恶化，但不得按全green筛选高产序列。
- 本地从紧凑样本证据独立复算27条数值/16条LLJ、6项关联、PSH bootstrap/permutation和4种CV，除约`8.3e-17`浮点舍入外与制品逐项一致；PNG人工检查无面板、坐标轴、图例或标签遮挡。未重新运行远程TNP，也未修改结果数据。

### Git sync status

- 远程结果提交`1a6e908`已在本地`main`且与`origin/main`同步。
- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Review TNP V2 yield validation result`。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
