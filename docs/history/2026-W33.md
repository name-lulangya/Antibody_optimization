# 2026-W33 项目历史

## 2026-08-10 09:53:14

### ChimeraX `runscript` 入口真实集成修复

- 用户已在 ChimeraX 1.12 中打开项目目录内的原始 `Nb252-optimization.cxs`，并以截图确认 `NK2R-252.pdb` 的合作者橙色界面注释可见。ChimeraX 命令历史进一步确认当前打开路径为仓库内 CXS，而不是桌面根目录的同哈希副本。
- 连续运行既定 `runscript` 命令时，ChimeraX 只回显并折叠重复命令，`data/structures/cxs_exports/` 及其父目录均未生成；因此没有把该尝试记录为成功导出，结构、映射、界面和候选设计阶段门继续保持 `blocked`。
- 根因是 `export_cxs_session_chimerax.py` 原先只在 `__name__ == "__main__"` 时调用 `main()`，而 ChimeraX 1.12 的 `runscript` 将 Python 脚本置于具有唯一模块名、`__spec__ is None` 且注入 `session` 的 sandbox 中执行。旧入口因而只定义函数后静默结束，没有进入任何输入、模型或写出检查。
- 导出器现通过 sandbox 特征分派入口：ChimeraX `runscript` 环境直接调用 `main(session, sys.argv[1:])`，普通 `importlib` 导入保持无副作用，普通 Python 直接执行仍明确提示必须使用 ChimeraX。没有使用 `SystemExit(0)` 结束 ChimeraX sandbox，也没有放宽源 CXS 路径、哈希、模型身份或输出目录安全检查。
- 外部 CLI 合同、三个必需参数和输出目录合同均未改变。预计和本次修复/测试实测均远低于 1 小时，不需要 Slurm、checkpoint 或服务器环境。
- 新增真实执行语义回归测试：模拟非 `__main__` 的 ChimeraX sandbox 并断言注入的 `session` 传入 `main()`；同时验证普通模块导入不调用 `main()`。完整本地验收为 `93 passed, 1 skipped, 4 subtests passed`，唯一跳过仍是 Windows 真实 symlink 权限测试；`pip check`、Python `compileall` 和 `git diff --check` 均通过。
- 修复后尚待用户在当前正确打开的 ChimeraX 1.12 会话中重新运行原命令。只有生成并验证 `cxs_export_manifest.json`、5 个 mmCIF 和颜色清单后，才能首跑 Gemmi 结构基线并进入链角色/橙色人工审核。

### Git sync status

- 本次入口修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:01:46

### CXS真实模型名合同修正

- 修复 sandbox 入口后，ChimeraX 已真实进入导出器的模型身份检查，并报告三个原子模型实际名称为 `NK2R-252.pdb`、`NK2R-NKA` 和 `fold_2r_252_nomg_model_0.cif`。这证明先前记录的 `NK2R-NKA.pdb` 与原会话实际名称不一致；本次失败发生在任何 staging/output 写出之前，`data/structures/cxs_exports/` 仍不存在。
- 按真实会话证据将导出器和 Gemmi 结构基线的唯一模型名合同同步修正为 `NK2R-NKA`，不要求用户重命名会话模型，也不增加别名、模糊匹配或自动 fallback。对应更新项目级科学上下文、handoff及测试，使导出和下游结构入口继续使用同一精确三模型集合。
- 外部命令、源CXS哈希和输出路径均不变。完整本地验收仍为 `93 passed, 1 skipped, 4 subtests passed`；`pip check`、Python `compileall` 和 `git diff --check` 均通过。当前仍待用户在正确打开的原会话中再次运行相同命令，真实导出成功前所有结构相关阶段门继续为 `blocked`。

### Git sync status

- 本次入口与模型名合同修复、测试和文档尚未提交或推送；等待真实 ChimeraX 导出复测。

## 2026-08-10 10:06:30

### CXS真实导出与Gemmi首轮结构清单

- 用户在正确打开仓库原始CXS的ChimeraX 1.12会话中重跑修复后导出器。`data/structures/cxs_exports/cxs_export_manifest.json` 为 `status=pass`：源CXS SHA-256仍为 `1bc636c28f66ae60edc658d2e1c4aad0b07f4141ca5411c78662aa19da793c4d`且运行前后身份未变；会话含3个目标`AtomicStructure`和2个名为`missing structure`的非原子pseudobond child。
- 导出生成3个native-frame和2个实验参考坐标系mmCIF、`cxs_residue_colors.csv`、manifest及run summary，共8个文件；5个mmCIF均由Gemmi 0.7.5成功读回。ChimeraX提示`Not saving entity_poly_seq for non-authoritative sequences`是`bestGuess false`下不补写非权威序列的保守行为，不是导出失败。
- 三个原子模型计数分别为：实验`NK2R-252.pdb` 2链/396残基/3090 atom sites，`NK2R-NKA` 2链/307残基/2424 atom sites，AF3 VHH 1链/126残基/988 atom sites；全部occupancy为1且无非空altloc。颜色清单共829行，其中唯一精确内置橙色类为`[255,165,0,255]`，在实验模型chain C的24个残基上同时出现在ribbon和atom通道；无自定义颜色或无法回映的surface颜色。
- 在全新目录`docs/result_artifacts/input_baseline/structure_review_20260810/`完成Gemmi结构基线首跑。export和inventory为`pass`；跨工具模型/链/残基/atom object/site计数一致，两个reference-frame文件的全原子刚体残差均低于0.001 Å。按既定合同，首跑因5个链角色、橙色绑定和权威构建体仍待review而返回code 2/`status=blocked`，并生成inventory、residue inventory和`baseline_review_template.json`，未自动填写科学身份。
- 当前需要用户集中确认链角色和唯一橙色类；权威构建体边界可继续保持pending。真实导出与首轮清单是科学阶段进展，但尚未生成Nb252结构映射、FR-only对齐、严格`<4.0 Å`界面或临时保护集合。

### Git sync status

- 真实导出、首轮结构清单、入口/模型名修复、测试和文档尚未提交或推送；等待review确认及后续结构基线重跑。

## 2026-08-10 10:27:21

### 链角色、橙色注释、结构映射与临时界面基线完成

- 用户在 ChimeraX 1.12 中逐项确认：实验模型 `#1/C` 为 Nb252、`#1/R` 为 NK2R；NKA 模型 `#2/L` 为 NKA、`#2/R` 为 NK2R；预测模型 `#3/A` 为 AF3 Nb252；24 个绿色选中残基与合作者橙色区域一致。确认已写入 `structure_review_20260810/baseline_review.json`，绑定精确 RGBA `[255,165,0,255]` 与 `atom+ribbon` 渠道；权威构建体 review 仍保持 `pending_collaborator_confirmation`。
- 首次 confirmed structure run 忠实阻断于实验结构缺失片段造成的多最佳序列比对。检查发现实验 VHH 的 115 个有坐标残基全部具有 1–128 范围内唯一、严格递增的整数 source auth 编号，并逐位匹配 reported Nb252。`polymer_mapping` 1.1.0 因此新增严格直接路线：仅在无插入码、编号唯一递增、范围有效且每一 WT 均一致时使用 source auth 编号；否则仍进入原 exact-first/BLOSUM62 安全门。
- 第二次 run 暴露 AF3 导出保留 536-aa source polymer 元数据，而当前坐标仅含其起始 126-aa VHH；536-aa 序列的前 128 aa 与 reported Nb252 完全一致。映射器现仅在较长 polymer 中存在唯一、完整、逐字一致的 128-aa 连续片段，且全部有坐标残基落在该片段内时接受该片段；额外序列不被称为 VHH，内部冲突、多匹配或坐标越界仍阻断。失败 run 及其 run summary 予以保留作为负结果。
- 最终结构制品位于 `structure_reviewed_20260810/`：实验 VHH 以 source auth exact-WT 映射，AF3 VHH 以 source label ID 加唯一 authoritative segment 映射；residue mapping 为 `pass`。82 个共同 framework Cα、无 outlier rejection 的 Kabsch 拟合 RMSD 为 0.631994 Å；拟合后 CDR3 Cα RMSD 为 6.490853 Å，明显高于 FR aggregate 0.631994 Å。这些是结构计算结果，不是实验功能效应。
- 严格非 H/D、正 occupancy、polymer atom-center `<4.0 Å` 界面得到 246 个原子接触对和 24 个 VHH 残基；这 24 个残基与确认橙色集合完全相同，保护并集仍为 24 个。该集合仅是保守突变保护标志，不是能量热点集合。
- finalizer 已使用真实 mapping/interface 制品重建 128 位点绘图数据、PNG/SVG 和阶段门。期间修复 summary 对缺失坐标位点合法 `not_evaluable` 状态的读取，不再误当非法布尔值；人工查看 PNG 后又将旧的“结构轨道仍为灰色”说明替换为当前适用的灰色单元含义。`local_baseline_build=pass`；`candidate_design_release` 仅因 authoritative Nb252 construct 阻断；`pooled_expression_model_release` 仍因跨 assay pooling 证据阻断。
- 本轮计算实测远低于 1 小时，不需要 Slurm。最终验证：`96 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 已提交并推送至 `origin/main`。
- 结果提交：`1e6bd655ef316a9f5e10ea7331f3eceff302bc45`（`Complete reviewed stage-1 structure baseline`）。

## 2026-08-10 12:08:42

### 合作者确认解除构建体与跨来源可比性阻断

- 项目用户转达合作者明确确认：LTT、WCC、LLJ 不同来源的 reported yield 可直接相互比较；Nb252 末端 `SSGS` 不是 linker，必须保留原始 128-aa 序列。该确认分别写入 machine-readable expression confirmation 和既有统一 `baseline_review.json`，证据来源标为 `collaborator_via_project_user`。
- Nb252 authoritative construct 现固定为 reported sequence 1–128、`construct_scope=mature_vhh`、terminal `GS=included_in_authoritative_construct`。ANARCII 仍只编号前 126 aa；未编号不再解释为构建体外序列。新目录 `structure_released_20260810/` 的 export、inventory、chain role、mapping 和 authoritative sequence 均为 `pass`；重新绑定 review 哈希的 `interface_released_20260810/` 仍复现原 246 个原子接触对和 24 个 VHH 界面残基。
- 表达审核升级为 1.1.0并新增显式 `--comparability-confirmation` 输入。47 条样本的跨来源 pooling 状态现为 `pass`，允许保留原始观测语义的联合探索：31 条 LTT/WCC individual approximate 仍按数值处理，9 条 LLJ group lower bound 与 7 条 group approximate 仍为删失/分组信息，不得插值成个体点估计。协议字段未知不再阻断跨来源比较，但 `nb252_transfer_status` 对 47 条仍为 `blocked`，因为可比性不等于对未见突变的预测准确性验证。
- 用户同时明确优化必须维持实验观察到的表位和结合构象；合作者没有额外人工突变禁区，也没有既往突变效应数据。24 个严格 `<4.0 Å` 界面位点继续作为“突变时谨慎”的可复现注释，而不是绝对禁止或能量热点。
- finalizer 已用 released structure/interface 与更新的 expression audit 重建 canonical summary。当前 `local_baseline_build=pass`、`candidate_design_release=pass`、`pooled_expression_model_release=pass`；独立的 `nb252_expression_transfer` 子门仍为 `blocked`，需后续模型验证而非更多协议询问。
- 本次确认同步、轻量重跑和测试实测远低于 1 小时，不需要 Slurm。最终验证为 `97 passed, 1 skipped, 4 subtests passed`；唯一 skip 仍为 Windows 真实 symlink 权限测试；`pip check`、`compileall`、`git diff --check` 均通过。

### Git sync status

- 本次合作者确认、审核逻辑、released制品、阶段门、测试和文档尚未提交或推送。

## 2026-08-10 15:03:19

### 末端 `SSGS` 设计约束澄清

- 用户明确“保留末端 `SSGS`”的操作含义是候选设计时不得突变或删除该四肽，而不只是把它纳入权威参考序列。该约束现固定为 reported-sequence positions 125–128 的硬冻结集合；由于末端 `GS` 不在 provisional ANARCII/IMGT 编号跨度内，后续必须用 reported-sequence index 表达，不能为其虚构 IMGT 位点。
- 新增机器可读设计约束 `docs/result_artifacts/input_baseline/reviews/nb252_design_constraints.json`，记录母本哈希、不可变位点和证据来源；同步修正 `AGENTS.md` 与 handoff。该澄清不改变既有结构映射、界面结果或三个已通过阶段门，也不释放仍需模型验证的 `nb252_expression_transfer`。
- 复核现有证据后，没有必须继续向合作者确认、否则会阻断第二阶段计算基线的重要信息。最终实验面板容量和是否进入组合突变属于看到单点候选景观后的项目决策，可后置，不需要现在补问无关实验细节。
- 本次仅为设计合同与文档更新，预计和实测均远低于 1 小时，不需要 Slurm。复核确认权威母本长度为128 aa且reported positions 125–128精确为`SSGS`；全套验证为`97 passed, 1 skipped, 4 subtests passed`，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`871a2db8c3f4a4fa3d1bdc7b2df961179c389d43`（`Record collaborator confirmations and release stage gates`）。

## 2026-08-10 15:41:44

### 关键残基事实的机器可读约束与运行前核验规则

- 用户要求实验缺失坐标、界面残基和其他会改变设计的关键事实不得仅依赖对话上下文或代理记忆。既有`AGENTS.md`虽要求记录缺失坐标、界面定义、映射和数值来源，但没有明确要求集中机器可读绑定、每次设计前重读核验以及哈希不一致时拒绝运行，因此本次补充了强制preflight规则。
- 新增`docs/result_artifacts/input_baseline/reviews/nb252_critical_residue_sets.json`，绑定authoritative parent、实验VHH链、reported indices `9-15,24-29`的实验缺失坐标集合、24个严格`<4.0 Å`实验可评估界面位点、不可突变的`SSGS` positions `125-128`，并记录mapping、interface、design constraints和confirmed review的路径与SHA-256。缺失坐标在实验界面语义中明确为`not_evaluable`，不能被预测或补全模型静默改写为实验接触/非接触。
- 规则现要求所有候选生成、突变建模、亲和力/稳定性评分和排序入口重新打开关键制品，核对schema/status、哈希、模型/链、母本序列、编号、coordinate-evaluable和WT残基；缺失、过期、blocked或不一致时必须停止。所有下游manifest须绑定实际使用的关键制品哈希。
- 本次为规则和轻量机器可读事实汇总，预计和实测均远低于1小时，不需要Slurm；未运行结构补全、候选生成或评分。

### Git sync status

- 已提交并推送至`origin/main`。
- 结果提交：`8b768aaec6de837f205523d4d55db4cdb2571189`（`Require critical residue preflight validation`）。

## 2026-08-10 20:24:50

### 阶段2阶段0：候选设计合同与本地preflight完成

- 用户要求把后续完整优化方案写入replace-in-place handoff，并在方案、工具、门或时间估计变化时同步更新；`AGENTS.md`的文档维护规则现显式要求持续维护`Current Optimization Plan`并删除被替代路线。handoff已记录从远程WT结构门、WT序列/可开发性基线、单点景观、PyRosetta筛选、表达/多目标排序到组合与AF3终选复核的完整路线及约1–2周计算时间估计。
- 新增`antibody_optimization.design_contract`与`design_contract_plot`，以及单一阶段0入口`scripts/candidate_design/build_stage2_design_contract.py`。入口重新打开`nb252_critical_residue_sets.json`，核验其绑定的sequence mapping、interface manifest、design constraints、confirmed baseline review的精确SHA-256，并进一步核验`stage1_gate.json`和实验`NK2R-252__native.cif`；缺失、过期、越出仓库、角色/WT/编号/坐标状态不一致均拒绝运行。
- 阶段0从真实实验chain C的SG几何识别到唯一一对1.8–2.3 Å范围内二硫键，映射为reported Cys22/Cys95；与不可替换/删除的reported positions 125–128 `SSGS`合并为6个硬冻结位点。实验缺失坐标仍为13位并保持`not_evaluable`；24个严格`<4.0 Å`实验界面位点为“谨慎但可突变”，也是首轮亲和力放行位点。首轮不要求批量补全缺失结构，缺失位点不进入首轮亲和力扫描。
- 正式Git-tracked输出位于`docs/result_artifacts/candidate_design/stage0_contract_20260810/`：`stage2_design_contract.json`、严格128行且UTF-8 BOM/LF的`mutable_position_inventory.csv`、`stage2_preflight.json`、精确绘图源对应的600 dpi PNG/SVG及`stage2_stage0_manifest.json`；独立run summary位于`docs/run_summaries/candidate_design/stage0_contract_20260810.json`。正式运行耗时1.205213秒，图已人工检查且轨道、轴、图例无遮挡。
- 阶段门结果为`stage0_local_contract=pass`、`candidate_manifest_release=pass`；`pyrosetta_affinity_scoring_release=blocked_pending_remote_gap_safe_import`，直到服务器验证缺失密度jump/cutpoint、PDBInfo可逆映射、有限能量及约束WT pose稳定性。阶段0没有生成突变、没有补全结构，也没有运行PyRosetta、AF3或语言模型。
- 24个界面位点的全氨基酸替换理论上限为`24 × 19 = 456`，因此将旧的500–800个首轮单点估计修正为最多456个原始替换、预计经提议/硬约束和风险过滤后保留约200–400个；实际数目以后续真实候选制品为准。
- 预计与实测均远低于1小时，不需要Slurm或checkpoint。专项测试`3 passed`，覆盖真实合同、陈旧哈希阻断、拒绝覆盖、CSV BOM/LF和固定时间戳双跑六个正式制品逐字节一致；完整验收为`100 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试，`pip check`、`compileall`和`git diff --check`均通过。

### Git sync status

- `ready to commit`：本次阶段0实现、正式制品、测试和文档已完成验证。
- 计划提交信息：`Implement stage-2 local design contract`。
- 目标：`origin/main`。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
