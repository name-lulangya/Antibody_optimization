# 2026-W31 项目历史

## 2026-07-31 16:04:20

### 工作内容

- 启动 NK2R 纳米抗体 Nb252 优化项目的规则与文档基础建设。
- 建立 `docs/codex-handoff.md`，用于 replace-in-place 维护当前状态、风险与近期步骤。
- 建立当前周历史、长期周索引和 `src/README.md`；同时建立 `.gitignore` 与 `.gitattributes`。
- 建立项目级 `AGENTS.md`：沿用通用工程、Git、长任务、结果制品、周审计和文档维护规则，并将旧项目专属内容替换为 NK2R/Nb252 的结构分析、候选设计、多目标优化与实验验证约束。

### 已核验输入与背景

- 本次初始化前，项目目录中唯一的科学/结构输入为 `Nb252-optimization.cxs`；文件大小为 537670 bytes，SHA256 为 `1BC636C28F66AE60EDC658D2E1C4AAD0B07F4141CA5411C78662AA19DA793C4D`。
- 用户说明会话内包含实验 NK2R–Nb252、NK2R–NKA 和 AF3 VHH 三个 model。
- 实验 VHH 存在未搭建区域；与 AF3 VHH 对齐时，除 CDR3 外其余区域总体吻合。
- 三个 `.pdb`/`.cif` 名称目前仅代表 `.cxs` 内 model，并非项目目录中的独立结构文件。

### 文件与规则决策

- 当前没有可复用代码；未来稳定的共享逻辑统一放入 `src/antibody_optimization/`。
- 不忽略 `.cxs`、`.pdb` 或 `.cif` 结构输入；忽略缓存、环境目录及常见的大型生成结果。
- 将 `*.cxs` 标记为 Git 二进制文件，避免无意义的文本差异比较。

### Git sync status

- 已在项目根目录初始化 Git 仓库，初始分支为 `main`。
- 当前仓库尚无提交；本次任务未提交或推送。

## 2026-07-31 16:11:57

### 远程仓库与服务器执行环境

- 将本地 Git 远程 `origin` 配置为 `git@github.com:name-lulangya/Antibody_optimization.git`，fetch 与 push URL 均已在本地配置中核验。
- 用户计划后续将项目检出到远程服务器的 `/homes/Tianlab/luly25/` 目录下；最终仓库根目录以服务器实际检出结果为准。
- 远程服务器的 Conda 环境尚未创建，因此当前不记录或假定环境名称、环境路径及激活命令。
- 服务器登录别名、调度器与计算资源策略仍未提供，不在本次任务中推断。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Initialize NK2R Nb252 optimization project`。
- 目标远程与分支：`origin/main`。
- 提交范围：项目初始化规则与文档、Git 配置文件以及原始 `Nb252-optimization.cxs` 结构会话，共 8 个文件。
- 用户仅要求提交与推送，不创建 PR。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
