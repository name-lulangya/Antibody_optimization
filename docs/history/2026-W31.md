# 2026-W31 项目历史

## 2026-07-31 16:04:20

### 工作内容

- 启动 NK2R 纳米抗体 Nb252 优化项目的规则与文档基础建设。
- 建立 `docs/codex-handoff.md`，用于 replace-in-place 维护当前状态、风险与近期步骤。
- 建立当前周历史、长期周索引和 `src/README.md`；同时建立 `.gitignore` 与 `.gitattributes`。
- 建立项目级 `AGENTS.md`：沿用通用工程、Git、长任务、结果制品、周审计和文档维护规则，并将旧项目专属内容替换为 NK2R/Nb252 的结构分析、候选设计、多目标优化与实验验证约束。

### 已核验输入与背景

- 本次初始化前，项目目录中唯一的科学/结构输入为 `Nb252-optimization.cxs`；文件大小为 537670 bytes，SHA256 为 `1BC636C28F66AE60EDC658D2E1C4AAD0B07F4141CA5411C78662AA19DA793C4D`。
- 用户说明会话内包含实验 NK2R–Nb252、NK2R–NKA 和 AF3 VHH 三个 model。
- 实验 VHH 存在未搭建区域；与 AF3 VHH 对齐时，除 CDR3 外其余区域总体吻合。
- 三个 `.pdb`/`.cif` 名称目前仅代表 `.cxs` 内 model，并非项目目录中的独立结构文件。

### 文件与规则决策

- 当前没有可复用代码；未来稳定的共享逻辑统一放入 `src/antibody_optimization/`。
- 不忽略 `.cxs`、`.pdb` 或 `.cif` 结构输入；忽略缓存、环境目录及常见的大型生成结果。
- 将 `*.cxs` 标记为 Git 二进制文件，避免无意义的文本差异比较。

### Git sync status

- 已在项目根目录初始化 Git 仓库，初始分支为 `main`。
- 当前仓库尚无提交；本次任务未提交或推送。

## 2026-07-31 16:11:57

### 远程仓库与服务器执行环境

- 将本地 Git 远程 `origin` 配置为 `git@github.com:name-lulangya/Antibody_optimization.git`，fetch 与 push URL 均已在本地配置中核验。
- 用户计划后续将项目检出到远程服务器的 `/homes/Tianlab/luly25/` 目录下；最终仓库根目录以服务器实际检出结果为准。
- 远程服务器的 Conda 环境尚未创建，因此当前不记录或假定环境名称、环境路径及激活命令。
- 服务器登录别名、调度器与计算资源策略仍未提供，不在本次任务中推断。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Initialize NK2R Nb252 optimization project`。
- 目标远程与分支：`origin/main`。
- 提交范围：项目初始化规则与文档、Git 配置文件以及原始 `Nb252-optimization.cxs` 结构会话，共 8 个文件。
- 用户仅要求提交与推送，不创建 PR。

## 2026-07-31 16:21:08

### Slurm 配置与参考项目对齐

- 用户确认本项目采用与参考项目一致的 Slurm 资源规则。
- 预计运行超过 1 小时的脚本必须配套 Slurm 提交文件，并应在远程 Linux 服务器上通过 Slurm 运行。
- 默认使用 `#SBATCH --partition=batch`；仅在用户针对具体任务明确要求时使用 `#SBATCH --partition=gpu`。
- 每个作业至少申请 1 个 GPU，并按每 GPU 12 个 CPU 的项目约定配置；默认不显式添加 `#SBATCH --mem`。
- 多 GPU 作业仅使用 `n1` 或 `n2`，不得使用 `n3`。单节点多 GPU 包装脚本申请一个节点并添加 `#SBATCH --exclude=n3`，不得同时通过 `--nodelist` 列出 `n1,n2`。
- 远程 Conda 环境、登录别名和实际检出路径仍待建立或核验。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Align Slurm configuration with reference project`。
- 目标远程与分支：`origin/main`。
- 提交范围：`AGENTS.md`、`docs/codex-handoff.md` 和本周历史中的 Slurm 配置更新。
- 用户仅要求提交与推送，不创建 PR。

## 2026-07-31 16:35:17

### 远程 PyRosetta 环境

- 以下部署信息由用户提供，尚未通过远程服务器会话独立检查。
- Conda 环境：`/data/software/env/luly25/multi_ligand`。
- Python：3.10.20。
- PyRosetta：2026.03。
- Rosetta commit：`5e498f1409c68ade56c8ce5842bf79e1b02e8db4`。
- 激活与版本检查命令：

```bash
source /data/software/anaconda3/etc/profile.d/conda.sh
conda activate /data/software/env/luly25/multi_ligand

python -c "import pyrosetta; print(pyrosetta.version())"
```

### 远程 nanoBERT 环境

- Conda 环境：`/data/software/env/luly25/vhh-lm`。
- 模型：`NaturalAntibody/nanoBERT`。
- 固定模型 revision：`edc8182ad89a827f8737fa572c6b5fac6197e6b0`。
- 本地模型快照：
  `/homes/Tianlab/luly25/software/huggingface_cache/vhh-lm/hub/models--NaturalAntibody--nanoBERT/snapshots/edc8182ad89a827f8737fa572c6b5fac6197e6b0`。
- 环境激活命令：

```bash
source /data/software/anaconda3/etc/profile.d/conda.sh
conda activate /data/software/env/luly25/vhh-lm
```

- 建议使用以下离线模式设置，确保固定 revision 从本地缓存加载：

```bash
export HF_HOME=/homes/Tianlab/luly25/software/huggingface_cache/vhh-lm
export TRANSFORMERS_OFFLINE=1
export HF_HUB_OFFLINE=1
```

### `NK2R-252` 橙色 VHH 界面注释

- 在 `Nb252-optimization.cxs` 的 `NK2R-252` model 中，合作者将处于推定结合界面的 VHH 残基标记为橙色。
- 合作者说明这些残基采用“距离小于 4 Å”的条件，但尚未说明距离是在任意原子、重原子、主链原子、Cα 或其他对象之间计算，也未提供 ChimeraX 选择命令。
- 当前尚未导出橙色残基的确切链 ID、残基编号和序列映射，因此该颜色集合属于待复核的合作者注释，而不是已经脚本化验证的界面定义。
- 后续优化该区域时需谨慎。生成突变前应先导出残基清单、核验编号映射，并以明确记录的原子选择和距离规则重新计算界面接触；不得仅根据颜色批量突变。

### 文档同步

- 更新 `AGENTS.md`，移除“远程 Conda 环境尚未创建”的过时陈述，并加入环境固定版本与橙色界面残基约束。
- 在 `docs/codex-handoff.md` 中保留环境与界面风险的简要当前状态，完整命令和来源信息保留在本条历史中。

### Git sync status

- 本次环境与界面注释文档更新尚未提交或推送。

## 2026-07-31 16:48:33

### 项目环境与工具环境角色澄清

- 用户澄清：本项目自身的远程 Conda 环境为 `/data/software/env/luly25/ab_optim`。
- `/data/software/env/luly25/multi_ligand` 仅为 PyRosetta 工具环境。
- `/data/software/env/luly25/vhh-lm` 仅为 nanoBERT 工具环境。
- 上一条历史中记录的 PyRosetta 与 nanoBERT 激活命令只用于调用对应工具，不能视为项目主环境的激活方式，也不能假定两个工具的依赖已安装在 `ab_optim` 中。
- `ab_optim` 的 Python 版本、完整依赖和远程检查结果尚未记录，不在本次澄清中推断。

### 文档同步

- 更新 `AGENTS.md` 与 `docs/codex-handoff.md`，明确项目主环境和两个独立工具环境的职责边界。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Document remote environments and interface annotation`。
- 目标远程与分支：`origin/main`。
- 提交范围：`AGENTS.md`、`docs/codex-handoff.md` 和本周历史中的项目/工具环境及橙色界面注释更新。
- 用户仅要求提交与推送，不创建 PR。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
