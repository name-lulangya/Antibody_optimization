# 2026-W35 项目历史

## 2026-08-25 12:00:00

### 表达导向V3三指标单突终选

- 现行候选路线恢复为30条单突，不再使用历史19+11面板、双突结果或F30/Q1/T27固定配额。旧制品不改写，只保留为计算provenance；现有报告和PPT尚未同步V3，不能作为当前候选来源。
- 复用已通过重复一致性门的847条完整性质/稳定词矩阵和全空间AntiFold证据，不重新运行NetSolP、NanoMelt或AntiFold。NetSolP U、NetSolP S和NanoMelt预测Tm分别计为三个正向指标；沿用既定幅度档，档内原始小数不参与排序。稳定词只作最后平局条件，本次没有候选依靠稳定词单独入选。
- AntiFold改为纯负向联合否决：`ΔlogP<=-3`且突变残基在同位置20种氨基酸状态中处于最差4名时排除，改善不提供正向选择权重。位置排名使用WT与19种替换，包括禁止进入候选但用于完整排名的新Cys状态；151/847条被否决，其中实验复合物来源130条、AF3-only补充来源21条。
- 硬序列风险、AntiFold联合门及U/S/Tm幅度资格门后得到61条合格单突：A/B/C分别8/11/42条，无D层。最终按位置第1、2、3轮优先覆盖，每位置最多3条，轮内依次使用A>B>C>D、明显改善数、改善指标数、较少中等恶化、较少软风险、稳定词平局条件和候选ID；得到30条单突及31条替补。
- 最终30条由A/B/C层6/7/17条组成，覆盖23个reported位置，实际每位置最多2条；均为唯一128-aa单突、保留末端SSGS和原有Cys、不新增Cys、不触及冻结空间，无AntiFold联合否决、无硬序列风险且至少一个U/S/Tm指标中等或明显改善。机器合同、逐条审计、CSV/FASTA、gate和图位于`expression_single_mutant_selection_v3_20260825`制品目录。
- 该本地流程预计并实测均低于1小时，不需要Slurm、checkpoint或resume。新旧选择模块专项测试为6 passed；全套验证为282 passed、1 skipped、4 subtests passed，用时150秒。`compileall`、`pip check`、`git diff --check`及最终CSV/FASTA/gate回读均通过。选择图已人工检查，漏斗、位置覆盖和三独立指标热图均可辨识。

### Git sync status

- 状态：已提交并推送。
- 提交：`e442c33 Select V3 expression single-mutant panel`。
- 目标：`origin/main`。

## 2026-08-25 12:01:00

### 最终面板V3合同更新

- 最终面板合同现行版本定为V3，组成改为15条单突加15条双突；双突只能从最终选定的15条父单突中产生，同位点替换互斥，不生成三突或更高阶突变。
- `expression_single_mutant_selection_v3_20260825`释放的30条单突改作V3只读上游短名单，不直接等同于最终交付面板。当前只完成合同更新，尚未选择15条父单突、枚举有效双突或选择15条双突。
- V1旧30单突试选路线与V2的19单突加11双突路线均明确为只读历史版本；不修改其代码和结果，也不使用其候选、固定配额、双突分数或排名生成V3面板。
- 本次只修改`AGENTS.md`、当前handoff和周历史，不运行候选选择或预测工具；预计并实测均低于1小时，无需Slurm、checkpoint或resume。

### Git sync status

- 状态：已提交并推送。
- 提交：`8b466fd Update V3 final panel contract`。
- 目标：`origin/main`。

## 2026-08-25 12:30:00

### V3上游30条单突的结构与VHH专家审查

- 本阶段只审查`expression_single_mutant_selection_v3_20260825`的30条上游单突，不选择、淘汰或重排父单突。审查逐条原样保留既有NetSolP U/S、NanoMelt预测Tm、AntiFold、稳定词与序列风险证据，并将`parent_single_selection_status`固定为`not_performed`。
- 结构主证据采用已释放的实验NK2R–Nb252复合物C链；26条候选具有实验坐标。L11Y、L11M、I28Y和F29Q因实验坐标缺失，仅以独立AF3 VHH单体作为低置信度建模上下文，未转写为实验观察。A23/F30缺口边界与A96/T99 CDR3另生成6张AF3敏感性图。
- 结构量化使用Biopython Shrake–Rupley 200点和Tien最大ASA得到描述性相对SASA，另记录phi/psi启发式、4.5埃VHH邻居、二硫键硫原子距离、受体距离、冻结界面残基壳层距离及FR对齐后的实验–AF3 Cα位移。30条均不直接突变冻结界面；7条候选位于冻结界面残基4.0埃壳层内，R71G位于4.0–4.5埃边界壳层，这些只作为结构谨慎标记。
- ChimeraX 1.12为每条候选从新打开的来源结构独立执行Dunbrack `swapaa`并生成一个固定主链侧链rotamer图；同位点替代不继承前一模型。共生成61张图：30张主候选、6张AF3敏感性、23张WT位点近景和2张来源总览。全部图已人工检查；图片不保存突变结构、不执行主链松弛、不输出定量clash或能量。
- 每条候选已写入结构事实、ChimeraX逐图观察、VHH专家机制推断、预计溶解度方向、预计热稳定性方向、置信度、主要风险与不确定性。汇总仅为描述：结构判断7条`reasonable`、13条`reasonable_with_caution`、9条`structurally_concerning`、1条`indeterminate`；这些是后续选择前的专家假设，不是实验结果或自动否决。
- 主要机器制品为30行UTF-8-BOM/LF评价CSV、结构视图manifest和阶段manifest。渲染入口为837行，但输入合同、ChimeraX独立模型生命周期、视图编排和manifest共同构成一个不可分的单一渲染工作流；保持同一文件可避免普通Python计划与ChimeraX运行语义漂移，因此未机械拆分。
- 预计和实测均低于1小时；本地ChimeraX渲染约数分钟，表格构建约2秒，不需要Slurm、checkpoint或resume。专项测试8 passed；全套测试290 passed、1 skipped、4 subtests passed，用时148秒。`compileall`、`pip check`、`git diff --check`、30行CSV、61行视图manifest及全部非空PNG回读均通过。

### Git sync status

- 状态：已提交并推送。
- 提交：`f5e0658 Add V3 parent-single expert review`。
- 目标：`origin/main`。

## 2026-08-25 14:10:00

### 将稳定词探索突变T99F纳入V3父单突专家审查池

- 原始`expression_single_mutant_selection_v3_20260825` 30条上游短名单保持只读且未改写；按用户决定从847条V3审计中显式追加`T99F`，形成31条、23个位点的专家审查/父单突选择池。`T99F`保留原始`not_eligible`/`not_selected`事实，稳定词新增仅作为待实验检验的软假设，不将其改写为通过U/S/Tm资格门的候选。
- 同一评价表已扩展为31行：27条使用实验复合物主证据、4条实验缺失位置使用AF3-only低置信度证据。ChimeraX制品同步为63张图，包括31张主候选、7张AF3敏感性、23张WT位点近景和2张来源总览；原30条既有PNG保持不变，只新增`T99F`的实验与AF3视图。
- `T99F`位于CDR3且实验坐标可评价，不是冻结界面残基，但处于冻结界面残基近邻壳层。固定骨架单rotamer审查提示芳香侧链体积和疏水性增加、WT极性网络丢失及局部过度包装风险；专家结论为`structurally_concerning`，溶解度和热稳定性方向均为`unfavorable`，置信度`medium`。该结论只用于后续选择前审查，`parent_single_selection_status`仍为`not_performed`。
- 本地实现和渲染均预计且实测低于1小时，无需Slurm、checkpoint或resume。审查池身份合并已提取到仅用标准库的`v3_expert_review_pool`，供普通Python与ChimeraX共同复用；超过800行的渲染入口仍只承担一个不可分的ChimeraX模型生命周期、绘图和manifest工作流，未为行数机械拆分。专项测试为11 passed；全套验证为293 passed、1 skipped、4 subtests passed，用时147秒。`compileall`、`pip check`、`git diff --check`、31行评价CSV、63行视图manifest及全部非空PNG回读均通过；原30行V3短名单和61张既有审查PNG保持不变。

### Git sync status

- 状态：已提交并推送。
- 提交：`d732d69 Add T99F to V3 expert review`。
- 目标：`origin/main`。

## 2026-08-25 15:45:44

### V3父单突15条选择与31条决策审计

- 用户确认的15条父单突为`L11Y、F30S、K86S、A23R、Q5V、S55G、K75A、F29Q、K43A、N76G、F30N、K75E、L11M、Q1D、T99F`。展示顺序沿用审查池顺序，不解释为1–15的效力排名。
- 选择优先保留U/S/Tm幅度档中的强改善：11条强改善候选中9条入选，`A49F`和`A49M`因实验结构支持的高置信核心过度包装风险排除；另有5条高置信明确物理风险硬排，分别为`A49F、A49M、S50F、R71G、A96R`。其余11条未入选均记录为同位点竞争或综合证据较弱，未误写成专家硬否决。
- `T99F`是唯一用户指定的稳定词`gain_only`探索例外。其上游`not_eligible`/`not_selected`、U无改善、S/Tm弱下降和中等置信结构风险均原样保留，不将其改写为预测器合格候选。
- 新阶段制品包含31行详细选择/淘汰审计、15行父单突CSV、15条完整128-aa FASTA、紧凑绘图表、600 dpi PNG/SVG、manifest及轻量run summary。15条覆盖12个位点，L11、F30和K75各有两个替换；105个理论无序对扣除3个同位点非法对，未来有效双突空间为102条，本阶段没有枚举或评分双突。选择总览图的弱不利/中性/弱有利统一显示为中性，避免微小小数驱动视觉；图片已人工检查。后续V3报告须从该决策审计读取理由，不从对话或旧V1/V2报告手抄。
- 该本地流程预计并实测均低于1分钟，不需要Slurm、checkpoint或resume。选择模块专项测试为7 passed；与专家审查联跑为18 passed；全套验证为300 passed、1 skipped、4 subtests passed，用时158秒。`compileall`、`pip check`、`git diff --check`及最终CSV/FASTA/manifest/run summary回读均通过。

### Git sync status

- 状态：已提交并推送。
- 提交：`c69f310 Select V3 parent single panel`。
- 目标：`origin/main`。

## 2026-08-25 18:30:00

### V3双突完整空间、共同评分输入与远程执行合同

- 以只读`v3_parent_single_selection_20260825` 15条父单突为唯一来源，枚举105个理论无序对并显式排除`L11Y+L11M`、`F30S+F30N`、`K75A+K75E`三个同位点互斥对，释放102条唯一完整双突和WT加102条共同评分CSV/FASTA。扫描前未使用U/S/Tm删减候选，也未引入父集外替换或三突。
- 102条候选均重新核对完整128-aa序列、恰好两个不同reported位置替换、末端SSGS、原有两枚Cys和硬序列风险；硬风险为0。1,336条简并稳定词在每条完整双突上重新匹配，15条出现净新增、无减少项，其中14条含`T99F`。AntiFold仅保留两个组成单突各自已通过的来源、ΔlogP、位置内排名和否决状态，不相加为双突分数。
- WT位点结构初筛独立生成102行：64条位置对使用实验复合物VHH坐标，38条因至少一个实验缺失位置仅报告AF3 VHH预测距离；53条按距离、模型敏感性或已有单点关注标为后续详细结构审查触发项。该表只描述WT位点几何，不推断双突后的侧链重排、稳定性或表达量。
- 新增现行V3专用计划、评分和联接入口及单作业Slurm路线。远程将对同一103条ID/完整序列顺序运行NetSolP Distilled SU和NanoMelt，要求两工具103/103覆盖；联接阶段只生成102行相对WT性质矩阵、冻结幅度档和预测器非加和诊断，不在评分阶段选择最终15条，也不运行Rosetta、TNP、AF3批量预测或新AntiFold推理。
- 本地计划构建实测3.37秒并释放机器manifest、逐候选CSV、结构初筛、稳定词变化表、共同评分CSV/FASTA和PNG/SVG；计划图已人工检查。专项测试为9 passed；全套验证为309 passed、1 skipped、4 subtests passed，用时276秒。远程预计10–30分钟且低于2小时；固定为一个非数组`batch`作业、1 GPU、12 CPU、2小时上限，无checkpoint或resume，日志写入`logs/v3_double_mutant_scan/`。远程尚未启动，运行命令为`bash scripts/candidate_design/run_v3_double_mutant_scan.sh`。

### Git sync status

- 状态：已提交并推送。
- 提交：`9134b63 Implement V3 double-mutant scan`。
- 目标：`origin/main`。

## 2026-08-25 19:03:05

### V3双突远程结果同步、完整性审计与性质矩阵分析

- 本地`main`已同步到远程结果提交`aac1bf8c34dd0452d9e937e86bdcfa0b834462e5`。正式矩阵包含102条唯一128-aa双突，身份和序列与冻结计划逐行一致；NetSolP和NanoMelt均对同一WT加102条输入实现103/103覆盖，NanoMelt全部评分前126 aa并仅裁去末端`GS`。AntiFold仍只保留两个组成单突各自的已通过证据，没有相加或伪造双突得分；最终15条选择仍为`not_performed`。
- 幅度档审计显示91/102条至少一个U/S/Tm指标达到中等或强改善，其中8条三指标均达到中等或强改善；84条有性质支持且无中/强不利，6条带中度不利权衡，1条带强不利，11条没有中/强正向。唯一强不利组合为`K75A+K43A`的NetSolP U下降，同时NanoMelt预测Tm为强改善；因此不能只按单个稳定性指标选择。
- 完整双突预测相对两个组成单突的加和预期普遍呈收益递减：U、S和预测Tm的非加和残差分别有89、71和69条为负。该量只描述预测器输出，不解释为物理上位性，也不替代完整双突实算。15条稳定词净新增组合中14条含`T99F`，但没有任何一条达到至少两个中/强性质改善，继续只作为探索证据。
- 同步审计发现一条责任基序表示勘误：`N76G+F30N`在reported 76删除旧`NT`的同时在reported 30新增`NG`，所以脱酰胺基序净计数为0但仍有新增风险。位置级勘误已写入`v3_double_mutant_post_sync_review_20260825`机器制品；共享责任基序函数改为按发生位置判断`new_*`，避免新增项被别处删除抵消。现有预测数值不变。
- 原总览图数值无误，但Panel D长分类标签重叠，不适合直接放入报告；绘图模块已改为固定短标签，并生成、人工检查了数值不变的可读版PNG/SVG。远程`results/`与`logs/`受Git忽略，因此单纯pull不会带回4个原始工具文件或Slurm日志；本地已核验的是由其SHA绑定的正式矩阵、manifest和run summary。后续若要求独立重放原始103行环境记录，应从远端另行复制原始目录。
- 本地同步、分析和代码修正预计并实测低于1小时，不需要Slurm、checkpoint或resume。V3双突专项测试为9 passed；全套验证为309 passed、1 skipped、4 subtests passed，用时158秒。`compileall`、`pip check`和`git diff --check`均通过。

### Git sync status

- 状态：已提交并推送。
- 提交：`b174a59 Analyze V3 double-mutant results`。
- 目标：`origin/main`。

## 2026-08-25 19:41:16

### V3双突统一专家审查、最终15条选择与15+15面板释放

- 本阶段只读取已释放的102行双突性质矩阵、15条权威父单突及其31行审计和post-sync勘误，不重新运行NetSolP、NanoMelt或AntiFold，也不调用V1/V2终选逻辑。全部102条采用相同的性质、组成AntiFold、序列风险和结构/VHH规则；42条多指标支持、7条中/强不利和12条非空间分离组合三类通用触发取并集后形成58条`enhanced`，其余44条为`standard`，二者只控制解释详略，不决定资格或排序。
- 14条含`T99F`的双突按同一公式自然分为2条`enhanced`和12条`standard`，未设置突变专属触发、配额、奖励、否决或入选例外。`N76G+F30N`在reported 30新增`NG`脱酰胺基序的勘误作为overlay进入终选审计，正式远程矩阵保持只读。双突专家综合明确限于WT位点几何、父单突既有专家意见、完整双突序列风险和U/S/Tm预测档，未声称执行双突侧链建模或获得实验结果。
- 显式终选15条双突中6条有三个中/强正向指标、9条有两个，均无中/强不利；覆盖13/15个父单突和10/12个reported位置，每个精确父单突最多3次、每个位点最多4次且每个无序位置对最多1条。入选项包含4条AF3-only距离来源、2条软序列风险和0条局部邻近组合，满足冻结的组合多样性上限。
- `v3_final_15plus15_panel_20260825`释放102行完整选择/未选审计、15条双突、最终15单突加15双突CSV/FASTA、精确绘图数据、PNG/SVG及单一权威manifest；展示顺序不解释为效力排名。流程为本地CPU、实测2.11秒，无需Slurm、checkpoint或resume；专项测试9项、全套318项及4项subtests全部通过（1项按既定条件跳过），覆盖58/44、T99F平等处理、勘误overlay、冻结15条、多样性、15+15和CLI原子输出/拒绝隐式覆盖。`compileall`、`pip check`、`git diff --check`、输出SHA回读和PNG人工检查均通过。

### Git sync status

- 状态：已提交并推送。
- 提交：`7ec7607 Release V3 final 15+15 panel`。
- 目标：`origin/main`。

## 2026-08-25 20:17:02

### V3计算链与报告就绪性完整审计

- 系统检查`862394d78d229618d13048229457f9be1ed2f759`至`e44c2ae87f215510ae15729c4dac6553656fc645`的V3代码、序列/编号/结构映射、结果、manifest、图表、文档与交付准备状态。正式审计已标记Complete并写入`docs/result_artifacts/weekly_report_result/Nb252_V3_expression_report/Nb252_V3_audit_report.md`，机器证据JSON由只读入口`scripts/reporting/audit_v3_release.py`从实体制品重新联接生成。
- 机器审计18/18通过：847→30→31→15→102→15→最终30的ID、完整序列和数量闭合；15个父单突理论105对扣除3个同位点互斥对后精确得到102条；最终CSV/FASTA逐条一致，全部保留128 aa、末端SSGS、Cys22/Cys95，且无冻结或24位直接界面突变。47个本地可用manifest哈希绑定全部匹配，未发现序列错配、突变重建错误、候选遗漏或哈希漂移。
- 审计结论为`pass_with_material_caveats`，未发现Critical问题。现有DOCX/PDF/PPT/delivery和生成器属于历史V2的19单突＋11双突/162条路线，这是预期的版本沿革而非V3缺陷；它们保持原样。新的V3项目报告、审计报告、PPT和交付材料统一放入`Nb252_V3_expression_report`目录。上游30单突gate仍自称最终面板，后续V3报告只能将其称为只读短名单并只使用V3 final manifest作为当前候选权威。
- 固定披露项包括：T99F是上游不合格且性质弱不利的稳定词探索例外；A23R/T99F为中置信结构担忧；7/30构建体依赖实验缺失位点的AF3补充证据；6/30属于F30缺口边界家族；4/30有NG脱酰胺或Met氧化软责任；15条双突未做双侧链建模或双突AntiFold；冻结直接界面不等于已证明结合保持。四个远程双突normalized score/model-run原始文件仍未随Git归档，当前只能由SHA绑定的102行正式矩阵追溯。
- 人工图形检查覆盖上游V3单突、父单突、可读双突性质矩阵和最终双突总览。图源数值可用，但上游单突图的`Final 30`/“三个独立指标”与当前V3语义不再适合直接引用，终选报告也不得把旧53/49机器triage写成最终58/44复核深度；高纵横比总览应在Word/PPT中拆分面板，避免缩小后标签不可读。
- 审计入口预计和实测均低于1分钟，不需要Slurm、checkpoint或resume。脚本为865行，但保持为单一、只读、一次性报告就绪性审计工作流：集中展示18个阶段边界检查及一份证据JSON，不承担绘图、结果修改或预测执行；当前没有可复用的第二调用方，故本轮不为行数机械拆分。后续若该审计被复用于其他版本，再按稳定输入/输出拆出共享模块。
- 全套验证为320 passed、1 skipped、4 subtests passed，用时161.45秒；目录与报告语义调整后的专项测试为2 passed；`compileall`、审计脚本`py_compile`、`pip check`和`git diff --check`均通过。证据JSON以相同提交、时间和输入重复生成时SHA-256均为`020177B5BF1F9B661A13ED4E44685B5E1CA2042B52D82E48FE0B1354CC09DC0C`。

### Git sync status

- 状态：已提交；主提交为`78130c2 Audit V3 report readiness`。
- 本状态更新将随同步记录提交一并推送。
- 目标：`origin/main`。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
