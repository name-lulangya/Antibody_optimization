# 2026-W32 项目历史

## 2026-08-03 12:05:40

### 合作者表达量数据提取

- 新增根目录输入 `nb序列及产量（1L）.docx`；文件大小 14172 bytes，SHA-256 为 `a6e4022f0978fbd70a0e04dc78f479140ab6f55caaa90b467fb77a62eb5db5d1`，本次未覆盖或改写源文件。
- 直接读取 DOCX 中的 `word/document.xml`：正文共 116 个段落、100 个非空正文段落、0 个 Word 表格。数据按段落排列，不能按表格 API 直接读取。
- 完整提取 47 条记录：LTT 23、WCC 8、LLJ 16。LTT 在 `Nb252` 后仍包含 `Q1`、`P26`、`Q17`、`P27` 和 6 条 `S` 开头记录，均已保留，未按 `Nb*` 前缀误删。
- 47 个 `sample_uid` 唯一、47 条序列无完全重复，全部只含 20 种标准大写氨基酸字母，字面长度范围为 105–142 aa。序列从源段落自动读取，未手工转录、裁剪、改大小写或修复；每条均记录长度和 SHA-256。
- `LTT__Nb252` 位于源顺序第 13 条，字面长度 128 aa，序列 SHA-256 为 `df5b83ddde8a3486383c12afe45e22af6a358f507eab5503d5dbd4430710288d`，文档报告的个体近似产量为 `~0.5 mg`。

### 数据语义与输出合同

- 新增 `src/antibody_optimization/nb_expression.py`，使用 section 状态机区分 LTT、WCC 和 LLJ；WCC 的 `>4-7` 中 `>` 只作为源标题标记，LLJ 的 `>20 mg` 才表示数值下界。为控制职责与文件长度，CSV/FASTA/manifest/QC 写出和全字段回读校验拆分到 `nb_expression_artifacts.py`。
- 源段落同时保留不删除空白的 `raw_text` 和只供语法识别的 `parse_text`。源标题段落 40、42 含尾随空白并已原样保留；47 个序列段落均满足 `raw_text == parse_text`。若表格、文本框、tab、换行节点、修订、隐藏文字或序列首尾空白出现，解析器会直接失败而非静默遗漏或清理。
- 新增入口 `scripts/data_preparation/prepare_nb_expression_dataset.py`。主要输入为源 DOCX、预期 SHA-256、显式的原文档标题培养体积元数据、Git 跟踪的输出目录和 run-summary 路径；培养体积不再依赖可变文件名。
- 新增 `src/antibody_optimization/file_transaction.py`：在写出前拒绝 source/target、target/target 的精确和祖先路径碰撞，拒绝目录或符号链接目标；三个 CLI 均先保留用户路径的绝对词法终点供符号链接检查，再只使用校验器返回的规范路径。`--overwrite` 使用全套备份与 rollback 事务，避免中途失败留下新旧文件混合。run summary 与 10 个数据产物纳入同一事务。
- 主要输出位于 `docs/result_artifacts/nb_expression/`：`samples.csv`、`yield_observations.csv`、`assay_context.csv`、`nb_expression_records.csv`、`raw_transcription.csv`、`nb_expression_sequences.fasta`、`manifest.json`、`validation_report.json`、`qc_plot_data.csv`、`nb_expression_qc.svg` 和 `data_dictionary.md`。这些均为小型 Git 跟踪制品，不写入 ignored 结果目录。
- `.gitattributes` 将 DOCX 标记为二进制，并固定 CSV、FASTA、JSON、SVG 和 Python/Markdown 的 LF 行尾，避免 Git 换行转换影响序列制品或哈希复核。
- LTT/WCC 的 31 条记录保存为 `individual_approximate`，数值仅进入 `point_estimate_mg`。LLJ 的 9 条 `>20 mg` 保存为组级下界，数值只进入 `lower_bound_mg=20`；6 条 `~10 mg` 和 1 条 `~2 mg` 只进入 `group_anchor_mg`。LLJ 的 `point_estimate_mg` 全部留空，不虚构个体精确值或分档边界。
- 表型命名为 `reported_yield`，不自动生成 `expression_mg_per_l`。WCC 明确记录 1 L TB 和纯化后近似产量；LTT/LLJ 的 1 L 仅来自原文档标题，并通过 `--document-title-culture-volume-l 1` 显式传入。宿主、诱导、批次、构建体边界、纯化回收、重复和误差尚未记录。
- `sample_uid` 使用如 `LLJ__1E2`、`WCC__4-7` 的来源前缀，防止跨文件冲突和常见 Excel 自动类型转换；原始 `source_sample_id` 仍需按文本导入。
- FASTA 使用 `sample_uid` 和全长序列 SHA-256 作为 header；表型语义以 CSV/manifest 为准，不依赖 FASTA header。

### 校验、运行环境与复现

- 主命令：

```powershell
python scripts\data_preparation\prepare_nb_expression_dataset.py --source "nb序列及产量（1L）.docx" --output-dir docs\result_artifacts\nb_expression --run-summary docs\run_summaries\nb_expression\prepare_20260803.json --expected-source-sha256 a6e4022f0978fbd70a0e04dc78f479140ab6f55caaa90b467fb77a62eb5db5d1 --document-title-culture-volume-l 1 --generated-at 2026-08-03T12:01:38+08:00 --overwrite
```

- 本地运行环境为 Windows 11、Python 3.13.5；未使用模型、数据库、抗体编号、CDR 定义或结构比对。数据准备提交事务前耗时约 0.1 s，预计和实测均小于 1 小时，不需要 Slurm，也不需要 >5 小时任务的恢复点。
- run summary 保存 `python_executable`、脚本绝对路径、结构化 `argv`、工作目录和可直接重放的 PowerShell 命令，不再使用缺解释器且不适配 Windows 的 POSIX shell 字符串。即使调用者省略 `--generated-at`，实际采用的时间也会写回记录的 `argv` 和重放命令，以固定所有含时间戳的数据制品。
- `tests/` 当前共 25 个测试：24 个通过，1 个真实符号链接测试因 Windows 当前账户无创建 symlink 权限而跳过。测试覆盖真实 DOCX、全部 clone 顺序、逐序列 raw 段落与哈希、LLJ 字段分离、源文件改名时显式体积元数据不变、所有 CSV/FASTA 回读、QC 图、路径碰撞、目录目标拒绝、注入式中途失败完整 rollback、固定时间双跑字节一致、隐式时间戳的实际重放，以及三个 CLI 的危险路径拒绝。新增的五个 CLI 符号链接场景覆盖 prepare 的 source、output directory、run summary，plot 的输出和 verifier 的报告；测试在进程内模拟词法链接终点和规范目标，不依赖 Windows 创建符号链接权限。
- 独立验证脚本 `scripts/data_preparation/verify_nb_expression_outputs.py` 刻意不导入生产解析器或写出器，以第二套 stdlib 状态机比较源 XML、主要 CSV、原文转录和 FASTA。报告位于 `docs/run_summaries/nb_expression/independent_validation_20260803.json`：47 条逐序列、长度、全 SHA-256、顺序、产量与实验上下文字段 mismatch 均为 0；`word/document.xml` SHA-256 为 `c781f09dd38a407ff4868032dd360e2514e8dc2f627b05b6e1bdc59744e16c61`。
- 另由独立只读复核逐条检查源 DOCX 与输出，结果同样为 47/47 一致，特别确认了 Nb252 后的 Q/P/S 克隆、WCC 标题 `>` 和 LLJ 三类数值字段。
- QC SVG 的 XML 结构与源数据由测试核验；当前会话的图片查看器不支持 SVG，且无可用的应用内浏览器，因此未完成像素级视觉渲染检查。图中仅展示来源计数和产量语义计数，不作性能比较。
- 电子表格技能要求的 `load_workspace_dependencies` 与 `@oai/artifact-tool` 运行时未在当前会话提供；按工具约束未使用 `openpyxl`、`xlsxwriter` 或其他替代库，故本次未生成 XLSX。主数据 CSV 为 UTF-8 with BOM，可直接导入 Excel；仅供绘图脚本读取的 `qc_plot_data.csv` 为无 BOM UTF-8。后续在合规运行时可补充带文本 ID 列和数据字典工作表的 XLSX。

### 序列与建模注意事项

- 6 条字面序列短于 115 aa；`Nb257` 与 WCC `4-11` 各仅含一个字面 Cys；9 条不含字面字符串 `WGQGT`。这些仅写入 `sequence_review_flags` 供人工复核，未自动判错、修复或删除。
- WCC 序列存在来源特异的 N/C 端特征；所有记录的 `sequence_scope` 当前为 `unknown`，`vhh_region_sequence` 留空。在构建体核验和抗体编号前，不应把全部字面字符串直接视作统一裁剪的成熟 VHH。
- 即使用户说明数据来自同一体系，源文档仍缺少跨 LTT/WCC/LLJ 的完整实验协议、共享对照和重复信息。后续建模需核实可比性、按来源/批次分层，并防止来源或末端序列泄漏。

### Git sync status

- 本次实现、源 DOCX 和派生制品尚未提交或推送。

## 2026-08-03 15:09:46

### Windows 结果文件访问权限修复

- 用户报告 `docs/result_artifacts/nb_expression/` 中的 CSV 同时无法由 VSCode 和 Excel 打开。复核确认这不是 CSV 内容损坏：6 个 CSV 均可被 Python 严格 CSV 解析器和 PowerShell `Import-Csv` 读取，字段数一致，无 NUL、异常控制字符、加密、只读属性或文件锁。
- 根因是 Windows DACL。原先由准备脚本事务生成的 10 个结果制品和 `prepare_20260803.json` 均为 `AreAccessRulesProtected=True`，只允许 `CodexSandboxOffline`、SYSTEM 和 Administrators；交互账号 `Tian_lab_luly25\\16217` 没有访问项。直接在目标目录生成的 `data_dictionary.md` 和独立验证报告权限正常。
- `tempfile.TemporaryDirectory` 使用私有临时目录；Windows 上从其中通过 `os.replace` 移动文件会保留私有 DACL，而不会重新继承最终目录 ACL。问题影响所有使用 `replace_staged_files` 从私有 staging 安装的结果，也包括 QC 图的独立重绘路径。
- 修改 `src/antibody_optimization/file_transaction.py`：在任何最终文件变更前，先把全部 staged 文件逐字节复制到各自 `final.parent` 内的唯一安装候选，再执行同目录备份和原子替换。Windows 候选继承目标父目录 ACL；POSIX 候选保留 staged 文件 mode。替换失败时原 stage 始终保留；若 rollback 本身未再次失败，则恢复全部旧目标，不完整 rollback 会显式报错。成功后再清理 stage 和 backup。该路径不复制时间戳、xattr 或显式 ACL，并需要与 staged 数据等量的短期候选空间；当前小型 Git 制品不构成资源问题，预计和实测均远低于 1 小时。
- 新增候选复制失败零变更、同目录候选安装和 Windows ACL 继承回归测试，并扩展中途失败/成功测试以检查 `.install` 无残留。全套共 28 个测试：27 个通过；仅真实 symlink 创建测试因当前 Windows 账户权限跳过。`git diff --check` 和 Python 编译检查通过。
- 使用原冻结源 SHA-256、显式 1 L 元数据和固定时间戳重新运行准备脚本。修复后结果目录的 11 个文件及两个 run summary 均为 `AreAccessRulesProtected=False`，并包含 `Tian_lab_luly25\\16217` 与 `CodexSandboxUsers` 的继承访问项，且均非只读。
- 修复仅改变 NTFS 权限，不改变文件内容。所有结果文件 SHA-256 与修复前一致；独立解析器再次确认 47 条记录的序列、顺序、长度、逐序列 SHA-256、FASTA、产量字段、实验上下文和原文转录 mismatch 均为 0。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Add validated nanobody expression dataset`。
- 目标远程与分支：`origin/main`。

## 2026-08-03 16:02:31

### 本地项目 Conda 环境规则

- 用户已通过 `conda create -n ab_optim python=3.11` 创建本地项目专属环境。本次只读核验确认环境解析到 `D:\miniconda\envs\ab_optim`，当前 Python 为 3.11.15。
- `AGENTS.md` 已规定：后续与本地 Windows 和 Python 3.11 兼容的项目包与工具默认直接安装到该环境，不使用 `base` 或其他无关本地环境。
- 本地命名环境 `ab_optim` 与远程 `/data/software/env/luly25/ab_optim` 是两个相互独立的环境，不能假定包已同步。AF3、PyRosetta、nanoBERT、Linux-only/许可证受限二进制及不兼容 CUDA/PyTorch 栈仍须使用经核验的远程或工具专属环境，不能为满足同名而强行混装。
- 本次未安装或升级任何软件包，也未启动结构预测、打分或训练任务。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Document local ab_optim environment`。
- 目标远程与分支：`origin/main`。

## 2026-08-03 16:41:09

### 第一阶段本地工具链安装与验收

- 安装目标为本地 Windows `ab_optim`（`D:\miniconda\envs\ab_optim`，Python 3.11.15），预计和实测均远低于 1 小时，不需要 Slurm。安装前该环境除 Python 打包基础组件外没有项目分析依赖。
- ANARCII 2.0.8 的官方直接依赖包含 PyTorch。Conda-forge dry-run 会选择 CUDA 13.3 PyTorch 并计划下载约 2.19 GB，因此没有执行该方案；改为先从 PyTorch 官方 CPU 索引安装 `torch==2.13.0+cpu`，再通过 PyPI wheel 安装项目依赖，避免本地 CUDA 栈。
- 已安装并固定项目本地直接依赖：ANARCII 2.0.8、Biopython 1.87、Gemmi 0.7.5、Matplotlib 3.11.1、NumPy 2.4.6、pandas 3.0.5、PyTorch 2.13.0+cpu、SciPy 1.17.1、scikit-learn 1.9.0、statsmodels 0.14.6、seaborn 0.13.2、PyYAML 6.0.3、tqdm 4.70.0 和 pytest 9.1.1。`requirements-local.txt` 记录直接依赖，`requirements-local.lock.txt` 记录完整 pip 快照；二者仅用于本地环境，不代表远程环境已同步。
- 环境验收通过：`pip check` 报告无破损依赖，`torch.cuda.is_available()` 为 `False`，确认使用 CPU build。使用原始 `LTT__Nb252.sequence_raw`（128 aa，SHA-256 `df5b83ddde8a3486383c12afe45e22af6a358f507eab5503d5dbd4430710288d`）运行 ANARCII CPU smoke test，返回重链 `H`、IMGT、`Error: None`、零基闭区间 `query_start=0`/`query_end=125`；末端两个 `GS` 未进入编号域。该边界只作为 provisional 审核结果，不能在构建体确认前写回正式 `vhh_region_sequence`。
- 现有全套回归测试在该环境中运行 28 个：27 个通过，1 个因 Windows 缺少创建真实符号链接的权限而跳过，0 个失败。精确命令、版本和验收字段记录在 `docs/run_summaries/environment/local_setup_20260803.json`。
- 统计与建模包当前只是可用工具；在表达量协议、分组切分和评价方法确认前不使用 47 条数据训练决策模型。AF3、PyRosetta、nanoBERT、AntiFold、AbNatiV2、NanoMelt、OpenMM 等仍保留在远程或独立工具环境，不混入本地项目环境。

### 下一步工作定义

- 第一科学步骤定义为“建立 Nb252 的序列—结构—实验身份基线”，而不是生成突变：冻结原始输入；对 47 条 `sequence_raw` 做 provisional IMGT 编号与边界审核；向合作者确认成熟 VHH/表达构建体、末端 `GS` 和表达协议；从 `.cxs` 导出并核验三个模型及橙色注释；建立原序列索引、IMGT、结构 source 编号、缺失坐标和接口集合的可逆映射。
- 在权威序列边界、结构链身份、接口定义和跨来源产量语义确认前，不启动正式候选设计或产量模型训练。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Add local antibody analysis toolchain`。
- 目标远程与分支：`origin/main`。

## 2026-08-03 21:35:46

### 第一阶段序列—结构—实验身份基线实现

- 本轮按“输入身份基线”实施，不生成突变、不训练表达量模型，也未调用 AF3、PyRosetta 或 nanoBERT。全部常规计算使用本地 `D:\miniconda\envs\ab_optim`（Python 3.11.15、ANARCII 2.0.8、Gemmi 0.7.5），预计和实测均小于 1 小时，不需要 Slurm、checkpoint 或服务器环境。
- 再次核验原始 `Nb252-optimization.cxs` 为 537670 bytes、SHA-256 `1BC636C28F66AE60EDC658D2E1C4AAD0B07F4141CA5411C78662AA19DA793C4D`，`nb序列及产量（1L）.docx` 为 14172 bytes、SHA-256 `a6e4022f0978fbd70a0e04dc78f479140ab6f55caaa90b467fb77a62eb5db5d1`；两者大小、mtime 和哈希均未改变。`input_freeze_manifest.status=pass`。其中源会话由 ChimeraX 1.9/macOS 生成仅是 `user_provided` 信息，不是本轮从 CXS 自动验证出的事实。
- 新增 `scripts/input_baseline/` 的七个单一职责入口：ANARCII 序列审核、表达可比性审核、ChimeraX 会话导出、Gemmi 结构清单/映射/对齐、临时界面重算、阶段汇总和图像重绘。常规 Python 入口默认拒绝未知覆盖，允许覆盖的入口只事务性替换本流程已知文件；CXS exporter 目标目录必须不存在。

### 47 条 provisional IMGT 审核

- `docs/result_artifacts/input_baseline/sequence/` 保存固定列序的序列级和位置级 CSV、manifest；47 条原始 `sequence_raw` 和逐序列 SHA-256 均与上游冻结制品一致，未改写 `vhh_region_sequence`。
- ANARCII 结果为 46 pass、1 failed；唯一失败是 `WCC__4-28`，错误原文为 `Score less than cut off.`。成功结果中 H=45、L=1，chain type 仅作为工具输出，不作为实验链身份。
- `WCC__4-11` 被判为 L、编号止于 IMGT117，已标记人工复核，不能据此宣称真实轻链。`WCC__4-1` 只编号原序列索引 2–88 的 87 aa，止于 IMGT102。
- Nb252 被判为 H，原始零基闭区间 0–125、126 aa 进入编号，编号跨度 IMGT1–128；末端 `GS` 未编号。该边界和 reported 128-aa 序列仍是 provisional 参考，不是已确认成熟 VHH 或正式裁剪。

### 表达数据允许用途审核

- `docs/result_artifacts/input_baseline/expression/` 保存 78 行 `assay_metadata_review.csv`、严格 47 行 `sample_comparability_review.csv`、47 行浏览视图和 allowed-use manifest；来源计数仍为 LTT 23、WCC 8、LLJ 16，表型语义仍为 31 条 individual approximate、9 条 group lower bound、7 条 group approximate。
- LTT/WCC 31 条仅允许来源内 exploratory numeric 分析；LLJ 16 条仅允许来源内 ordinal/censored 探索，个体 `point_estimate_mg` 全空。47 条的跨来源 pooling 和向 Nb252 转移均为 `blocked`。
- 用户所述“同一体系”只记录为 `user_provided` 概括证据，未自动补齐宿主、载体、信号肽、标签、诱导、表达区室、纯化、定量、批次、重复、误差或协议等价性；表型继续称为 `reported_yield`，未重命名为统一 mg/L expression。

### 结构、映射和界面实现状态

- ChimeraX exporter 按名称唯一匹配三个目标 `AtomicStructure`，同时在 `session_model_inventory` 中清单化 surface/group/child 等全部非原子模型；导出 3 个 native-frame 和 2 个 experimental-reference-frame mmCIF，并记录模型层级、坐标集、变换、计数、颜色/显示/选择和可回映 surface 颜色。保存参数固定为 `relModel`、`bestGuess false`、`computedSheets false`。
- Gemmi 路线核验 model/chain/residue/logical atom/site 计数、atom-site 分类、altloc/occupancy、native/reference 拓扑和全原子刚体变换；`setup_entities()` 只用于内存分析副本，物理 atom-site 签名必须不变。
- 将 raw mmCIF `_entity*`/`_struct_asym`/`_pdbx_poly_seq_scheme` 证据读取、polymer 映射组合、结构入口辅助合同分别拆到 `source_mmcif_evidence.py`、`polymer_mapping.py` 和 `structure_baseline_support.py`。优先源 polymer 序列及源 label/auth scheme；源证据存在但冲突时阻断，只在对应标识缺失时允许 exact-first 或固定 BLOSUM62 fallback。最终映射保留 reported 128-aa Nb252 为可逆参考，不冒充 confirmed construct。
- 单一 `baseline_review.json` 严格绑定当前 inventory/颜色哈希，并分别审核全量链角色、RGB 精确为 `[255,165,0]` 的实际 RGBA/显示渠道和 authoritative construct。构建体确认必须给出 reported 哈希、连续字面序列及哈希、1-based inclusive 边界、scope、末端 `GS` 处置、确认人、带时区时间、说明和证据；仅填写 reviewer 名称不能通过。
- 定量对齐固定使用实验 VHH 为 reference、AF3 VHH 为 mobile，只对两者唯一映射且有坐标的 framework Cα 拟合，CDR3 不参与拟合；保存刚体变换、拟合数、常规定义 RMSD 和 FR/CDR 位移。
- 临时界面固定为同一实验模型 VHH/NK2R polymer 的正 occupancy 非 H/D 原子中心严格 `<4.0 Å`，无晶体/NCS，排除水/配体/糖/离子并检查 altloc 兼容性。`orange_vs_4A.csv` 保留 `both/orange_only/distance_only/neither/not_evaluable`，临时保护并集是“confirmed orange ∪ strict `<4.0 Å`”，不是能量热点。
- 当前没有执行必须由用户完成的 ChimeraX 1.12 GUI 视觉核验和 exporter；`data/structures/cxs_exports/` 不存在。因此 `structure_precheck` 与 `interface_precheck` 只生成诚实的 blocked manifest，尚未导出真实结构、确认链角色、产生结构映射/定量对齐、橙色残基集合、界面残基或临时保护并集。

### 汇总制品、阶段门与验收

- `docs/result_artifacts/input_baseline/summary/` 保存输入冻结 manifest、128 行 exact plotting data、4 行状态计数、`stage1_gate.json`、可复现 SVG 和 600 dpi PNG。图已人工检查，底部说明使用独立版面行，无标签重叠；当前 128 个实验/AF3 结构位置均是 `not_available`，橙色和临时界面为空，不能解释为真实结构覆盖结果。
- Finalizer `status=pass` 只表示汇总程序成功。当前 `local_baseline_build=blocked`，阻断项为 structure export、inventory、identity、mapping 和 interface safety；`candidate_design_release=blocked`，另加 authoritative Nb252 sequence；`pooled_expression_model_release=blocked`，阻断项为 cross-assay pooling。
- 全部 7 个人工 CSV 均为 UTF-8 with BOM、LF、固定列序，可由 PowerShell `Import-Csv` 解析；20 个 Git 跟踪 CSV/JSON/SVG 文本制品均为 LF-only。Matplotlib SVG 在保存后确定性移除行尾空格/Tab 并保留单个终止换行，避免 Git 行尾规范化使 manifest 哈希失真。21 个新增正式制品/run summary 均继承项目 ACL 且不是只读。固定时间戳在真实 47 条数据上双跑时，序列、表达审核和 summary 的正式数据/图制品逐字节一致；run summary 的实际耗时字段是运行遥测，不纳入字节固定合同。
- 全套测试最终为 91 passed、1 skipped、4 subtests passed；唯一跳过仍是 Windows 真实 symlink 权限测试。覆盖真实 47 条序列、插入码/auth-label/内部缺失/重复最佳比对/WT 冲突、3.999999/4.000000/4.000001 Å 边界、H/D/水/配体/occupancy 0/altloc/晶体 mate、颜色渠道/透明橙色/surface 回映、导出计数/刚体、LF JSON/SVG 写出和严格构建体 review。`pip check`、Python compileall 和 `git diff --check` 均通过。

### Git sync status

- 状态：`ready to commit`。
- 计划提交信息：`Implement stage-1 identity baseline`。
- 目标远程与分支：`origin/main`。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
