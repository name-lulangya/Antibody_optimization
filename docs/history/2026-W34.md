# 2026-W34 项目历史

## 2026-08-17 10:32:25

### 30条预终选与6条替补的本地确定性实现

- 本阶段只实现既定步骤1–3：统一候选证据、应用终选前硬约束/分轨比较、形成三类预终选。预计并实测均低于1小时，不运行新模型，不需要Slurm、checkpoint、resume或额外smoke。
- 新增纯选择模块和本地CLI，读取14条已释放活跃单突、86条V2.1双突、单突亲和力/性质PyRosetta证据及阶段2母本/硬约束。逐条核对完整128-aa序列、突变标签、Cys22/Cys95、末端`SSGS`和未配对Cys；输出100行完整审计，其中14条单突与42条支持双突组成56条主证据池，44条权衡/不清晰双突保留为审计而不进入主池。
- 30条预终选由全部14条活跃单突和16条平衡支持双突组成，分类为亲和力单突/性质单突/平衡组合=`8/6/16`。保留全部单突是为了实验上拆解组合组成效应；20样本Flex ddG单突与3重复局部双突的绝对REU不直接比较。
- 16条双突只在共同V2.1协议内使用`ΔdG`、跨界面能、非微小性质有利数和AntiFold固定骨架加和值进行Pareto分层；其后显式限制每个组成突变最多5次、每个reported位置对最多2次，避免R45或30/45假设垄断名单。入选双突覆盖10种组成突变和11种位置对，其中13条配对WT接触不变、3条存在已通过结构安全门的局部变化。
- 另生成6条分层替补：平衡支持、亲和力支持且性质非不利、性质支持且亲和力非不利各2条。gate为`pass/ready_for_targeted_finalist_review`，明确`preliminary_panel_selection_performed=true`而`final_candidate_selection_performed=false`；WT不计入30条，预测值不解释为实测亲和力、表达、溶解性、稳定性、Tm或yield。
- 正式制品位于`docs/result_artifacts/candidate_design/preliminary_panel_20260817/`：30条CSV/FASTA、6条替补、100行审计、gate、精确绘图数据和600 dpi PNG/SVG；run summary位于`docs/run_summaries/candidate_design/preliminary_panel_20260817.json`。人工检查图中100→56→30及6替补证据流、`8/6/16`构成和`13/3`双突接触状态均清晰无遮挡。
- 新增4项专项测试，覆盖真实配额/硬约束、多样性上限、输入顺序确定性、序列标签不一致阻断及端到端CLI/图/FASTA；连同相邻单/双突测试共`13 passed`。最终全套为`227 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试；`pip check`、全项目Python编译和`git diff --check`通过。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`806e2e85162152d7c81e4db9b9f38fdeb8475f62`（`Build preliminary 30-sequence panel`）。

## 2026-08-17 15:20:00

### 终审能量来源复用与显式最终冻结合同

- 根据用户复核，取消默认的14个位置对单突状态补算和批量AF3终选预测。现有单突均已完成PyRosetta；不同单/双突协议不能直接组成严格四状态循环，但最终实验选择主要需要现有AB相对位置对WT的直接表现，不需要为解释上位性重复计算。只有真实结果出现无法取舍的关键组合时，后续才条件触发极少数同协议WT/A/B/AB。
- 检查真实制品后确认：亲和力全量单突和性质单突的突变体/WT总分已在Git跟踪CSV中；双突V2.1紧凑表只保留`ΔdG`和跨界面能等摘要，完整`mutant_total_score`与位置对WT总分仍位于远程ignored目录`results/candidate_design/double_mutant_scan_20260816/pyrosetta/`。因此本阶段不从摘要或对话猜测缺失值，而是实现一次远程轻量提取分析。
- 新增纯分析模块和CLI，对当前30条预终选加6条替补逐重复计算`ΔE_complex`、`ΔE_separated`并验证`ΔdG=ΔE_complex-ΔE_separated`。所有差值只按记录中的`wt_control_id`、replicate和seed配对，绝对REU不跨协议比较；分离态明确标记为Rosetta代理而非VHH实验单体稳定性、折叠自由能或Tm。输出合同为108行重复、36行汇总、36行显式决策模板、gate和600 dpi PNG/SVG，不调用PyRosetta或AF3。
- 远程唯一入口为`run_finalist_energy_review.sh`，使用项目`ab_optim`环境直接读取既有双突原始CSV，预计低于1分钟且不需要Slurm、checkpoint、resume或smoke。由于本地没有该ignored双突表，本轮只实现并测试入口，不生成或伪造真实终审数值，最终选择门继续等待远程轻量运行结果。
- 新增显式最终冻结合同：只有36行均填写`select/reserve/exclude`和非空科学理由、且恰好30条为`select`时，才生成最终CSV/FASTA、储备/决策审计、结果图、run summary并设置`final_candidate_selection_performed=true`。入口重新核对128-aa、突变标签、序列唯一性、Cys22/Cys95、总Cys数及末端`SSGS`；WT仍是30条之外的独立实验对照。
- 新增5项专项测试，覆盖36条×3重复能量恒等式、协议作用域、缺失配对WT阻断、显式30条冻结、待审决定阻断及两类结果图渲染；与预终选相邻测试合计`9 passed`。最终全套为`232 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试；`pip check`、全项目Python编译和`git diff --check`通过。使用明确test-only数值渲染的能量终审图和最终面板图已人工检查，轴定义、分离态非实验说明、类别计数及版面均清晰；真实结果图仍必须由远程双突原始表生成后再次检查。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`020ff8aece3312b2284f0a27007aef6795cf1813`（`Implement finalist energy review`）。

## 2026-08-17 11:45:13

### 终审制品事务写出接口修复

- 远程运行在完成36条候选能量汇总和绘图后，于最终文件安装阶段报错：两个新CLI误把`replace_staged_files`调用写成不存在的`staged_paths`/`final_paths`关键字；因此本次失败没有释放正式终审制品，也没有改变任何科学计算结果。
- 将能量终审和后续30条最终冻结两个入口统一改为项目事务层的真实合同：传入“暂存文件→最终文件”映射、`project_root`和已验证的受保护源文件。输入、输出、能量定义、筛选语义及运行命令均未改变。
- 新增端到端CLI回归，使用明确test-only数据实际完成36条终审汇总和30条最终冻结，覆盖此前单元测试未经过的事务安装路径。专项与相邻事务/预终选测试为`19 passed, 1 skipped`；Python编译和`git diff --check`通过。预计运行仍低于1分钟，不需要Slurm或额外模型计算。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`1be2b35fb9166722f657b2877da6988ce5a5cc72`（`Fix finalist artifact installation`）。

## 2026-08-17 11:52:15

### 真实终审能量来源结果同步与分析

- 远程事务修复后运行成功，提交`84905a6533287195e27682497823480b21ba7a6f`已随pull同步。权威制品位于`docs/result_artifacts/candidate_design/finalist_energy_review_20260817/`，运行摘要位于`docs/run_summaries/candidate_design/finalist_energy_review_20260817.json`；实际耗时2.055765秒，未运行新PyRosetta或AF3。
- gate为`pass/ready_for_explicit_final_30_decision`：36条候选、每条3重复、共108行，所有`ΔdG=ΔE_complex-ΔE_separated`恒等式误差为0。能量来源分为复合物与分离态均有利5条、表观结合改善完全由分离态惩罚驱动12条、其余一致分离态谨慎19条；12条强谨慎候选全部来自当前30条预终选，6条替补均无强谨慎标记。
- 33/36条`ΔdG`中位数<0，35/36条跨界面能中位数<0，但仅18/36条复合物总分中位数<0；33条负`ΔdG`中23条主要由分离态惩罚贡献。因此该预选池中的负`ΔdG`和负跨界面能区分力有限，不能单独当作终选证据。5条双态均有利候选为`A23S;E105F`、`E105F`、`E105L`、`A23S;E105L`和`A23S`，其中`A23S`的`ΔdG`中位数为+0.050 REU且仅1/3重复有利，仍不能称为结合改善。
- 真实PNG已人工检查：能量来源分解、直接结合信号和类别计数三面板清晰，无标签或图例遮挡，并明确注明分离态不是实测单体稳定性。当前决策模板仍为36条`pending`，`final_candidate_selection_performed=false`；本次只同步与解释真实结果，不自动冻结最终30条。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`a79900cc058085a97d29ffafd4bdf3f3047c1971`（`Analyze finalist energy origins`）。

## 2026-08-17 12:13:40

### 最终30条序列显式终审与冻结

- 将最终冻结入口改为只接受原始36行能量终审模板与独立显式决策JSON，不再要求人工改写完整生成表。新增纯函数严格校验36个唯一候选、每条`select/reserve/exclude`、非空候选特异理由、reviewer、decision basis及恰好30条选择；不自动推断或补齐决定。预计并实测均低于1分钟，不使用Slurm或新增模型计算。
- 综合配对Rosetta能量来源、复合物/跨界面信号、接触与表位、AntiFold、U/S/Tm、TNP、化学/结构风险、单突拆解价值和组合多样性后，最终选择6条亲和力单突、6条性质单突和18条组合突变。排除`D101W`、`I103W`、`Q1D;R45V`；将`F30S;E105L`、`R45T;E105L`、`S55G;E105F`保留为储备；从原储备提升`A23S;R45V`、`F30T;E105F`、`R45T;E105F`。双突任一组成突变最多出现5次。
- gate为`pass/ready_for_experimental_submission`。真实制品验证30条均为唯一128-aa序列，突变标签一致，Cys22/Cys95、总Cys数和末端`SSGS`保持，FASTA恰好30条；WT仍为面板外独立对照。最终面板仍含9条“结合改善由分离态惩罚驱动”的低置信性质/机制假设，决策理由明确未将其称为亲和力改善。
- 正式CSV/FASTA、3条储备、3条排除的完整审计、显式决策合同、精确绘图数据、gate和600 dpi PNG/SVG位于`docs/result_artifacts/candidate_design/final_candidate_panel_20260817/`；run summary位于`docs/run_summaries/candidate_design/final_candidate_panel_20260817.json`。人工检查最终图无标签遮挡，类别、能量来源与记录风险计数清晰。
- 新增显式决策合同联接测试并更新端到端CLI测试；全套为`234 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试。`pip check`、Python编译和`git diff --check`通过。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Freeze final 30-sequence panel`。

## 2026-08-17 12:39:08

### W31–W34累计阶段周报生成

- 将项目启动至最终30条候选冻结的全部工作整理为中文阶段周报，正文按“输入身份基线→结构/界面→PyRosetta校准→456×3亲和力扫描→50候选/1000任务Flex ddG→1962条统一性质景观→86条双突→36条能量来源终审→30条最终面板”展开；所有数值均重新读取机器可读run summary、gate、最终CSV或权威项目记录，未从对话或旧图注抄写。
- 新增单一职责报告入口`scripts/reporting/build_nb252_stage_weekly_report.py`。脚本约621行，超过500行检查信号但仍保持一个内聚职责：读取冻结制品、生成一张报告专用证据漏斗图、复用既有7张结果图、设置Word样式/三线表及写出报告清单；未混入候选筛选、模型计算或科学语义变更，故本轮不机械拆分。
- 为保证该入口可在文档化的本地环境直接复现，将`python-docx==1.2.0`加入`requirements-local.txt`，并把其解析依赖`lxml==6.1.1`和python-docx本身写入完整锁文件；未改变远程模型或工具专属环境。
- 正式制品位于`docs/result_artifacts/weekly_report_result/report_2026_W34_nb252_optimization/`，包括`Nb252_optimization_stage_report_2026_W31_W34.docx`、8张报告图、漏斗精确数据、README和`report_manifest.json`。报告采用`standard_business_brief`设计预设和编辑式封面，正文明确30条均为计算优先候选而非实验验证，并附最终候选概览、30条完整128-aa序列、3条储备、3条排除、软件角色和权威制品索引。
- 本地结构审计结果：DOCX含27个package entries、17745个文本字符、19张表、12个重复表头和8幅嵌入图；最终30条mutation set及30条完整序列均可在报告中定位，全部序列以`SSGS`结尾，未发现`PLACEHOLDER`、`TODO`或`test-only`文本。新增证据漏斗图已人工检查，中文字体、箭头、计数和说明清晰。
- 本机没有LibreOffice/`soffice`，Word COM也无法在当前无交互会话启动；文档技能的DOCX→PNG逐页渲染门因此无法执行。已按允许的降级路线完成OOXML内容、表格几何、嵌图和关键文本结构检查，但不能声称通过逐页视觉QA。预计与实测均低于1小时，不需要Slurm、checkpoint、resume或远程模型计算。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Add Nb252 stage weekly report`。

## 2026-08-18 14:37:49

### 面向合作者与导师的阶段周报筛选逻辑补充

- 将周报从内部制品索引式叙述调整为面向合作者和导师的科学汇报。正文新增完整亲和力漏斗：456条界面单突在全量三重复后按双能量方向、重复一致性、接触保持、局部排斥和界面骨架稳定性分为`18/30/39/82/287`五层；全部48条Tier 1/2加位置多样性Tier 3 `D33N/Y115F`进入50条Flex ddG，随后以两项能量各至少18/20有利且中位数有利形成8个核心模块，并通过Cys/结构风险审查与同位点替代形成8条活跃亲和力单突，终审排除`D101W/I103W`后保留6条。
- 正文新增完整性质漏斗：122个可变位置共枚举2318条单突，122条新增未配对Cys阻断、234条实验缺失坐标候选暂缓，1962条进入统一评分；1530条性质发现候选产生49条Pareto 1，幅度感知门以“至少1项U/S/Tm非微小改善、无非微小恶化、无新增化学风险”选出30条；三重复配对PyRosetta与接触、AntiFold、AF3局部包装和化学风险复核后，最终保留`Q1D/A23S/F30A/F30S/F30T/S55G`六条活跃性质单突。
- 明确解释100条审查记录的来源为14条活跃单突加86条可实现双突；86来自`C(14,2)=91`去除5组同位点互斥组合。主证据池由全部14条单突与42条联合支持双突组成，共56条；其余44条权衡/不清晰双突只留审计。初步面板保留14条组成单突，并从28条平衡支持双突中按同协议四目标Pareto和突变/位置对多样性选16条；再按三类联合证据各留2条储备，形成30+6=36条finalist。
- 删除读者可见的附录E内部相对路径索引、图下注明的内部制品ID和过度工程化的环境版本表；附录C改为简洁的“工具作用/不能声称”说明，封面改为项目阶段交流材料。更新证据漏斗图为`48严格层+2多样性补充`、`2318→1962`及`14+86→56→36`，并为8幅嵌图加入替代文本。
- 重新生成DOCX并完成结构审计：22张表、8幅嵌图、30条最终突变标签和30条完整序列均存在，正文不含`docs/`内部路径或附录E，DOCX ZIP完整且无损坏条目；a11y高严重度问题由8项降为0，剩余7项为无表头语义的单格callout布局表。`pip check`、脚本编译和`git diff --check`通过。当前机器仍无LibreOffice/`soffice`，且文档渲染器缺少`pdf2image`，因此未完成逐页PNG渲染，不能声称通过逐页视觉QA；更新后的证据漏斗PNG已人工检查，无文字、箭头或计数遮挡。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Refine weekly report selection narrative`。

## 2026-08-18 14:57:49

### 面向外部读者补充协议与性质指标释义

- 在周报5.1中明确`interface_repack_constrained_min`是项目内局部结构准备流程：界面邻域侧链repack后，对同一邻域主链和侧链进行坐标约束下的有限最小化；其余残基和链间刚体位置固定。补充其目的为缓解局部碰撞并维持实验结合构象，说明该流程不是全局relax或重新对接，WT与突变体按同一设置作配对相对能量比较。
- 在性质工具章节新增四项指标的简要定义、方向和证据边界：NetSolP U/S分别为0–1的综合可用性/溶解性模型分数；NanoMelt输出预测表观Tm（°C）；实验复合物视图AntiFold输出突变相对WT的条件对数概率差。统一说明正向变化仅代表相应模型方向更有利，不能替代BL21产量、实测溶解度、实验Tm、结合自由能或亲和力。
- 本次仅局部修订报告生成脚本并重新生成同一DOCX，不改变候选、序列、数值结果或筛选结论。预计与实测均低于1小时，不需要Slurm或远程计算。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Package collaborator delivery files`。

## 2026-08-18 15:08:06

### 周报外部交流表述清理

- 按合作者阅读场景删除第二页“不展示项目内部文件路径”的自指说明，并通读DOCX清理同类工程化措辞。正文不再显示内部路径、命令、脚本、CSV/JSON文件名、`gate/release/status`、内部候选ID或运行制品标识。
- 将最终状态改为“建议进入实验测试”，将36条`finalist`统一称为“终审候选”；附录A的英文机器风险/能量标签和截断英文审查理由改为中文科学说明，附录B的储备与排除理由全部改为中文，附录D仅展示突变名和完整序列。
- 重新生成同一DOCX；候选数量、突变、序列、数值结果和筛选结论均未改变。预计与实测均低于1小时，不需要Slurm或远程计算。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Polish collaborator-facing weekly report`。

## 2026-08-18 15:26:39

### 周报纯黑字体与标题样式移除

- 按用户要求仅调整DOCX格式：所有可编辑文字统一为黑色；正文中不再应用Word的`Heading`、`Title`或`Subtitle`段落样式。原一级、二级和三级标题字号分别通过直接格式保留为16、13和12 pt，封面、正文、表格及序列字号均保持原值。
- 深浅底纹、内容顺序、表格结构、图片、候选、序列和数值均未改变。生成前后正文与表格文本摘要一致；最终DOCX中标题样式段落为0，正文、表格、页眉和页脚的显式非黑色文字为0。
- 本次为低于1小时的本地文档格式修改，不需要Slurm或远程计算。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Apply plain black report formatting`。

## 2026-08-18 16:05:00

### 合作者交付包整理

- 新增单一职责入口`scripts/reporting/build_nb252_collaborator_delivery.py`，从已冻结的最终30条候选CSV、权威`LTT__Nb252`母本记录和最新版阶段报告生成独立对外交付目录；预计与实测均低于1分钟，不需要Slurm、checkpoint或远程计算。
- 交付包包含阶段报告、30条候选标准FASTA、人工可读序列目录、独立WT对照FASTA和简短使用说明，并同时生成含相同5个文件的同级ZIP便于直接发送。对外样品名统一为`Nb252-C01`至`Nb252-C30`，不暴露内部候选ID、评分字段、运行状态或项目相对路径。
- 一次性边界核对通过：30条候选与权威最终清单逐条一致且互不重复，均为128 aa并保留末端`SSGS`；WT为权威完整128-aa序列且不计入30条候选；复制的DOCX与来源文件一致且ZIP结构完整。当前会话没有提供专用工作簿生成运行时，因此未用替代库强行生成XLSX，序列交付采用FASTA和UTF-8 Markdown目录。
- 首次生成目录通过同卷暂存目录重命名安装，导致Windows目标目录保留了暂存ACL而未继承项目父目录权限，用户和Git无法正常遍历。已用可恢复的同级备份复制方式重建目标目录并核对5个文件一致；生成入口改为显式创建最终目录后逐文件安装，使后续交付目录继承项目ACL。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Refine natural VHH conservation contract`。

## 2026-08-18 17:10:00

### 导师讨论后的项目目标与现行路线替换

- 项目显式目标由亲和力、稳定性和表达多目标优化改为只优化Nb252在BL21体系中的表达产量；PyRosetta、Flex ddG和既有Rosetta能量退出当前候选生成与排序，既有亲和力、双突和旧30条面板仅保留为历史provenance。
- 24个已复现实验界面位点由“谨慎可突变”提升为硬冻结集合；后续还必须通过有明确输入范围、去冗余、gap/覆盖、编号映射和保守性阈值的MSA合同冻结保守位点。Cys22/Cys95与末端`SSGS`继续不可突变。
- 新实验面板固定为30条单突；单突表达实验完成并复核前禁止生成或交付组合。现行工具缩减为NetSolP、NanoMelt和AntiFold；至多依次测试一至两个新表达工具，只有在47条yield数据上显示样本外增益后才纳入。
- 工具验证合同新增离散评价：在保留连续相关分析的同时，预声明yield标签和指标方向，在训练折内选择阈值，并用外层LOOCV、重复分层或序列簇留出报告ROC-AUC、PR-AUC、MCC、balanced accuracy、sensitivity、specificity与阈值稳定性；LLJ继续保留分组/删失语义，不填补虚构个体产量。
- 本轮仅更新`AGENTS.md`和replace-in-place的`docs/codex-handoff.md`，尚未修改机器可读关键残基集合、候选生成代码或结果。新的表达单突设计门在界面硬冻结迁移、MSA合同和工具分类验证合同完成前保持blocked。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Refocus project on expression optimization`。

## 2026-08-18 19:00:00

### Nb252 邻近天然 VHH 保守性合同与单突约束空间

- 本阶段预计少于一小时；实际本地 CPU 全量运行约 226 秒，因此未使用 Slurm、checkpoint 或 resume。
- 核验发现 TNP 仓库当前原始 `vhh_oas.fasta` 含 4,383 条，而论文最终描述符表恰为 4,059 条。项目冻结后者，并绑定 TNP 提交 `a9ba3edc3d967ecf8a2b9b5c2c29bf7495bbc9a0` 与输入 SHA-256；这批天然 VHH 只用于保守性估计，不含 BL21 产量标签。
- 4,059 条序列经固定 ANARCII/IMGT 审核后 4,057 条合格；按完整 IMGT 域 90% identity 单连接去冗余得到 3,784 簇，再以 framework-only identity/coverage 均不低于 0.80 得到 1,564 条 Nb252 邻域序列和 1,532 个有效簇。
- 加权分析将 128 个 reported positions 分为 55 个硬保守、33 个谨慎、33 个可变和 7 个证据不足。硬保守位点与 24 个实验界面位点、Cys22/Cys95 和末端 SSGS 合并后冻结 81 个位置；其余 47 个位置形成 846 条非 Cys、完整 128-aa 单突，全部保留末端 SSGS。这是待性质预测的约束空间，不是最终 30 条面板。
- 新增可复用保守性/绘图模块、单一编排入口、真实数据测试、权威合同、审核表、压缩映射、候选 FASTA/CSV、sequence logo、约束轨道、阶段门和轻量 run summary。机器可读合同是下游权威来源，handoff 不复制残基列表。
- 验证：全套测试 `237 passed, 1 skipped, 4 subtests passed`；4,059/4,057/1,564/128/846 计数、末端 reported 127=G 与 128=S、候选唯一性和 gzip 回读均通过；`pip check`、Python 编译检查、`git diff --check` 通过，结果图已人工检查。

### Git sync status

- 状态：`ready to commit`；目标为 `origin/main`。
- 计划提交信息：`Build Nb252 natural VHH conservation contract`。

## 2026-08-18 21:55:00

### 项目表达序列Logo与FR/CDR区域标注

- 扩展统一SeqLogo绘图函数，用淡蓝色FR背景、淡橙色CDR背景和顶部区域标签显式标出IMGT FR1–FR4及CDR1–CDR3；重新生成全局天然VHH和Nb252邻域的区域标注Logo。
- 新增47条项目产量序列的等权Logo。依据已冻结编号审核，45条`pass/H`序列进入IMGT频率统计；`WCC__4-28`编号失败、`WCC__4-11`被判为L链，均在审核表中明确排除。产量不参与Logo加权，因此该图只描述序列频率，不是产量关联模型。
- 新增项目位置频率表、完整47行纳入/排除审核、绘图清单、PNG/SVG和run summary。任务预计少于5分钟，实际本地CPU约13秒，无需Slurm、checkpoint或resume。
- 验证：全套测试 `239 passed, 1 skipped, 4 subtests passed`；三张PNG已人工检查，FR/CDR色带、标签、IMGT插入码和项目Logo标题均可读。

### Git sync status

- 状态：`ready to commit`；目标为 `origin/main`。
- 计划提交信息：`Add FR CDR annotated VHH sequence logos`。

## 2026-08-18 23:30:00

### RP3Net—BL21产量连续与离散验证实现

- 固定RP3Net 0.0.2、`RP3Esm2_650m`、专属环境、官方CLI和已上传checkpoint；checkpoint身份在新的模型输入边界验证一次。已生成47条真实序列计划，全部进入远程推理；31条LTT/WCC个体近似值进入数值和分类分析，16条LLJ只保留分组/删失有序分析，不生成个体产量。
- 新增通用无泄漏分类模块。高/低产量以匹配来源的外层训练折中位数定义，RP3Net指标方向预声明为越高越好，判别阈值仅在训练折内按MCC、balanced accuracy和确定性tie-break选择；分别执行逐样本留一和90%序列簇留一，报告ROC-AUC、PR-AUC、MCC、balanced accuracy、sensitivity、specificity及阈值分布。
- 新增计划、官方GPU推理、分析/绘图三个单一职责入口，以及单任务Slurm与提交包装。正式47条运行预计少于一小时，使用`batch`分区、1 GPU和12 CPU，不使用数组、checkpoint或resume；日志写入`logs/rp3net_yield_validation/`。
- 当前仅完成本地计划和实现，未运行远程RP3Net正式评分，也未产生项目验证结论；RP3Net仍不得用于846条候选排序。远程结果将生成连续关联表、两种分类汇总/逐折预测、四面板图、gate和run summary，再据预声明门决定是否纳入。
- 验证：全套测试`242 passed, 1 skipped, 4 subtests passed`；`pip check`、Python编译检查、Bash语法检查与`git diff --check`通过。任务未提交或推送。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Implement RP3Net yield validation`。

## 2026-08-19 09:30:00

### RP3Net评分环境的分析依赖泄漏修复

- 首次远程运行在模型推理前导入`score_rp3net_sequences.py`时失败：评分入口经`rp3net_yield`提前加载NetSolP/nanoBERT分析模块，从而在RP3Net专属环境中无关地要求Biopython。该错误未产生RP3Net分数或分析结果，不需要安装Biopython来掩盖依赖边界问题。
- 将计划构建与统计分析依赖移动到各自函数内部；评分阶段导入及`normalize_rp3net_scores`现在只依赖Python标准库。未改变47条计划、checkpoint、官方RP3命令、输出schema、统计定义或Slurm资源合同，原提交命令可直接重新运行。
- 新增隔离导入回归测试，以`python -S`禁用site-packages后导入评分模块，防止Biopython、NumPy或其他分析包再次泄漏到RP3Net评分环境。预计与实测修复验证均少于一小时，无需Slurm、checkpoint或resume。
- 验证：隔离导入通过；全套测试`243 passed, 1 skipped, 4 subtests passed`；`pip check`、Python编译检查和`git diff --check`通过。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Fix RP3Net scoring dependency boundary`。

## 2026-08-19 10:15:00

### RP3Net—BL21产量正式验证结果同步与决策

- 同步并核验远程正式结果：47/47条序列完成RP3Net评分；31条LTT/WCC保留个体数值语义，16条LLJ保留有序/删失语义。结果目录包含样本证据、连续关联、两种外层分类、逐折预测、PNG/SVG、gate和run summary，计数与schema一致。
- 直接合并Spearman为0.476（p=0.0067），但来源内分层Spearman降为0.198，LTT/WCC来源内分别为0.195/0.265，分层bootstrap 95%区间为[-0.266, 0.629]且置换p=0.353。较强的合并相关主要受来源间分数与产量尺度共同分离影响，不能作为稳定的序列内排序证据。
- 长度校正偏Spearman为0.398，LOOCV与序列簇CV相对provider-only基线均有正增量；但16条LLJ的有序Kendall为-0.451，与“分数越高、产量越高”的预声明方向相反，削弱了跨来源一致性。
- 逐样本留一和序列簇留一的汇总均为ROC-AUC 0.621、PR-AUC 0.620、MCC 0.313、balanced accuracy 0.650、sensitivity 0.500、specificity 0.800。31条数值记录有11条外层误分类；两种方案有6条逐样本折结果差异，但聚合混淆矩阵和排序指标相同。训练折分数阈值主要位于0.776–0.819，只是本数据集的折内阈值，不是通用阈值。
- 预声明连续与分类联合门未通过，机器可读结论为`no_supported_use` / `rp3net_not_supported_for_candidate_use`。RP3Net不加入846条单突筛选或最终30条排序；后续保持NetSolP、NanoMelt和实验复合物视图AntiFold三工具精简路线，并先补齐其离散分类验证。
- 本次只读取并解释既有正式结果、更新replace-in-place handoff与周历史；未重跑模型、未修改结果文件、未改变候选空间。结果图四面板已人工检查，内容与表格一致。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`201fe45ca6f448cea531c20d79614c14528ca553`（`rp3net_yield_validation_result_20260818`）。

## 2026-08-19 11:00:00

### RP3Net与NetSolP正式分数对照分析

- 按`sample_uid`联接两套47条正式样本证据，并逐条核对序列字面值一致。RP3Net与NetSolP U/S在全体47条上的Spearman分别为0.550/0.583，在31条个体数值记录中为0.579/0.529；说明模型存在中度重叠，但并非同一指标。
- 来源分层后重叠减弱且不均一：全体47条的来源内分层Spearman为RP3Net–U 0.400、RP3Net–S 0.203；LTT内部为0.339/0.066，WCC内部为0.833/0.857但仅8条，LLJ内部均为0.529。因而较高的总体相关部分来自来源/序列家族构成，不能解释为两个工具在目标局部突变空间中必然一致。
- 与31条数值产量的连续关联比较：RP3Net、NetSolP U、NetSolP S的合并Spearman分别为0.476/0.498/0.583，来源内分层Spearman为0.198/0.394/0.376；LLJ有序Kendall均为负，分别为-0.451/-0.165/-0.275。NetSolP整体比RP3Net更稳定，但仍不足以单独排序候选。
- 将RP3Net完全相同的训练折产量中位数标签和折内MCC阈值合同临时复用于NetSolP：U的逐样本留一ROC-AUC/PR-AUC/MCC/balanced accuracy为0.621/0.697/0.300/0.648；S为0.663/0.682/0.477/0.717。S在序列簇留出时MCC降至0.313、balanced accuracy降至0.650，显示逐样本留一优势部分受相关序列影响。该对照是从现有结果表确定性重算的分析结论，尚未替代后续正式NetSolP分类制品与gate。
- 复核RP3Net图的四面板数据及分类代码：A为31条数值记录的对数产量散点；B同时展示普通相关、来源内相关、长度校正相关和相对provider-only基线的cluster-CV增量；C为外层留一分类指标；D为各训练折选定的RP3Net阈值分布。图C的0.5虚线只适合作为ROC-AUC/balanced accuracy的随机参照，PR-AUC基线应为阳性率0.516，MCC随机参照应为0。
- 按用户要求将panel C纵轴由`-1–1`改为`0–1`，并从既有精确CSV重新生成PNG/SVG；所有统计值、候选结论和其他面板不变，更新图已人工检查。
- 方法学复核认为：训练折内最大化MCC选择预测分数阈值是合理的探索性做法，但当前“各外层训练折按来源中位数重新定义产量标签”会使同一样本的目标定义随折变化，WCC小样本尤其不稳定。正式三工具分类应优先固定一个与预测器无关的产量定义，再在内层训练折选择预测分数阈值；不应在同一数据层同时搜索产量阈值和分数阈值以最大化MCC。若将双阈值搜索作为敏感性分析，必须完整嵌套在外层序列簇留出内并约束最小类别数，不能作为主要gate。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Compare expression predictors and refine RP3Net figure`。

## 2026-08-19 12:00:00

### 固定5 mg产量标签的探索性展示图

- 按用户最终选择，将LTT/WCC的31条个体近似数值记录显示为`reported yield >= 5 mg/1 L`高产；得到高产14条、低产17条。16条LLJ继续保持分组/删失语义，不生成个体二分类标签。
- 对RP3Net、NetSolP U和NetSolP S统一按“分数越高越好”展示。逐样本留一和90%序列簇留一的每个外层训练折按最大MCC选择预测分数阈值，MCC并列时依次选择balanced accuracy更高、分数阈值更高者；5 mg产量标签本身不参与搜索。
- 序列簇外展示结果：RP3Net的ROC-AUC/PR-AUC/MCC/balanced accuracy为0.744/0.635/0.131/0.563，NetSolP U为0.744/0.724/0.418/0.710，NetSolP S为0.777/0.704/0.411/0.697。该结果只描述当前31条记录，不能反向证明5 mg是最佳生物学阈值。
- 新增固定标签分类复用函数、单一展示入口、5项针对性测试、机器可读合同、汇总/逐折CSV、run summary及四面板PNG/SVG。图题与合同均明确`display_only`，不改变RP3Net现有gate，也不参与后续候选生成或排序。
- 本地统计和绘图低于一分钟，无需Slurm、checkpoint或resume。验证：针对性测试`5 passed`、Python编译检查和`git diff --check`通过；最终5 mg PNG已人工检查，标题、阈值线、阈值IQR及两组留出指标均可读。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Add fixed 5 mg predictor display`。

## 2026-08-19 17:25:29

### PLM_Sol—BL21产量正式验证实现

- 在Nb252单序列smoke通过后实现47条正式验证路线；smoke确认ProtT5嵌入为`(128,1024)`/float16、完整128-aa序列和末端SSGS保留，分类器分数0.922947只作为调用链证据，不解释为实测溶解度或产量。
- 固定PLM_Sol V1.0、ProtT5-XL-U50、本地模型位置及嵌入/分类/分析三个环境。合同记录发布包实际需要的四项源码接口修复和遗漏依赖`pyaml==21.10.1`；评分入口只验证该固定合同，不重新下载或替换权重。
- 新增计划、嵌入、分类和分析四个单一职责入口及一个Slurm任务。47条原始序列全部进入通用蛋白嵌入，不继承TNP/NanoMelt适用域排除；官方哈希ID通过完整且唯一的原始序列回映，要求47/47覆盖、1024维有限float16嵌入及`[0,1]`有限分类分数。
- 统计复用项目既有31条LTT/WCC数值与16条LLJ有序/删失语义，报告合并/来源分层/长度校正/重采样/序列簇外连续证据，以及训练折内MCC阈值的逐样本和90%序列簇留一分类。另固定与NetSolP S/U及历史RP3Net的相关比较，并预声明NetSolP S基础上的序列簇外增量；固定5 mg制品只作展示，不进入gate。
- 正式任务预计10–30分钟且低于1小时，使用`batch`、1 GPU、12 CPU，无数组、checkpoint或resume；日志写入`logs/plm_sol_yield_validation/`。本地已生成47条正式计划，尚未运行远程嵌入/分类，PLM_Sol不得用于846条候选排序。
- 新增3项专项测试，覆盖哈希输出的精确序列回映、错配阻断、47条真实表型/比较/分类语义及两类绘图。最终全套测试为`247 passed, 1 skipped, 4 subtests passed`，唯一skip仍为Windows真实symlink权限测试；`pip check`、Python编译、Bash语法和`git diff --check`均通过。远程正式模型运行尚未执行。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`2e03a074ce124701f52502a639081ef04fa5e4ad`（`Implement PLM Sol yield validation`）。

## 2026-08-19 18:30:00

### PLM_Sol Python 3.8 写出兼容性修复

- 首次远程正式运行在嵌入配置写出阶段、模型执行前失败：两个工具环境均为 Python 3.8，而 `Path.write_text(newline=...)` 是较新 Python 才支持的接口。此次失败未生成有效嵌入、分类分数或分析结果。
- 将嵌入与分类入口的配置和运行 JSON 写出统一改为 Python 3.8 支持的显式文本文件句柄，同时保持 UTF-8 与 LF 合同不变；模型、权重、47 条输入、统计定义和 Slurm 资源均未改变。
- 提交包装器现在允许失败运行遗留的空父目录存在，但仍拒绝覆盖正式 `embeddings`、`model_scores` 和结果制品目录，因此修复后可从头重新提交同一任务。
- 新增 Python 3.8 语法及不支持 API 的回归检查；专项测试为 `4 passed`，全套为 `248 passed, 1 skipped, 4 subtests passed`，`pip check`、Python 编译、Shell 语法和 `git diff --check` 均通过。修复与验证低于一小时，不增加 checkpoint、resume 或额外 smoke 阶段。

### Git sync status

- 状态：`ready to commit`；目标为 `origin/main`。
- 计划提交信息：`Fix PLM Sol Python 3.8 compatibility`。

## 2026-08-19 19:10:00

### PLM_Sol—BL21产量正式验证结果同步与决策

- 同步并核对远程正式制品：PLM_Sol完成47/47条唯一序列评分，保留31条LTT/WCC个体近似数值和16条LLJ有序/删失语义；结果gate与run summary均为`pass`，其中`pass`表示分析制品完整，并不表示预测器获准使用。Nb252正式面板分数为0.9228592，该值是模型预测溶解度分数，不是实测产量或溶解度。
- 连续证据呈正向但跨来源不完全稳定：31条数值记录的合并Spearman为0.561，来源内分层Spearman为0.473，95% bootstrap区间为0.096–0.749，分层置换p=0.017；LTT内部为0.497而WCC内部为-0.096，LLJ有序Kendall为0.099。长度校正偏Spearman为0.401，序列簇外产量预测Spearman为0.277。
- 嵌套分类的逐样本留一与90%序列簇留一结果相同：ROC-AUC 0.638、PR-AUC 0.662、MCC 0.313、balanced accuracy 0.650、sensitivity 0.500、specificity 0.800。ROC-AUC未达到预声明0.65门槛，因此未形成独立的连续与分类联合支持。
- PLM_Sol与NetSolP U/S高度重叠：47条Spearman分别为0.793/0.640，31条数值记录中为0.926/0.715。把PLM_Sol加入NetSolP S后，序列簇外Spearman由0.455降至0.315，增量为-0.140；因此最终证据等级为`no_supported_use`，release为`plm_sol_not_supported_for_candidate_use`，不进入846条单突排序。
- 固定5 mg展示的序列簇留一ROC-AUC/PR-AUC/MCC/balanced accuracy为0.761/0.685/0.411/0.697，但该分析按既定合同仅为展示，不能覆盖正式准入gate。两张PNG已人工检查，数据与表格一致；主图Panel D长标题在位图边缘显示较紧，但不影响表中权威数值，本轮未改动生成结果。
- 本轮未重跑模型或修改结果制品，仅更新replace-in-place handoff和历史结论。PLM_Sol分析run summary记录的13.09秒仅是下游统计/绘图耗时，不代表嵌入和分类器的完整远程运行时间。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Record PLM Sol validation decision`。

## 2026-08-19 20:05:00

### Natural-VHH保守性合同v2：亲本共识一致性与定向回变

- 修正“位置保守”等同于“Nb252当前残基必须冻结”的语义。新合同仅在全局/邻域保守门通过且Nb252亲本残基等于共同优势残基时赋予`hard_conserved`；若保守门通过但亲本不同，则赋予`conserved_nonconsensus`，只开放亲本到共同天然优势残基的唯一回变，仍服从界面、二硫键和末端等重叠硬约束。
- 用未改变的TNP 4,059条冻结输入重新生成独立v2制品。4,057条通过编号/coverage审核，得到3,784个90% identity簇；Nb252邻域仍为1,564条序列和1,532个有效簇。
- v2位置分类为54个`hard_conserved`、1个`conserved_nonconsensus`、33个`cautious`、33个`variable`和7个`insufficient_evidence`。唯一非共识高保守位点为reported Q5/IMGT5，Nb252为Q而邻域/全局优势残基均为V，频率分别为0.99803123/0.99755235。
- 最终硬冻结并集由81个变为80个；47个常规全扫描位置仍产生846条非Cys单突，另仅新增`Nb252_expr_seq005_Q5V`，总计847条。与v1候选ID集合比较未发现删除或其他新增。
- 更新AGENTS、复用代码说明和replace-in-place handoff；旧v1目录保留为历史provenance，后续下游必须读取`vhh_conservation_consensus_v2_20260819`合同。
- 本地真实数据运行228.9秒，低于一小时，无需Slurm、checkpoint或resume。专项测试`6 passed`，全套测试`249 passed, 1 skipped, 4 subtests passed`；Python编译和`git diff --check`通过，新约束轨道PNG已人工检查。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`4acdb5a43b5724f7d1874b5717cbc8945987bb37`（`Refine natural VHH conservation contract`）。

## 2026-08-19 20:30:00

### v2候选合同迁移与NanoMelt/AntiFold决策用途冻结

- 完成活动源码审计：没有现行评分脚本直接消费旧846候选；`build_project_vhh_sequence_logos.py`中的v1目录引用属于已完成Logo历史provenance，不改写。新增一次性下游preflight并以真实v2制品运行通过，核对847条候选、48个候选位置、80个硬冻结位置、24个实验界面位置、CSV/FASTA/亲本身份；候选由846条常规非Cys扫描和1条Q5V共识回变组成，无多突、新Cys或冻结/界面冲突。
- 复用既有43/47条NanoMelt评分和连续证据，在27条可评分LTT/WCC数值记录上补齐训练折内阈值的嵌套分类。逐样本与90%序列簇留出汇总相同：ROC-AUC 0.57142857、PR-AUC 0.55408849、MCC 0.40787852、balanced accuracy 0.70054945、sensitivity 0.78571429、specificity 0.61538462；较高MCC没有得到足够的无阈值排序支持，连续与分类联合结论仍为`no_supported_use`。NanoMelt不得用于BL21产量排序，只保留为预测稳定性相容约束。
- 对AntiFold建立正式分类适用性合同而不伪造指标：47条产量序列中仅Nb252具有匹配实验NK2R复合物，其余46条没有可比实验复合物；AntiFold输出为逐位点结构条件logP而非统一表达标量，因此ROC-AUC、PR-AUC、MCC和阈值估计均`not_applicable`。允许用途限定为实验复合物视图中坐标可评价位点的突变相容性约束，禁止BL21产量排序、跨样本分类、通用ΔlogP阈值和AF3静默补全。
- 本阶段只复用既有结果并进行本地统计/合同写出，总耗时低于一分钟，无需Slurm、checkpoint或resume。专项测试`23 passed`；全套测试`251 passed, 1 skipped, 4 subtests passed`；Python编译、`pip check`和`git diff --check`均通过。NanoMelt正式分类图已人工检查。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`da1adb47d6c98ba30a358af9e62058778a5f6cb4`（`Finalize expression predictor decision contracts`）。

## 2026-08-19 22:45:00

### 847条表达性质结果复用审计与缺口补算实现

- 核对本地历史制品后确认：旧AntiFold表覆盖2318条广义单突并可精确回映当前847条；旧NetSolP/NanoMelt只覆盖其中1962条三视图可评价候选，映射到当前空间后为721条，另有126条未评分。126条集中在实验坐标缺失的reported 11、14、24、26–29，每个位点18条；Q5V已存在完整旧结果。
- 新增v2复用/补全模块和本地计划入口。连接键固定为reported位置、WT、mutant和完整128-aa序列；旧candidate ID只记录为provenance。真实数据计划生成847行映射审计、WT加12条确定性复核面板以及WT加126条补算清单，计数门为`pass`。
- 抽样面板固定包含Q5V、NetSolP U/S和NanoMelt变化极值及FR/CDR代表。远程流程先重跑13条NetSolP/NanoMelt输入，并对三个固定WT结构视图各重跑一次AntiFold；按历史输出精度逐值比较，任何差异都会阻断旧结果复用和后续补算。
- 复核通过后，仅对126条缺口运行NetSolP/NanoMelt，再将721条复用值、126条新值和847条AntiFold值合并。实验缺失位置保留实验视图`not_evaluable`和独立AF3-only证据；最终矩阵不计算综合分、Pareto、Tier或候选选择。
- 单一Slurm任务按复核门、补算和合并顺序执行，使用`batch`、1 GPU、12 CPU、3小时上限，日志写入`logs/expression_property_completion_v2/`。预计30–90分钟、可能超过一小时但低于五小时，因此不增加checkpoint、resume或数组。
- 当前只完成本地实现和计划制品，尚未运行远程三工具复核或126条补算。专项测试`4 passed`；全套测试`255 passed, 1 skipped, 4 subtests passed`；Python编译、Shell语法、`pip check`和`git diff --check`均通过。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`4e9aea7d23acf1d5c86440ee50b66cfe24f74937`（`Implement expression property score completion`）。

## 2026-08-19 22:55:00

### AntiFold重复一致性门的实测精度修正

- 远程复核已完成13条NetSolP/NanoMelt和三个冻结WT结构视图的AntiFold重跑；NetSolP与NanoMelt比较全部通过，只有AntiFold数值门失败，说明序列、ID、视图、评价状态和工具覆盖均已正确匹配。
- AntiFold失败项的最大绝对差为`6.04e-6`，新旧结果在至少五位小数上一致，符合相同模型与输入在GPU推理中的末位浮点差异；原`5e-8`容差低于实测可复现精度约两个数量级，属于假阻断而非模型结果改变。
- 将AntiFold重复验证绝对容差固定为`1e-5`；NetSolP的`5e-8`和NanoMelt的`0.0050001 °C`保持不变。新增边界测试，确认`9e-6`通过、`1.1e-5`阻断，避免无限放宽复用门。
- 首次失败运行在第4步停止，126条缺口尚未补算，完整847行矩阵仍保持`blocked`；修正后需从头重新提交短流程并以新gate为准。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`d23fbfe06e72b3ddb355419aa546e6d33cb17000`（`Fix AntiFold repeat validation tolerance`）。

## 2026-08-19 23:10:00

### 847条表达性质完整矩阵远程结果同步

- 远程重跑完成并由本地回读审核。重复一致性门共183项、0失败：NetSolP 26项与NanoMelt 13项均逐值一致；AntiFold 144项最大绝对差为`5.72e-6`、中位差为`6.0e-7`，通过固定`1e-5`容差，旧结果获准复用。
- 完整矩阵共847条唯一candidate ID和847条唯一128-aa序列，覆盖48个允许位置；846条来自非Cys全扫描，1条为Q5V天然共识回变。逐条验证均为相对权威亲本的单一替换，全部保留末端SSGS，没有冻结位置突变或新增Cys，所需数值均为有限值。
- NetSolP、NanoMelt和AF3 VHH AntiFold均覆盖847条。NetSolP/NanoMelt中721条来自经重复验证的旧值，reported 11、14、24、26–29共126条来自本轮新算；这126条在实验VHH和实验复合物视图中继续为`not_evaluable`，只保留独立AF3-only预测证据。其余721条具有三个AntiFold视图。
- WT基线在847行中严格一致：NetSolP U为0.39849445、S为0.5319069、NanoMelt预测Tm为65.18 °C。相对WT，U/S/Tm正向变化分别为193/303/334条；实验复合物AntiFold在721条可评价候选中60条为正向，AF3视图在847条中74条为正向。这些只是原始景观描述，不构成阈值、投票或候选筛选。
- 最终gate为`pass`并释放`complete_raw_scores_ready_for_next_declared_stage`；全部847行的`candidate_selection_performed=False`，没有计算综合分、Pareto、Tier或30条面板。结果制品已由用户提交并推送，提交为`67f462a4e5f7096245136e4e1ee3a5dc0081d92d`。

### Git sync status

- 状态：已提交并推送至`origin/main`。
- 提交：`3decc36ec66fcd80fe6a0cd45ea2418990b7841f`（`Record complete expression property matrix`）。

## 2026-08-19 23:40:00

### 简并稳定词单突特征与BL21产量验证

- 将用户提供的1,336条稳定词冻结为大小写敏感的12符号合同：酸性/碱性/疏水/酰胺/羟基/小残基/芳香族分别编码为`a/b/h/n/o/s/@`，`C/H/M/P/Y`保留原字符。实现完整精确连续子串扫描，重叠和嵌套命中分别保留；不做模糊匹配或词库扩展。
- 在已通过合同的847条单突上逐条验证亲本、位置、WT、替换残基和完整128-aa序列后比较出现位置。Nb252 WT没有命中稳定词；22条单突新增24个出现，825条不变，无减少项。所有24个变化窗口均覆盖对应突变位点；结果已联接到完整性质矩阵，但没有执行筛选、排名、Pareto、Tier或30条面板选择。
- 在冻结47条BL21产量记录上独立验证稳定词描述符。主指标为按所有可能词窗归一化的命中密度；31条个体数值记录的直接合并Spearman为-0.228、来源分层Spearman为-0.185、95% bootstrap区间为-0.572–0.216、分层置换`p=0.367`。序列簇留一ROC-AUC为0.431、PR-AUC为0.545、MCC为-0.277；16条LLJ继续保持有序/删失语义。
- 证据等级固定为`no_supported_use`：现有数据不支持稳定词越多则BL21产量越高。稳定词增加只保留为用户指定的低优先级、可解释软偏好，不得覆盖硬约束或明确性质恶化，也不得宣称为实测稳定性或因果表达证据。
- 两个入口均为本地短任务，实际运行低于一分钟，不需要Slurm、checkpoint或resume。新增5项专项测试，覆盖12符号编码、大小写、重叠/嵌套、简并等价替换、变化对账以及真实847/47条集成数据。最终专项测试`5 passed`，全套测试`260 passed, 1 skipped, 4 subtests passed`；制品回读、Python编译、`pip check`和`git diff --check`均通过。

### Git sync status

- 状态：`ready to commit`；目标为`origin/main`。
- 计划提交信息：`Add stable-word single-mutant evidence`。

<!-- HISTORY_APPEND_MARKER: insert new entries immediately above this line -->
